# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | M001 | arch | Control inversion pattern | Closures/callbacks, not traits | Zero-trait codebase convention (0 traits across 17K lines); all 3 brainstorm pairs converged | No |
| D002 | M001 | arch | Orchestration location | `assay-core::orchestrate` module (feature-gated) | Module avoids premature crate boundary; feature gate provides rollback safety | Yes — if module grows past ~3K lines |
| D003 | M001 | arch | Harness adapter location | New `assay-harness` leaf crate | Implementations depend on core, not vice versa; preserves clean dep graph | No |
| D004 | M001 | convention | Manifest format | TOML with `[[sessions]]` array | Forward-compatible for multi-agent; `[[sessions]]` even for single-session avoids breaking change | No |
| D005 | M001 | convention | MCP tool evolution | Additive only — new namespaced tools, never modify existing signatures | Preserves backward compatibility for existing 18 tool consumers | No |
| D006 | M001 | arch | Session vocabulary | AgentSession → GateEvalContext; Smelt manifest → RunManifest; runner → RunExecutor | 5 "session" concepts cause confusion; clean names before adding more types | No |
| D007 | M001 | arch | Launcher sync vs async | Sync (std::process::Command + spawn_blocking) | Single-shot launch is request-response; async adds complexity without benefit until M002 | Yes — if M002 needs async |
| D008 | M001 | arch | Git interaction | Shell out to git CLI, no git2/gix | git2 is !Send+!Sync (breaks spawn_blocking), gix has 150+ transitive deps, git CLI is proven | No |
| D009 | M001 | arch | Session persistence | JSON file-per-record, not SQLite | Single-project scope (tens-to-hundreds of sessions); consistent with existing history module | Yes — if scale exceeds thousands |
| D010 | M001 | arch | HarnessProfile location | Type in assay-types, implementation in assay-harness | Cross-crate via serialization; follows existing pattern for types vs logic separation | No |
| D011 | M001/S03 | convention | Settings merger field construction | Explicit struct construction (no `..base`) | Compile-time safety — adding a field to SettingsOverride forces merge_settings to handle it | No |
| D012 | M001/S03 | convention | Vec merge semantics | Replace (non-empty override wins entirely) | Extend semantics would cause surprising duplicates; replace is predictable and explicit | Yes — if extend is needed later |
| D013 | M001/S05 | convention | session_id scope | Metadata-only (not on WorktreeInfo) | Session linkage is a persistence/lifecycle concern; list/status responses don't need it. Keeps WorktreeInfo lean for MCP consumers | Yes — if S07 pipeline needs session_id in list results |
| D014 | M001/S06 | arch | ManifestSession override shape | Inline optional fields (settings, hooks, prompt_layers) not embedded HarnessProfile | ManifestSession is a TOML authoring surface — users set individual overrides, not a full profile. S07 constructs HarnessProfile from these fields plus defaults | No |
| D015 | M001/S07 | arch | Pipeline harness config injection | HarnessWriter function parameter, not direct assay-harness call | assay-core cannot depend on assay-harness (reverse dependency). Pipeline accepts `Fn(&HarnessProfile, &Path) -> Result<Vec<String>, String>` closure. Concrete claude adapter composition happens at CLI/MCP call site. Consistent with D001 (closures, not traits) and D003 (harness depends on core, not vice versa) | No |
| D016 | M001/S07 | convention | PipelineError wraps String, not AssayError | Error messages captured as String | AssayError is not Clone; pipeline errors need safe collection across sessions. String messages preserve diagnostics without requiring error type refactoring | Yes — if AssayError gains Clone |
| D017 | M002 | arch | DAG executor threading model | `std::thread::scope` with bounded concurrency (default min(sessions, 8)) | Sync-core convention (D007); `std::thread::scope` is zero-dependency, guarantees join before return. Rayon/tokio add deps for no gain at this scale (≤20 nodes). | No |
| D018 | M002 | arch | Worktree creation serialization | Mutex around `git worktree add`, parallelize only agent execution | Concurrent worktree creation races on `.git/worktrees/` lock files with confusing errors. Creation is ~100ms; agent execution is minutes. Mutex costs nothing. | Yes — if git improves concurrent worktree add |
| D019 | M002 | arch | Merge execution strategy | Topological-order sequential merge with `git merge --no-ff`, re-check before each merge | Order-sensitive: merging A then B can succeed while B then A conflicts. Re-checking after each merge ensures correct conflict detection against updated base. | No |
| D020 | M002 | arch | Partial failure semantics | Skip dependents of failed sessions, continue independent sessions | Clear contract: failure doesn't cascade to unrelated work. Dependents get `Skipped { reason }` with reference to the failed upstream session. | No |
| D021 | M002 | convention | Session dependency reference | `depends_on: Vec<String>` referencing session `name` (or `spec` if no name) | Sessions need stable identifiers for dependency edges. `name` field already exists as `Option<String>` on ManifestSession. Require unique names when dependencies are declared. | Yes — if index-based addressing proves simpler |
| D022 | M002 | arch | Orchestrator state persistence | JSON file under `.assay/orchestrator/` | Consistent with session/history JSON-per-record pattern (D009). Enables `orchestrate_status` to read state without holding executor reference. | No |
| D023 | M002 | arch | Smelt absorption strategy | Port & adapt — use Smelt as reference spec, rewrite to Assay conventions | Smelt has ~10K lines of battle-tested orchestration (DAG, parallel exec, merge runner, scope checking). Port logic but replace traits with closures (D001), keep sync core (D007), hand-roll DAG (no petgraph). Smelt pivots to infra role (environments, credentials, process lifecycle). | No |
| D024 | M002 | arch | DAG implementation | Hand-rolled Kahn's algorithm (~30 lines), no petgraph | Smelt uses petgraph (200+ transitive deps) for a graph with ≤20 nodes. Kahn's toposort is textbook, keeps deps lean, consistent with sync-core convention. Reference: Smelt's dag.rs for ready_set and mark_skipped_dependents semantics. | No |
| D025 | M002 | arch | Merge ordering strategies | Port Smelt's completion-time + file-overlap strategies | Smelt's ordering module is ~150 lines of pure logic. File-overlap greedy algorithm minimizes conflicts. Port as-is, adapt types to Assay conventions. | No |
| D026 | M002 | arch | Conflict handler contract | Closure-based, not trait (supersedes Smelt's ConflictHandler trait) | Smelt uses `trait ConflictHandler`. Assay uses `Fn` closures per D001. Handler receives session name, conflicting files, scan results, work dir. Returns Resolved/Skip/Abort. AI implementation deferred to M003 (R026). | No |
| D027 | M002 | arch | Scope enforcement mechanism | Port Smelt's globset-based scope checking | Smelt's scope.rs is ~80 lines: file_scope + shared_files globs, returns Vec<ScopeViolation>. Advisory by default (not blocking). Port as-is. Adds globset dep to assay-core. | No |
| D028 | M002 | scope | R024 pulled to M002 | Codex and OpenCode adapters in M002, not M003 | User specified CLI surface for all three adapters: `assay harness generate claude-code\|codex\|opencode`. All three needed for harness CLI to be complete. Supersedes R024 provisional M003 assignment. | No |
| D029 | M002 | arch | Smelt/Assay boundary | Assay = harness config + local orchestration; Smelt = infra (environments, credentials, job lifecycle) | Smelt manifest references `harness = "claude-code"` and calls `assay harness generate`. Assay's `assay run` handles local dev orchestration. Smelt handles CI/CD provisioning. | No |
| D030 | M002 | scope | OpenTelemetry instrumentation timing | Deferred to a future milestone (M004+) | OTel tracing/metrics are valuable but cross-cutting — adding mid-M002 would bloat every slice. Better as a dedicated pass after the orchestration and harness surfaces stabilize. Design instrumentation points during M002 (span boundaries at pipeline stages, session lifecycle, merge phases) but don't wire them. | Yes — pull forward if observability becomes a blocker |
| D031 | M002/S02 | arch | Two-phase pipeline split for worktree serialization | Split `run_session()` into `setup_session()` (stages 1-2) and `execute_session()` (stages 3-6) | Executor needs to serialize worktree creation (D018) while parallelizing agent execution. Splitting the pipeline lets the executor hold a `Mutex<()>` only during setup (worktree create, ~100ms) while running the slow agent execution in parallel across threads. `run_session()` preserved as composition of both. | No |
| D032 | M002/S02 | arch | Session runner as closure parameter for testability | `run_orchestrated()` accepts a `&SessionRunner` closure, not hardcoded `run_session()` | `run_session()` spawns real agent subprocesses — untestable in unit tests. Closure parameter (consistent with D001) allows mock runners that simulate success/failure/timing without real agents. Production callers pass `run_session` or a setup+execute wrapper. | No |
| D033 | M002/S02 | arch | Orchestrate feature gate on assay-types | New `orchestrate` feature in assay-types Cargo.toml for OrchestratorStatus types | OrchestratorStatus, SessionRunState, etc. are orchestration-specific types that should not inflate the base types crate. Feature gate matches the pattern established for assay-core in S01. Enabled transitively by assay-core, assay-cli, and assay-mcp. | Yes — if types are needed without feature |
| D034 | M002/S02 | arch | Generic `F: Fn + Sync` for session runner | `run_orchestrated()` is generic over `F: Fn(&ManifestSession, &PipelineConfig) -> Result<PipelineResult, PipelineError> + Sync` instead of `dyn` trait object | `dyn Fn` requires `'static` bound that prevents test closures from capturing stack-local `AtomicUsize`/`Mutex` state. Generic parameter is zero-cost and equivalent API surface. | No |
| D035 | M002/S02 | arch | HarnessWriter excluded from `run_orchestrated()` signature | Caller captures HarnessWriter in the session runner closure | `HarnessWriter = dyn Fn(...)` is not `Sync`, making it unsendable to worker threads directly. Capturing in the closure is simpler and consistent with D001 (closures, not traits). | No |
| D036 | M002/S05 | arch | Scope types in assay-types, logic in assay-harness | ScopeConfig/ScopeViolation/ScopeViolationType in assay-types; check_scope() and generate_scope_prompt() in assay-harness/scope.rs | Scope is a harness-layer concern (generates prompt content for adapters), not core orchestration. Types in assay-types for cross-crate visibility. Consistent with D003 (harness depends on core, not vice versa) and D027 (globset-based advisory scope checking). | No |
| D037 | M002/S05 | convention | Scope prompt injection via PromptLayer, not adapter modification | Scope prompt injected as PromptLayerKind::System with priority -10 before calling existing adapter generate_config() | Keeps adapters pure — they accept HarnessProfile unchanged. Scope enrichment is a call-site concern (CLI/orchestrator adds the layer). Avoids breaking adapter function signatures. | No |
| D038 | M002/S05 | convention | `harness update` overwrites all managed files | `update` regenerates and overwrites all managed config files; users should run `diff` first to preview | Matches terraform/helm config-management convention. Attempting partial merge of user edits with generated config is error-prone and underspecified. | Yes — if users request selective update |
| D039 | M002/S06 | convention | Multi-session detection heuristic | Route to orchestrator when `sessions.len() > 1` OR any session has non-empty `depends_on` | Simple, deterministic, backward-compatible. Single-session manifests without depends_on always use the existing `run_manifest()` path. A single session with depends_on (nonsensical but valid TOML) routes to orchestrator which handles it correctly as a degenerate case. | Yes — if explicit `orchestrate = true` field is preferred |
| D040 | M002/S06 | convention | Hardcode Claude Code adapter in orchestrated runs | Session runner in orchestrate_run MCP tool and CLI multi-session path hardcodes Claude Code adapter | ManifestSession has no adapter field yet. All three adapters exist but per-session adapter selection is a future concern. Consistent with existing run_manifest behavior. | Yes — when per-session adapter field is added |
| D041 | M002/S06 | convention | .assay/orchestrator/ must be gitignored | Add `.assay/.gitignore` with `orchestrator/` entry in projects using orchestration | Orchestrator persists state.json under `.assay/orchestrator/<run_id>/`. Without gitignore, `git add .` in session runners stages these files on branches, and `git checkout main` deletes the directory — breaking state persistence and merge-phase clean-worktree checks. Discovered during T03 integration testing. | No |
| D042 | M003 | scope | R025 (SessionCore) deferred indefinitely | Defer — do not implement SessionCore `#[serde(flatten)]` composition | Only 3 fields overlap across `GateEvalContext` and `WorkSession` (`id`/`session_id`, `spec_name`, `created_at`). Naming inconsistency (`session_id` vs `id` vs `name`) and semantic differences suggest these aren't the same "session" concept. `#[serde(flatten)]` is incompatible with `deny_unknown_fields` (serde #1358). `GateEvalContext` uses `deny_unknown_fields`. Cost/benefit unfavorable — churn exceeds clarity gain. | Yes — if a fourth session type emerges with genuine overlap |
| D043 | M003 | arch | Sync subprocess for conflict resolution | Use `std::process::Command` (sync) for conflict resolution subprocess, not async `run_evaluator()` | Merge runner runs in `std::thread::scope` workers — async evaluator would require runtime bridge or `spawn_blocking` inversion. Conflict resolution is a one-shot subprocess call, same as `merge_execute()`. Reuse the prompt/schema patterns from `evaluator.rs` but spawn synchronously. Consistent with D007 (sync core). | Yes — if conflict resolution needs streaming output |
| D044 | M003 | arch | Two-phase merge lifecycle | `merge_execute()` optionally preserves conflict state for handler resolution | Current auto-abort prevents any handler from resolving because the merge state is gone. New mode: on conflict, collect details without aborting, return to caller for handler resolution. Caller (merge_runner) manages lifecycle: handler resolves → verify commit, handler fails → `git merge --abort`. Default behavior unchanged (auto-abort + skip). | No |
| D045 | M003/S01 | convention | Scripted resolver for deterministic testing | Integration tests use a scripted closure (strip markers + commit) instead of real Claude invocations | Real Claude is slow, non-deterministic, and costs money. Scripted resolvers prove the lifecycle mechanics. Real Claude invocation is UAT only. | No |
| D046 | M003/S01 | arch | ConflictResolutionOutput as crate-local type | `ConflictResolutionOutput` (AI response schema) lives in `assay-core::orchestrate::conflict_resolver`, not `assay-types` | This type is an implementation detail of the resolver subprocess protocol, not a cross-crate persistence/API contract. Keeps `assay-types` lean. | Yes — if S02 audit trail needs to reference it |
| D047 | M003/S01 | convention | ConflictResolutionMode CLI enum is crate-local | `ConflictResolutionMode` (Auto/Skip) lives in `assay-cli`, not `assay-types` | CLI mode enums are presentation/parsing concerns — they don't need to be shared cross-crate. Consistent with D046 (keep assay-types lean) and the pattern established for failure policy and merge strategy in the CLI crate. | No |
| D048 | M003/S01 | convention | MCP conflict_resolution as Option<String> | `OrchestrateRunParams.conflict_resolution: Option<String>` instead of a typed enum | Matches the existing pattern for `failure_policy` and `merge_strategy` in `OrchestrateRunParams`. MCP params are JSON-schema-described strings; validation happens at handler time with a descriptive error message. | No |
| D049 | M003/S02 | arch | ConflictResolutionResult as pub type in assay-core | `ConflictResolutionResult { action, audit, repo_clean }` lives in `assay-core::orchestrate::conflict_resolver` as a `pub` type, not in `assay-types` | It is a function return type for `resolve_conflict()` — an implementation detail of the resolution mechanic, not a persistence contract. `ConflictResolution` (the audit record) goes in `assay-types` because it's persisted in `MergeReport`. `assay-mcp` accesses `ConflictResolutionResult` via `assay_core::orchestrate::conflict_resolver::ConflictResolutionResult`. Supersedes D046 note "Yes — if S02 audit trail needs to reference it". | No |
| D050 | M003/S02 | arch | Handler type in merge_completed_sessions changes to return ConflictResolutionResult | `H: Fn(...) -> ConflictResolutionResult` replaces `H: Fn(...) -> ConflictAction` | The audit trail requires the handler to return more than just an action. `ConflictResolutionResult` carries action, optional audit record, and `repo_clean` flag. `default_conflict_handler()` returns `{ action: Skip, audit: None, repo_clean: false }` — semantically equivalent to the previous `ConflictAction::Skip`. Existing behavior is preserved for all non-resolution paths. | No |
| D051 | M003/S02 | convention | OrchestrateStatusResponse is a local struct in server.rs | `OrchestrateStatusResponse { status, merge_report }` is defined in `assay-mcp::server`, not in `assay-types` | `OrchestratorStatus` has `deny_unknown_fields` and a locked schema snapshot — wrapping it at the MCP handler layer is additive without changing the locked type. The response struct is a serialization concern only, not a persistence contract. Consistent with D005 (additive MCP tools only, no modification to existing signatures). | No |
| D052 | M004 | arch | Mode dispatch via free function, not executor trait | `run_mesh()` and `run_gossip()` are free functions in `mesh.rs` and `gossip.rs` under `assay-core::orchestrate`; mode dispatch in the CLI/MCP call site is a `match mode { Dag => run_orchestrated(), Mesh => run_mesh(), Gossip => run_gossip() }` | Zero-trait convention (D001). Mode-specific executors share the same `SessionRunner` closure type (D034). No `Executor` trait, no dyn dispatch. | No |
| D053 | M004 | arch | Mesh and Gossip modes ignore depends_on | When `mode` is `Mesh` or `Gossip`, `depends_on` fields on `ManifestSession` are ignored with a `tracing::warn` per session that has non-empty deps | Mode is exclusive (user decision). Depends-on processing is strictly a DAG concern. Warning prevents silent misconfiguration. | Yes — if composable modes are desired later |
| D054 | M004 | arch | OrchestratorStatus extended with optional mode-specific fields | `mesh_status: Option<MeshStatus>` and `gossip_status: Option<GossipStatus>` added to `OrchestratorStatus` with `serde(default, skip_serializing_if)` | `OrchestratorStatus` has `deny_unknown_fields` on some fields but the pattern for backward-compatible extension (established in M003 with `resolutions`) is `serde(default) + skip_serializing_if = "Option::is_none"`. Avoids creating a separate response wrapper (consistent with keeping `OrchestratorStatus` as the canonical status type). | No |
| D055 | M004 | convention | MeshConfig and GossipConfig as optional top-level RunManifest fields | `mesh_config: Option<MeshConfig>` and `gossip_config: Option<GossipConfig>` on `RunManifest` | Config is mode-specific; putting it at the top level keeps the manifest flat and avoids a polymorphic union type. Fields are silently ignored when mode doesn't match. | Yes — if a mode-union type is preferred for validation |
| D056 | M004/S01 | convention | `impl Default for RunManifest` instead of struct-literal updates | Added explicit `Default` impl (`mode: Dag`, `sessions: vec![]`, no configs) rather than updating every test that constructs `RunManifest` literals | Adding `deny_unknown_fields` struct fields cascades to all test literal constructors across the workspace. `Default` is the least-surprise fix: tests opt into it with `..Default::default()`, the serde contract is unchanged (no `#[derive(Default)]` visible in schema), and no schema snapshot change was required. | Yes — if Default semantics diverge from a useful "empty manifest" later |
| D057 | M004/S02 | arch | `persist_state` made `pub(crate)` in `executor.rs` | `persist_state` is elevated from private to `pub(crate)` so `mesh.rs` can reuse the atomic tempfile-rename pattern without duplication | `persist_state` is a tested, battle-hardened atomic write helper (tempfile + rename + fsync). Duplicating it in `mesh.rs` would introduce drift risk. `pub(crate)` is the minimal visibility needed — it stays an implementation detail of `assay-core::orchestrate`. Consistent with the "don't hand-roll what already exists" principle from S02 research. | Yes — if a shared persistence utility module is extracted later |
| D058 | M004/S02 | convention | Mesh roster PromptLayer uses "Outbox: <path>" as machine-parseable line | The roster layer's text includes `Outbox: <path>` on a dedicated line so sessions (and integration tests) can parse their own outbox path by scanning for a line starting with "Outbox: " | Sessions need to know their outbox path at runtime to write messages. Encoding it in the roster prompt layer (the same layer that names peers) keeps the contract self-contained. The `starts_with("Outbox: ")` parse is intentionally trivial — any agent or test can extract the path. | Yes — if a structured roster format (TOML/JSON) is preferred later |
| D059 | M004/S03 | convention | Gossip PromptLayer uses "Knowledge manifest: <path>" as machine-parseable line | The gossip knowledge-manifest layer's text includes `Knowledge manifest: <path>` on a dedicated line so sessions (and integration tests) can parse the manifest path by scanning for a line starting with "Knowledge manifest: " | Mirrors D058 convention. Sessions need to know the manifest path to read it during execution; encoding it in the injected PromptLayer makes the contract self-contained. Integration tests extract the path the same way an agent would. | Yes — if a structured layer format is preferred later |
| D071 | M005/S02 | arch | CycleStatus lives in assay-core::milestone::cycle, not assay-types | `CycleStatus { milestone_slug, milestone_name, phase, active_chunk_slug, completed_count, total_count }` is defined in `assay-core::milestone::cycle` and re-exported. Not in assay-types. | CycleStatus is a derived view type, not a persisted contract. assay-mcp already depends on assay-core so cross-crate access is free. Consistent with D046/D051 pattern of keeping assay-types lean — only persistence/API-contract types belong there. | Yes — if CycleStatus needs to be referenced from assay-types |
| D072 | M005/S02 | convention | cycle_advance CLI error exits with code 1 via eprintln, not anyhow propagation | `milestone_advance_cmd` prints `"Error: {e}"` to stderr and returns `Ok(1)` instead of `Err(e)` on cycle_advance failure | Consistent with the existing CLI pattern where domain errors (not programmer bugs) are reported as non-zero exit codes rather than panics/anyhow chains. Matches `just ready` convention that expects controlled exit codes for expected failure states. | No |
| D073 | M005/S02 | convention | ChunkStatusResponse is a local struct in server.rs | `ChunkStatusResponse { chunk_slug, has_history, latest_run_id, passed, failed, required_failed }` defined in assay-mcp::server, not assay-types | Same pattern as D051 (OrchestrateStatusResponse). It is a serialization-only response struct for the `chunk_status` MCP tool — not a persisted type. Keeping it local avoids polluting assay-types with transient response shapes. | No |
| D060 | M004/S03 | arch | Coordinator thread uses mpsc channel for session completion events | `std::sync::mpsc::channel::<GossipCompletion>()` for coordinator-worker communication; coordinator exits when `recv_timeout` returns `Disconnected` after all worker `Sender` clones drop; drain with `try_recv` before final `knowledge.json` write | Cleaner termination semantics than a shared `Mutex<Vec<>>` queue — channel disconnect is a natural completion signal. `drop(tx)` in parent before scope-join ensures the coordinator exits. The drain loop prevents last-message loss when all workers finish rapidly before the coordinator's next `recv_timeout`. | No |
| D061 | M004/S04 | convention | `execute_mesh` / `execute_gossip` use HarnessWriter pattern without merge phase | CLI functions for mesh/gossip reuse the same `Box<HarnessWriter>` closure construction as `execute_orchestrated()` (D035) but skip Phase 2 (checkout) and Phase 3 (merge) — Mesh/Gossip produce parallel outcomes only; no branch merging. `OrchestrationResponse.merge_report` is a zero-filled struct (D005 additive — no new response types needed). | Yes — if mesh/gossip gain a post-run merge phase in a future milestone |
| D062 | M005 | arch | Milestone persistence format | TOML files in `.assay/milestones/<slug>.toml`, not JSON | Consistent with all other Assay spec artifacts (specs, gates, config). TOML is human-readable and hand-editable, which matters for a file the wizard writes and users review. JSON is reserved for runtime-generated records (sessions, history, orchestrator state). | No |
| D063 | M005 | arch | Chunk = spec with parent metadata | Chunks are specs — `GatesSpec` gains backward-compatible `milestone: Option<String>` and `order: Option<u32>` fields; no separate chunk type | A separate `Chunk` type would duplicate the gate criteria model. Adding optional fields to GatesSpec is a zero-cost backward-compatible extension. Existing specs without these fields continue to work unchanged. | No |
| D064 | M005 | arch | Milestone module in assay-core | `assay-core::milestone` module (milestone_load, milestone_save, milestone_scan, cycle.rs for state machine) | Mirrors the existing `assay-core::spec` module structure. Keeps milestone I/O and cycle logic in assay-core where all domain logic lives. assay-cli and assay-mcp consume via function calls, consistent with zero-trait convention (D001). | No |
| D065 | M005 | arch | PR creation shells out to `gh` CLI | `assay pr create` calls `gh pr create` via `std::process::Command`, consistent with D008 (git CLI-first) | `gh` is the standard GitHub CLI and is already required for most developers using GitHub Actions. Shelling out avoids a GitHub API client dependency and stays consistent with the project's CLI-first subprocess pattern. Graceful error when `gh` is missing or unauthenticated. | Yes — if direct GitHub API is needed for non-GitHub hosts |
| D066 | M005 | convention | Wizard uses `dialoguer` crate for interactive prompts | Interactive CLI wizard uses `dialoguer` for text input, selection, and confirmation | `dialoguer` is widely used (10M+ downloads), zero-unsafe, and produces a good TTY experience. Consistent with Rust ecosystem conventions. `inquire` is an alternative but adds more deps. `dialoguer` covers all wizard needs (text, select, confirm). | Yes — if richer widgets (autocomplete, multiselect) are needed |
| D067 | M005 | convention | New MCP tools use `milestone_` and `cycle_` prefixes | Milestone tools: `milestone_list`, `milestone_get`, `milestone_create`. Cycle tools: `cycle_status`, `cycle_advance`, `chunk_status`. PR tool: `pr_create`. Spec authoring: `spec_create` | Additive naming consistent with D005 (additive MCP tools only). Namespace prefixes (`milestone_`, `cycle_`) mirror the existing `spec_`, `gate_`, `session_`, `worktree_` patterns. | No |
| D068 | M005 | arch | TUI is the preferred primary surface (M006+) | The TUI (`assay-tui`) is the primary product surface; CLI is the power-user / scripting surface; plugins are agent-tool integrations. M006+ builds the TUI as a first-class application. | User decision: TUI should serve both dashboard and workflow purposes; be configurable like Kata. This drives the M006 milestone as a full Ratatui application, not just a status overlay. | No |
| D069 | M005/S01 | convention | Milestone.slug is stored in TOML (not derived from filename) | `slug: String` is a required field in the TOML file (like `GatesSpec.name`). The filename `<slug>.toml` is the lookup key, but the `slug` field inside the TOML is authoritative. `milestone_save` ensures both match. | Consistent with `GatesSpec.name` convention and avoids `#[serde(skip)]` complexity. Simple, deterministic round-trip. | No |
| D070 | M005/S01 | arch | S01 verification strategy: contract + integration proof | S01 uses unit tests (types, TOML round-trip) + integration tests (file I/O, MCP tool registration, CLI stub). Schema snapshots locked by `cargo insta review`. No agent spawning; no end-to-end workflow. | S01 is the type/I/O foundation — the verification goal is contract determinism, not full workflow operation. Full workflow proven in S02+. | No |
| D074 | M005/S03 | convention | Test-first contract overrides plan API shapes for wizard | T01 contract tests are the authoritative API contract; T02 implementation follows the tests, not the plan's proposed struct names/shapes. Plan proposed `ChunkInput { name, criteria: Vec<CriterionInput> }` with auto-derived slug; tests used `WizardChunkInput { slug, name, criteria: Vec<String> }` with caller-provided slug. Implementation matches tests. | Plan shapes are pre-implementation speculation. Tests are the real contract. When tests and plan diverge, tests win — this is the test-first discipline. Deviations documented in S03-SUMMARY.md. | No |
| D075 | M005/S03 | convention | WizardChunkInput slug is caller-provided, not derived | `WizardChunkInput.slug` is a required field provided by the caller, not auto-derived from `name` via `slugify()`. The CLI calls `slugify(name)` before constructing the struct; the MCP tool receives the slug directly from the caller. | Caller-provided slug gives more control and avoids a hidden auto-slug that could surprise callers. MCP callers (agents) know the slug they want; the CLI can derive it from the name. | Yes — if auto-derivation is desired at struct construction time |
| D076 | M005/S03 | convention | create_spec_from_params criteria as Vec<String> | `create_spec_from_params` accepts `criteria: Vec<String>` (descriptions only), not `Vec<CriterionInput>`. Generated `gates.toml` criteria have description but no `cmd` field. Runnable gates require manual editing or a future extension. | Tests used `Vec<String>`; simpler than a full CriterionInput conversion layer. The tradeoff is that wizard-generated specs are not immediately runnable — they require a `cmd` field to be useful. This is a known limitation documented in S03-SUMMARY.md and S03-UAT.md. | Yes — if the wizard collects cmd per criterion in a future iteration |
| D077 | M005/S04 | arch | pr_create_if_gates_pass uses `gh --json number,url` for stable output | `pr_create_if_gates_pass` invokes `gh pr create --json number,url` instead of parsing plain-text stdout URL. Returns `{"number": N, "url": "..."}` as stable machine-readable output. | Parsing a URL from plain stdout is fragile — `gh` does not document the plain-text format as stable. `--json` is explicitly supported and produces schema-backed output. `serde_json::from_slice` parses the result. Consistent with D065 (gh CLI-first subprocess). | No |
| D078 | M005/S04 | convention | ChunkGateFailure and PrCreateResult live in assay-core::pr | Local structs in `assay-core::pr`, not in `assay-types`. `ChunkGateFailure { chunk_slug, required_failed }` is a gate-check return type; `PrCreateResult { pr_number, pr_url }` is a function return type. Neither is a persisted contract. | Consistent with D046/D051/D073 (response-only types stay local; assay-types is for persistence/API contracts only). assay-mcp and assay-cli access these via `assay_core::pr::*`. | No |
| D079 | M005/S04 | convention | S04 test-first: tests/pr.rs written in T01 before assay-core::pr exists | `tests/pr.rs` is written in T01 (red state — compile error) before `assay-core::pr` is implemented in T02. This follows the test-first discipline established in S02/S03. | Tests are the contract; implementation follows the tests. Each test binary in `tests/` compiles independently, so the pr test binary failing to compile does not block other test targets from running. | No |
| D080 | M005/S05 | convention | `{"active":false}` detection in bash via `jq 'has("milestone_slug")'` | The `cycle_status` MCP tool returns `{"active":false}` as a sentinel — the key `active` only appears in this sentinel, not in the `CycleStatus` struct. Bash detection: `echo "$JSON" \| jq -e 'has("milestone_slug")'` — true means an active milestone is present; false means the sentinel was returned. | `CycleStatus` has `milestone_slug` as a required field. The sentinel `{"active":false}` lacks `milestone_slug`. This is a stable structural invariant — more robust than checking for a key that doesn't exist in the real schema. | No |
| D081 | M005/S05 | convention | `assay milestone status --json` exits 0 always (no domain error exit codes) | `milestone_status_cmd` with `--json` follows D072: on `Err(e)`, prints to stderr and returns `Ok(1)`; on success (including the no-active-milestone case), returns `Ok(0)`. Callers (bash hooks) only need to check exit code 0 to know JSON was written to stdout. | Consistent with all other CLI domain errors. Hook scripts check `$?` after the call — exit 0 = JSON is valid; exit 1 = I/O problem (treat as graceful degradation, fall through to `--all`). | No |
| D082 | M005/S06 | convention | S06 all files authored in a single T01 pass | All 6 Codex plugin files (AGENTS.md + 5 skills) written in one task rather than split across tasks. | S06 is pure markdown with confirmed MCP tool signatures from S01–S03. No compilation, no schema changes. Six files × ~30-50 lines each fits comfortably in a single context window. Splitting would add ordering overhead for no benefit. | Yes — if a future skill requires Rust-side changes |
| D083 | M005/S06 | convention | cycle-status and next-chunk are separate Codex skills | `cycle-status.md` = overview-first (milestone phase + chunk summary); `next-chunk.md` = chunk-detail-first (active chunk criteria + gate status). Slight overlap is intentional. | User decision (S06-CONTEXT): agents benefit from a lightweight status check separate from a full chunk-detail dump. `cycle-status` answers "where am I?" while `next-chunk` answers "what do I need to implement?" — different intents warrant separate skills. | Yes — if users find the overlap confusing |
| D084 | M005/S05 | convention | Skill interview-first pattern | All Claude Code skills that create artifacts collect all user inputs conversationally BEFORE making any MCP tool calls | Premature MCP calls risk partial state — if the user abandons mid-interview, nothing is persisted. Collecting all inputs first, confirming a summary, then calling tools is atomic from the user's perspective and avoids orphan milestones with no specs. | No |
| D085 | M005/S05 | convention | next-chunk skill has two null guards | `next-chunk/SKILL.md` guards against both `active: false` (no active milestone) and `active_chunk_slug: null` (milestone in Verify phase — all chunks complete). The Verify phase null guard explicitly tells the user to run `assay pr create`. | Verify phase is a real state where `active_chunk_slug` is null but the milestone is not done — it's waiting for a PR. Without the explicit guard, the skill would treat "all chunks done" as an error. The user message "run `assay pr create`" closes the workflow loop. | No |
| D086 | M005/S05 | convention | cycle-stop-check.sh guard ordering: jq → stop_hook_active → MODE → dir → binary → work | Guards execute in the order: (1) jq available, (2) stop_hook_active env var, (3) ASSAY_STOP_HOOK_MODE=off, (4) .assay/ dir exists, (5) assay binary exists, then cd → cycle detection → gate evaluation | stop_hook_active (Guard 2) must immediately follow jq (Guard 1) to prevent infinite loops before any I/O. MODE (Guard 3) provides the user's escape hatch before any disk reads. dir/binary guards prevent confusing error messages from missing infrastructure. | No |
| D087 | M005/S05 | convention | BLOCKING_CHUNKS named verbatim in Stop hook block reason | The Stop hook block output includes `$BLOCKING_CHUNKS` (space-separated chunk slugs) in the `reason` JSON field: `"reason": "... in chunks: chunk-a chunk-b ..."` | The agent reading the Stop hook output needs to know which chunk to target without a separate diagnostic call. Naming the chunk slugs directly in the reason enables the agent to immediately invoke `/assay:gate-check <slug>` or `chunk_status` to diagnose. Without named slugs, the block reason is actionable only to humans. | No |
| D088 | M006 | arch | `assay-tui` binary name is `assay-tui`, not `assay` | Add `[[bin]] name = "assay-tui" path = "src/main.rs"` to `crates/assay-tui/Cargo.toml`; keep `assay-cli` claiming `name = "assay"` | `assay-cli` already declares `[[bin]] name = "assay"` — two workspace crates cannot produce the same binary name. TUI uses `assay-tui`; M006-CONTEXT noted this was a naming decision to resolve before S01. Alternative (CLI dispatching to TUI when invoked with no subcommand) is deferred — too much scope change for M006. | Yes — if the CLI gains a subcommand-less TUI dispatch in a future milestone |
| D089 | M006 | arch | TUI `App` struct with `Screen` enum for screen dispatch | `App { screen: Screen, milestones: Vec<Milestone>, list_state: ListState, project_root: Option<PathBuf>, config: Option<Config>, show_help: bool }` with `Screen` enum variants per view | Classic Ratatui architecture: single `App` owns all state; `draw(frame)` matches on `App.screen` to call the appropriate free render function; `handle_event(event)` mutates `App.screen` for navigation. Free functions (D001), no `Widget` trait impls on app state. Consistent with zero-trait convention. | No |
| D090 | M006 | arch | Wizard state as `WizardState` nested in `Screen::Wizard` variant | `Screen::Wizard(WizardState)` where `WizardState { step, fields, cursor, chunk_count }` holds all multi-step form state inline | Wizard is a TUI concern only — its state struct lives in `assay-tui`, not `assay-core`. Core provides `create_from_inputs`; the TUI provides the form rendering and event handling. Keeps `assay-core` clean of rendering concerns. Do NOT use `dialoguer` inside the event loop (Pitfall 2 from research). | No |
| D091 | M006 | arch | TUI data loading is synchronous, on navigation transitions only | Load milestone data synchronously in `handle_event` when the user navigates to a new screen (e.g. pressing Enter on a milestone triggers `milestone_load` before rendering the next frame); no background thread for S01–S04 | Sync-core convention (D007). `milestone_scan` and `history::load` are O(file count) and fast for typical project sizes. Blocking on navigation events (not inside `terminal.draw()`) is safe and avoids complexity. Revisit with `std::thread::spawn + mpsc` if profiling shows latency. | Yes — if gate history loading shows observable lag during S01 integration testing |
| D092 | M006 | arch | `ProviderConfig` in `assay-types` follows the D056 pattern exactly | New `ProviderConfig { provider: ProviderKind, planning_model: Option<String>, execution_model: Option<String>, review_model: Option<String> }` with all `Option` fields using `serde(default, skip_serializing_if = "Option::is_none")`; `Config.provider: Option<ProviderConfig>` with same attrs; schema snapshot updated | `Config` has `deny_unknown_fields` — failing to apply `serde(default)` on the new field causes existing `config.toml` files without the field to fail to load. This is a breaking change if done wrong. D056 pattern is the established safe path for backward-compatible Config extension. | No |
| D093 | M006 | arch | `config_save` free function in `assay-core::config` using NamedTempFile pattern | `config_save(root: &Path, config: &Config) -> Result<()>` added to `assay-core::config::mod` using the same `NamedTempFile::new_in + write_all + sync_all + persist` pattern as `milestone_save` | Atomic config writes prevent partial writes that would corrupt `config.toml`. Consistent with the established atomic-write pattern (D062, all prior persistence code). `assay-tui` must not write config files directly — goes through `assay-core` to maintain the clean dep graph and consistent write semantics. | No |
| D094 | M006/S02 | convention | ChunkCount digit validation uses replace-semantics, silently ignores invalid chars | When the wizard is at the ChunkCount step, `Char(c)` replaces the buffer contents rather than appending, and only '1'–'7' are accepted; '0', '8'+, and non-digits are silently ignored (field unchanged, no error set) | A count field should show exactly one digit, not accumulate characters. Silent ignore avoids confusing error messages for accidental keypresses. Range 1–7 matches the maximum chunk count established in create_from_inputs. | Yes — if the range or validation UX needs to change |
| D095 | M006/S02 | arch | Combined bin+lib crate: app.rs in binary module tree, wizard types accessed via assay_tui:: lib path | `main.rs` declares `mod app;`; `app.rs` imports wizard types as `use assay_tui::wizard::*` and `use assay_tui::wizard_draw::*` via the lib crate path | Standard Rust combined bin+lib pattern: the binary module tree (`main.rs` + `mod app`) is separate from the library crate (`lib.rs` + `pub mod wizard`). Integration tests in `tests/` also use `use assay_tui::*`. This is the only correct pattern — `mod wizard;` directly in `main.rs` would not make wizard types visible to integration tests. | No |
| D096 | M006/S02 | convention | draw() always renders Dashboard first, then conditionally overlays Wizard popup | `App::draw()` renders the Dashboard unconditionally, then calls `draw_wizard(frame, state)` if `screen` is `Screen::Wizard` — popup overlays the dashboard | Avoids borrow-split on `self.screen` while rendering. Dashboard as background context behind popup is also visually useful. Limitation: Dashboard is rendered even when completely hidden. For S03/S04 full-screen views (MilestoneDetail, ChunkDetail), draw() will need a proper match on screen variant rather than unconditional Dashboard render. | Yes — refactor in S03 when full-screen views are added |
| D097 | M006/S01 | pattern | Screen-specific render fns take individual fields, not `&mut App`, to satisfy borrow checker with stateful widgets | `draw_dashboard(frame, milestones: &[Milestone], list_state: &mut ListState, scan_error: Option<&str>)` rather than `draw_dashboard(frame, app: &mut App)` | Rust's borrow checker rejects simultaneous borrows when matching on `&app.screen` while needing `&mut app.list_state` for `render_stateful_widget`. Passing fields independently bypasses this. Standard Ratatui pattern for stateful widgets. Applies to all future screen-specific render fns (S03 MilestoneDetail/ChunkDetail, S04 Settings). | No |
| D098 | M006/S03 | pattern | `..` pattern in `draw()` match arms sidesteps Screen-variant borrow-split for detail fields | For `Screen::MilestoneDetail { .. }` and `Screen::ChunkDetail { .. }` arms, use `..` to avoid binding variant fields; any slug needed for the render call is already stored in `App.detail_milestone` or `App.detail_spec`. For `handle_event()` arms where slug data IS needed, clone the variant field into a local variable before mutating `self.screen`. | Avoids holding a borrow on `self.screen` data while also borrowing `&mut self.detail_list_state`; `..` means only `self.screen` itself is borrowed (not its contents) during the match arm. Consistent with D097 (pass individual fields, not `&mut self`). The clone-then-mutate pattern in `handle_event()` is the established solution for ChunkDetail Esc where `milestone_slug` must be read from the current screen before transitioning. | No |
| D099 | M006/S03 | arch | App-level `detail_*` fields for MilestoneDetail and ChunkDetail loaded data | `App.detail_milestone: Option<Milestone>`, `App.detail_list_state: ListState`, `App.detail_spec: Option<GatesSpec>`, `App.detail_spec_note: Option<String>`, `App.detail_run: Option<GateRunRecord>` stored on `App` rather than inside Screen enum variants | Storing data in App fields (not Screen variants) preserves the existing `match &self.screen` pattern in `draw()` — avoids `match &mut self.screen` refactor. Consistent with D089 (App owns all state). detail_list_state selection is preserved across ChunkDetail→MilestoneDetail Esc transitions (correct UX: position retained). detail_milestone is preserved on Esc from ChunkDetail so no reload needed when returning to MilestoneDetail. | No |
| D100 | M006/S03 | convention | Criterion/gate-run join by exact name match; unmatched spec criteria show as Pending | `join_results(criteria, run)` iterates `GatesSpec.criteria`, looks up each `criterion.name` in `GateRunRecord.summary.results[*].criterion_name` by linear scan; unmatched criteria → `None` (Pending); `CriterionResult.result = None` (skipped) → `None` (Pending); `result = Some(GateResult { passed: true })` → `Some(true)` (pass); `passed: false` → `Some(false)` (fail). Run results with no matching spec criterion are ignored. | Spec may be updated (criterion renamed) after a run — the join must tolerate mismatches in both directions without panicking or showing spurious data. Linear scan is O(n²) in criteria count but typical sizes are ≤15 criteria, making this negligible. Consistent with the research recommendation (S03-RESEARCH.md "Criterion name join mismatch" pitfall). | Yes — if sparse indexing is needed for large specs |
| D101 | M006/S04 | convention | Settings screen `w` key saves; `Esc`/`q` cancel | `w` = write/save config and return to Dashboard; `Esc`/`q` = cancel without saving | `s` is already taken (opens Settings from Dashboard). `w` for "write" is a natural mnemonic (vim/nano convention) and unambiguous. Avoids overloading `Enter` which would conflict with future list-item activation semantics. | Yes — if Enter-to-save is preferred in a future UX review |
| D102 | M006/S04 | convention | Settings screen is full-screen, not a popup | `draw_settings` renders as a full-screen bordered block (not a centered popup like draw_wizard) | The Settings screen requires enough vertical space to show three provider options, model hints, and an error line. The wizard popup approach works for 14-line multi-step forms but is inappropriate for a selection list. Full-screen matches the Dashboard pattern. | No |
| D103 | M006/S04 | convention | `w` save in Settings with no loaded config shows inline error, not panic | If `App.config` is `None` when `w` is pressed in Settings (e.g. no config.toml exists), `state.error` is set to a user-readable message and the Settings screen stays open | When `project_root` is Some but config.toml doesn't exist, we cannot derive a valid Config (no project_name). Inline error is the safest UX — the user sees why save failed and can run `assay init` to create the config. Consistent with S02's wizard error-in-state pattern. | Yes — if auto-creating a minimal config.toml is desired in future |
| D104 | M006/S05 | convention | Help overlay event guard: when `show_help = true`, only `?` or `Esc` are processed; all other keys are no-ops | When `App.show_help` is `true`, `App::handle_event` checks `show_help` at the very top, toggles it off on `?` or `Esc`, then returns `false` for ALL keys (including quit). The normal `match self.screen` dispatch is bypassed entirely while help is visible. | Prevents accidental navigation through the help overlay — pressing `q` while help is open should close help, not quit. The guard is placed before the `Screen::Wizard` check for global coverage. | No |
| D105 | M006/S05 | pattern | All `draw_*` functions accept explicit `area: Rect`; `draw()` does the global layout split once | Every screen renderer (`draw_no_project`, `draw_load_error`, `draw_dashboard`, `draw_milestone_detail`, `draw_chunk_detail`, `draw_wizard`, `draw_settings`) accepts `area: Rect` as second parameter. `App::draw()` computes `[content_area, status_area]` from `frame.area()` once at the top and passes `content_area` to all screen renderers. `draw_help_overlay` receives the full `frame.area()` since the popup spans the whole terminal. | Eliminates all internal `frame.area()` calls inside screen renderers; enforces a single layout authority. Consistent with D097 (pass individual fields, not `&mut self`). `draw_help_overlay` intentionally uses `frame.area()` not `content_area` so the help popup is centered over the full terminal including the status bar. | No |
| D106 | M006/S05 | convention | `App.cycle_slug` cached on `App`; refreshed only at lifecycle transitions | `cycle_slug: Option<String>` is loaded once in `with_project_root`, refreshed after wizard Submit success, and refreshed after Settings `w` save. It is NOT re-read on every frame (inside `terminal.draw()`). Errors from `cycle_status` degrade to `None` via `.ok()` — status bar renders without a slug rather than panicking or showing an error. | Sync-core convention (D007) — `cycle_status` is a synchronous scan of the milestones directory. Loading on every frame would cause repeated I/O. The lifecycle transitions (wizard creates milestones, settings doesn't change milestone state) cover all relevant state changes. | Yes — if live refresh is needed (e.g. milestone advanced externally while TUI is open) |
| D107 | M007 | arch | Unified TUI event loop using mpsc channel combining terminal events and agent output | `run()` refactored from blocking `event::read()` to `mpsc::Receiver<TuiEvent>` loop; one background thread converts crossterm events to `TuiEvent::Key`/`Resize`; when an agent is running, a second background thread sends `TuiEvent::AgentLine(String)` and `TuiEvent::AgentDone { exit_code }` to the same receiver | The blocking `event::read()` loop cannot interleave terminal key events with streaming subprocess output. A channel-based approach is the standard Ratatui pattern for background events. Both event sources push to a single `Sender<TuiEvent>`, keeping the main loop a simple `while let Ok(event) = rx.recv()`. Consistent with D007 (sync core — no tokio runtime). | Yes — if the TUI needs periodic timer events (add TuiEvent::Tick) |
| D108 | M007 | arch | `launch_agent_streaming` as a new free function in assay-core::pipeline; existing `launch_agent` unchanged | New `launch_agent_streaming(cli_args, working_dir, line_tx: Sender<String>) -> JoinHandle<i32>` spawns child with `Stdio::piped()`, reads stdout line-by-line in the spawned thread, sends each line to `line_tx`, and returns the join handle. The existing batch `launch_agent()` is left untouched for CLI/MCP use. | Streaming and batch are two different use cases: TUI needs lines as they arrive; CLI/MCP needs the full output after completion. Adding a new function avoids breaking existing callers and keeps backward compatibility. Consistent with D001 (no trait abstraction — just two free functions). | No |
| D109 | M007 | arch | Provider dispatch via free function `provider_harness_writer(config)` in assay-tui, no trait | `provider_harness_writer(config: &Config) -> Box<HarnessWriter>` returns a boxed closure: Anthropic → existing `claude::write_config` + `claude::build_cli_args`; Ollama → new `ollama::build_cli_args`; OpenAI → new `openai::build_cli_args`. All three are plain functions. | Zero-trait convention (D001). `HarnessWriter` is already a function type alias (`dyn Fn(...)`), so wrapping different provider implementations as closures is the natural pattern. No enum dispatch, no trait objects beyond the existing `HarnessWriter` alias. | No |
| D110 | M007 | arch | MCP panel reads `.assay/mcp.json` as static config; no live async MCP client | The MCP server panel reads and writes a `.assay/mcp.json` config file. It does NOT connect to MCP servers, query their tools, or maintain a live client. "Connected" status is out of scope for M007 — the panel is a configuration management surface. | A live async MCP client requires a tokio runtime bridged into the sync TUI event loop — high complexity and risk. Static config management (read/write JSON) delivers the user-visible value (managing server entries) without the async complexity. Live tool inspection can be added in M008+ when the integration pattern is clearer. | Yes — extend to live connection status in M008 |
| D111 | M007 | convention | Slash command dispatch is synchronous and in-process | `execute_slash_cmd(cmd, project_root)` calls assay-core functions directly (cycle_status, gate evaluation, pr_check_milestone_gates, etc.) without spawning subprocesses. Results returned as a String for inline display. | Slash commands are quick operations on local state — no agent spawning, no network. Sync in-process dispatch is the simplest correct approach and consistent with D007. If a command needs async work (e.g. launching an agent), it should transition to `Screen::AgentRun` rather than running in the overlay. | No |
| D112 | M007/S01 | arch | `App.event_tx: Option<Sender<TuiEvent>>` field for agent thread back-channel | The `r` key handler in `handle_event()` needs to give agent threads a sender into the main TUI event loop. Rather than threading the sender through function parameters (which would change existing `handle_event` signature), `App` holds the sender set by `run()` before starting the main loop. The `r` handler reads `self.event_tx.clone().unwrap()` to pass to the agent wrapper thread. | `handle_event` is called from both `run()` (production) and integration tests (via `App::with_project_root`). Adding the sender as a field avoids changing the `handle_event(key: KeyEvent) -> bool` signature that all 27 existing tests depend on. The field is `None` during tests — the `r` key handler only spawns if `event_tx` is `Some`. | Yes — if a cleaner dependency-injection approach is desired |
