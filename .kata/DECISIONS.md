# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| #    | When     | Scope   | Decision                       | Choice                                          | Rationale                                                        | Revisable?                  |
| ---- | -------- | ------- | ------------------------------ | ----------------------------------------------- | ---------------------------------------------------------------- | --------------------------- |
| D001 | M001     | arch    | Smelt role                     | Pure infrastructure layer                        | Assay owns orchestration; Smelt provisions environments          | No                          |
| D002 | M001     | arch    | Assay integration boundary     | Shell out to `assay` CLI, no crate dependency    | Process boundary keeps repos independent                         | No                          |
| D003 | M001     | arch    | v0.1.0 code disposition        | Gut entirely, start fresh                        | Orchestration logic migrates to Assay; manifest schema is new    | No                          |
| D004 | M001     | arch    | Runtime abstraction            | Pluggable RuntimeProvider trait                  | Docker first, Compose/K8s later via same trait                   | No                          |
| D005 | M001     | library | Docker client                  | bollard crate                                    | De facto Rust Docker client, async/tokio native                  | Yes — if exec API unreliable |
| D010 | M001     | arch    | Manifest authorship            | Assay generates manifests, Smelt consumes        | Single source of truth for job specs lives in Assay              | No                          |
| D012 | M001     | scope   | Image building                 | Pre-built images only, no Dockerfile building    | Simplifies M001 scope; users provide images                      | Yes — if user demand        |
| D013 | M001     | arch    | Repo delivery to container     | Bind-mount host repo into container              | Avoids clone overhead; container reads/writes directly to mount  | Yes — if K8s needs volumes  |
| D014 | M001     | arch    | Credential injection           | Environment variable passthrough                 | Simplest secure path; vault integration deferred                 | Yes — if vault needed       |
| D015 | M001     | arch    | Git module reuse               | Keep git/cli.rs and git/mod.rs from v0.1.0       | Branch ops, commit, push are reusable for result collection      | No                          |
| D016 | M001     | scope   | Workspace structure            | Keep smelt-cli + smelt-core two-crate workspace  | Established pattern, no reason to change                         | No                          |
| D017 | M001-S01 | pattern | Manifest strict parsing        | deny_unknown_fields on all 6 manifest structs    | Catches schema mismatches at parse time instead of at runtime    | No                          |
| D018 | M001-S01 | pattern | Validation error aggregation   | Collect all errors before returning, not fail-fast | Users see every issue at once instead of fixing one at a time   | No                          |
| D019 | M001-S01 | library | Async trait style              | RPITIT instead of async_trait macro               | Rust 2024 edition supports this natively; avoids boxing overhead | Yes — if edition downgraded |
| D020 | M001-S01 | pattern | Config missing file behavior   | Return defaults when .smelt/config.toml missing   | Non-fatal — first run should work without config file            | No                          |
| D021 | M001-S02 | pattern | Container keep-alive strategy  | `sleep 3600` as container CMD, work via exec      | Container stays running while exec commands are issued against it | No                          |
| D022 | M001-S02 | pattern | Container labeling             | `smelt.job=<name>` label on all containers        | Enables identification and cleanup via `docker ps --filter`       | No                          |
| D023 | M001-S02 | pattern | Teardown guarantee             | Explicit teardown in both success and error paths  | No scopeguard/Drop — explicit match ensures cleanup visibility    | Yes — if signal handling in S05 changes pattern |
| D024 | M001-S02 | pattern | Docker test skip pattern       | Tests skip gracefully when daemon unavailable       | Keeps `cargo test --workspace` green in all environments          | No                          |
| D025 | M001-S02 | pattern | ExecHandle result fields       | exit_code/stdout/stderr on ExecHandle directly      | Simpler API — results returned to caller without indirection      | No                          |
| D026 | M001-S02 | pattern | CLI teardown via async block   | Async block for exec work, teardown unconditional   | Guarantees cleanup without Drop/scopeguard complexity             | Yes — if signal handling in S05 changes pattern |
| D027 | M001-S03 | pattern | Container workspace path       | Fixed `/workspace` mount point for host repo         | Simple convention; all exec commands use `working_dir: /workspace` | Yes — if multi-repo support needed |
| D028 | M001-S03 | pattern | Assay manifest delivery        | Base64-encode TOML, write via exec `base64 -d`       | Avoids heredoc quoting issues with TOML special chars             | Yes — if Docker put_archive proves simpler |
| D029 | M001-S03 | pattern | Assay manifest translation     | Smelt-side serde structs for Assay format, no import  | Keeps D002 (no crate dep); mock-based testing for now             | Yes — when real Assay integration in S06 |
| D030 | M001-S03 | scope   | Repo path validation           | Local paths only; URLs rejected with clear error      | Bind-mount requires local path; clone support deferred            | Yes — if clone-into-container added later |
| D031 | M001-S04 | pattern | ResultCollector generic design  | Generic `<G: GitOps>` not `dyn GitOps`                | RPITIT (D019) makes trait not object-safe; generics keep testability | No |
| D032 | M001-S04 | pattern | Host-side collection            | Collector reads host repo directly, not via Docker exec | Bind-mount (D013) means commits are already on host filesystem     | No |
| D033 | M001-S04 | pattern | Target branch force-update      | Delete + recreate target branch if it exists            | Simple and explicit; warns with old/new hashes for auditability    | Yes — if append-mode needed |
| D034 | M001-S05 | pattern | Monitor state file format        | TOML at `.smelt/run-state.toml`, single-job model       | TOML already in deps; no new dependency; sufficient for M001 single-job | Yes — if concurrent jobs needed |
| D035 | M001-S05 | pattern | Timeout source                   | Max session timeout from manifest, fallback to config default | Matches existing `AssayInvoker::build_run_command()` logic; no new manifest field | Yes — if explicit job-level timeout field added |
| D036 | M001-S05 | pattern | Signal handling via tokio::select! | `ctrl_c()` + `sleep(timeout)` racing against exec future | Standard tokio pattern; testable via cancellation abstraction | No |
| D037 | M001-S05 | pattern | Testable cancellation abstraction  | Generic future parameter (not CancellationToken); oneshot in tests, ctrl_c() in prod | Avoids tokio-util dependency; same testability with simpler API | Yes — if CancellationToken needed across task trees |
| D038 | M001-S05 | pattern | Idempotent teardown on remove      | DockerProvider::teardown() tolerates 404 on remove_container | stop_container already tolerated 404; remove_container was missing it; double-teardown must be safe | No |
| D039 | M001-S06 | pattern | E2E test phase-chaining strategy   | Manually chain phases in integration tests, not via run_with_cancellation() | run_with_cancellation() provisions internally — impossible to inject mock assay setup step between provision and exec; direct phase-chaining mirrors test_collect_creates_target_branch pattern | No |
| D040 | M001-S06 | pattern | Mock assay PATH placement          | Mock assay binary placed at /usr/local/bin/assay in container | AssayInvoker::build_run_command() uses bare "assay" — must be on PATH; /usr/local/bin is on alpine PATH without additional config | No |
| D041 | M001-S06 | pattern | Pre-clean orphan containers in tests | Sweep docker ps --filter label=smelt.job at test start for tests that assert container absence | Defensive against parallel test runs and prior-run orphans; tolerate empty output silently | No |
| D042 | M001-S06 | pattern | Orphan-check scoped to job-specific label value | Use `label=smelt.job=<job-name>` (key=value) not `label=smelt.job` (key-only) in failure-path orphan assertion | Key-only filter returns containers from concurrently-running tests, causing spurious failures; job-specific value isolates the assertion to the exact job under test | No |
| D043 | M002     | pattern | Assay manifest translation (supersedes D029)   | Option A: Smelt writes both spec files + RunManifest; `AssayInvoker` generates `.assay/specs/<name>.toml` per session + `RunManifest` with `[[sessions]]` referencing spec names | Real Assay schema has `deny_unknown_fields`; `[[session]]` key is wrong (must be `[[sessions]]`); `spec` is a name reference not inline text; `harness`/`timeout` are unknown fields; Option A keeps Smelt self-contained — users need no pre-existing `.assay/` directory | No |
| D044 | M002     | pattern | `.assay/` setup via direct writes, not `assay init` | Write `.assay/config.toml` + `specs/` dir manually via targeted exec commands; never call `assay init` | `assay init` returns `AlreadyInitialized` error on repos that already have `.assay/`; also writes unwanted side-effect files (`hello-world.toml`, `.gitignore`, `srs.md`) to the bind-mounted host repo; targeted writes are idempotent and non-destructive | No |
| D045 | M002     | pattern | `.assay/` idempotency guard strategy | Check for `/workspace/.assay/config.toml` existence before writing; `mkdir -p /workspace/.assay/specs` is always safe; spec files are always written/overwritten (Smelt is authoritative for its sessions) | Repos already using Assay must not have their config clobbered; spec files generated by Smelt are ephemeral per-run and safe to overwrite | No |
| D046 | M002     | arch    | Streaming exec variant on RuntimeProvider | Add `exec_streaming()` alongside `exec()`; streaming variant takes a callback/writer for real-time chunk delivery; buffered `exec()` retained for short setup commands | Real `assay run` runs multi-minute AI sessions; buffered exec would show nothing until completion; `exec()` is simpler for setup phases where full output is needed at once | Yes — if unified callback API preferred |
| D047 | M002-S02 | pattern | Linux assay binary build for integration tests | Build via `docker run rust:alpine` with `CARGO_TARGET_DIR` mounted as a separate volume (`target/smelt-test-cache/assay-build`); cache output binary at `target/smelt-test-cache/assay-linux-aarch64`; detect assay source via `ASSAY_SOURCE_DIR` env var then `../../assay` sibling; skip test if source or Docker unavailable | No Linux Rust targets installed on host (only `aarch64-apple-darwin`); separate CARGO_TARGET_DIR avoids contaminating macOS build artifacts in assay source; caching avoids ~5–15 min rebuild on every test run; graceful skip keeps CI green in environments without assay source | Yes — if cross-compile target added to host |
| D048 | M002-S02 | pattern | Large binary injection into containers via `docker cp` | Use subprocess `docker cp <host_path> <container_id>:<dest_path>` for injecting large files (50–100 MB debug binaries) into test containers | Base64 exec pattern (D028/D040) is unreliable for files >a few MB (exec stdin transfer is slow; Docker may truncate); bollard `upload_to_container` requires implementing tar stream in-memory; `docker cp` is already used elsewhere in tests and correctly handles large payloads | Yes — if bollard tar stream injection is preferred for reproducibility |
| D049 | M002-S03 | pattern | `exec_streaming()` callback bound: `FnMut(&str) + Send + 'static` | Require `'static` on the output_cb parameter; test accumulators use `Arc<Mutex<Vec<String>>>` to satisfy this bound | RPITIT futures that capture `output_cb: F` across `.await` points must be `Send`; `FnMut` closures capturing a plain `&mut Vec` are not `Send`; `Arc<Mutex<Vec<String>>>` is `Send + 'static` and can be cloned into the closure without lifetime parameters; `'static` eliminates lifetime annotation friction on the trait method | Yes — if a non-'static streaming API is needed (e.g. for stack-local callbacks) |
| D050 | M002-S04 | pattern | Exit-code-2 path: save assay_exit binding before branching | Save `let assay_exit = handle.exit_code;` before any conditional logic in Phase 7's exec_future; return `Ok(assay_exit)` at closure end (not hardcoded `Ok(0)`) | Enables both 0 and 2 to propagate correctly; the explicit `ExecOutcome::Completed(Ok(2))` arm in the outcome match must appear before the generic `Ok(code)` arm to set `JobPhase::GatesFailed` instead of `JobPhase::Complete` | No |
| D051 | M002-S04 | pattern | ResultCollector merge-commit compatibility verified by unit test | Add `test_collect_after_merge_commit` using `git merge --no-ff` to replicate Assay's post-session merge state; no code changes to `collector.rs` required | `collect()` reads HEAD and base_ref agnostically — merge commits are ordinary commits from git's perspective; the existing `rev_list_count`/`diff_name_only` calls handle merge parents correctly; test makes this invariant explicit and regression-safe | No |
| D052 | M003     | arch    | GitHub API integration library | `octocrab` crate (not `gh` CLI shell-out) | R005 (library API) requires programmatic GitHub access — a CLI-only approach can't be embedded by Assay or other callers; octocrab is tokio-native (already in the stack), async, and composable; removes runtime dependency on `gh` binary; gated behind `forge` feature flag so non-forge consumers have zero new dependencies | Yes — if gh CLI proves more robust for auth edge cases (device flow, OIDC tokens) |
| D053 | M003/S01 | pattern | octocrab::Error wrapping strategy | Stringify via `.to_string()` initially; upgrade to `forge_with_source()` with stored source if `octocrab::Error: Send + Sync + 'static` confirmed at implementation time | Cannot confirm octocrab error bounds at design time; stringify is always safe; T02 must check and record the actual finding | Yes — confirmed at T02 implementation |
| D054 | M003/S01 | pattern | review_count field source | `pr.review_comments` (inline diff comments) not `list_reviews()` (submitted approvals) | No extra API call; sufficient for S01 display; if approval count is needed in S03, switch to `pulls.list_reviews(number)` — decision is revisable when S03 is planned | Yes — if approval count required in S03 |
| D055 | M003/S01 | pattern | ForgeConfig and forge types visibility | `pub mod forge` is unconditional; `ForgeConfig`, `ForgeClient`, `PrHandle`, `PrState`, `CiStatus`, `PrStatus` are always exported; only `GitHubForge` and octocrab-dependent code are gated behind `#[cfg(feature = "forge")]` | S02 needs `ForgeConfig` in `manifest.rs` for TOML parsing without enabling the forge feature (which would pull in octocrab as a build-time dep of smelt-cli unconditionally) | No |
| D056 | M003/S01/T02 | pattern | octocrab::Error Send+Sync confirmation (resolves D053) | octocrab::Error IS Send+Sync+'static — compile-time assertion fn _assert_octocrab_error_send_sync() where octocrab::Error: Send+Sync+'static {} compiled without error in Rust 1.93.1 with octocrab 0.49.5; forge_with_source() could be upgraded to carry a #[source] field analogous to Provider; kept as stringify for S01 MVP since error chain inspection is not a current requirement | snafu-generated error types with Backtrace fields satisfy Send+Sync in std-backtrace mode; upgrade path is clear when error chain traversal is needed | Yes — if error source chaining needed by S03 or callers |
| D057 | M003/S02 | pattern | Phase 9 guard extracted as `should_create_pr()` free function | `pub(crate) fn should_create_pr(no_pr: bool, no_changes: bool, forge: Option<&ForgeConfig>) -> bool` extracted from Phase 9 logic so all 8 guard combinations can be unit-tested without Docker | The guard has three independent boolean inputs; inline logic in an async block is not unit-testable; free function enables `#[test]` coverage of the exact conditions that gate PR creation | No |
| D058 | M003/S02 | pattern | smelt-cli always enables forge feature | `smelt-core = { path = "../smelt-core", features = ["forge"] }` in smelt-cli/Cargo.toml with no `#[cfg(feature = "forge")]` guards in CLI code | smelt-cli is a binary (not a library); library consumers use smelt-core directly; the no-octocrab feature isolation is for library consumers only; adding `#[cfg]` guards in run.rs would be unnecessary noise | No |
| D059 | M003/S03 | pattern | forge context stored in RunState for watch/status | `forge_repo: Option<String>` and `forge_token_env: Option<String>` written to RunState at Phase 9 completion; `smelt watch` reads these fields to reconstruct `GitHubForge` without needing the original manifest | Original manifest may not be present when `smelt watch` is called (e.g. in CI after `smelt run`); storing the non-secret forge context (env var *name* and repo slug, never the token value) in RunState makes watch self-contained | Yes — if manifest is always available at watch time |
| D060 | M003/S03 | pattern | smelt watch inner function accepts generic ForgeClient for testability | `run_watch<F: ForgeClient>(state_dir, forge, pr_number, repo, interval) -> Result<i32>` extracted from `execute()`; `MockForge` with pre-programmed `VecDeque<PrStatus>` responses injected in tests | Same pattern as should_create_pr() guard extraction (D057); async trait impl + execute() side-effects (env var reads, client construction) are not unit-testable inline; generic parameter avoids dyn dispatch overhead at runtime | No |
| D061 | M003/S03 | pattern | smelt watch transient API errors are non-fatal | On `poll_pr_status()` error, print warning and sleep interval rather than aborting; only terminal PR states (Merged/Closed) or explicit user cancellation terminate the loop | Transient network errors, rate-limit hiccups, or momentary GitHub outages should not abort a long-running watch session; the watched PR state is inherently recoverable; aborting on every API error would make watch unreliable in real CI | Yes — if repeated errors should trigger abort after N retries |
| D054R | M003/S03 | pattern | review_count field source — kept as inline diff comments (D054 confirmed) | D054 remains in effect: `pr.review_comments` (inline diff comments, no extra API call); approval count not needed for S03 display | S03 display requirements (state, CI, review count) are satisfied by the existing `poll_pr_status()` return value; no approval-count use case emerged in S03 planning | Yes — if approval count required by S05 API or users |
| D062 | M003/S03 | pattern | `toml` promoted to regular dep in smelt-cli | `toml.workspace = true` moved from `dev-dependencies` to `dependencies` in `smelt-cli/Cargo.toml` so `persist_run_state()` can serialize `RunState` to TOML in non-test code | `JobMonitor.state_dir` is private; constructing a `JobMonitor` purely for write-back would require API changes to smelt-core; a standalone `persist_run_state()` helper is simpler and avoids leaking watch state management into the monitor abstraction | No |
| D063 | M003/S03 | pattern | `PrState` and `CiStatus` gained `Serialize`/`Deserialize` derives | Added `#[derive(Serialize, Deserialize)]` + `#[serde(rename_all = "snake_case")]` to both enums in `forge.rs` | Storing `pr_status`/`ci_status` in `RunState` TOML required these derives; `snake_case` convention matches the existing TOML field naming in run-state.toml; decision is backward-compat since these fields are new and `#[serde(default)]` | No |
| D064 | M003/S04 | pattern | `smelt status` backward compat via optional positional `job_name` arg | `StatusArgs` gains `#[arg] pub job_name: Option<String>`; when `Some`, reads `.smelt/runs/<name>/state.toml` via `JobMonitor::read()`; when `None`, reads `.smelt/run-state.toml` via `JobMonitor::read_legacy()`; help text documents the distinction | After S04 migration, `smelt run` writes to the per-job path; users who call `smelt status` without args would see "No running job" for new-style runs; backward-compat fallback makes the transition non-breaking; requiring the arg would break existing workflows | Yes — if `smelt status` should always require a job name in a future breaking release |
| D065 | M003/S04 | pattern | `smelt init` skeleton written as raw string literal | The manifest skeleton in `init.rs` is a `const &str` with embedded `#`-comments; struct serialization via `toml::to_string_pretty()` is not used | `toml::to_string_pretty()` strips TOML comments; skeleton comments are essential for new-user onboarding; raw string literal is the only way to preserve them; skeleton must still pass `JobManifest::validate()` — enforced by unit test | No |
| D066 | M003/S04 | pattern | `.assay/` gitignore guard placed after Phase 3, before Phase 5 in `run.rs` | `ensure_gitignore_assay()` is called in `run_with_cancellation()` after the runtime type check (Phase 3) and before container provision (Phase 5); it operates on the host filesystem only | Guard is host-side I/O with no container dependency; placing it before Phase 4 (Docker connect) means a gitignore failure aborts early before any container resources are allocated; guard is idempotent so a retry after failure is safe | Yes — if guard should be deferred to after successful Assay execution |
| D067 | M003/S05 | pattern | Assay internal translation types demoted to pub(crate) | `SmeltRunManifest`, `SmeltManifestSession`, `SmeltSpec`, `SmeltCriterion` in `assay.rs` changed to `pub(crate)` | These are Assay's internal serde translation format — not embedding targets; smelt-cli calls `AssayInvoker` methods (which return `String`) and never references these types by name; `AssayInvoker` and all its `pub fn` methods remain `pub`; demotion removes misleading types from the library API surface | No |
| D068 | M003/S05 | pattern | GitWorktreeEntry kept pub with doc comments (not demoted) | `GitWorktreeEntry` remains `pub`; its 5 fields gain `///` doc comments | `GitWorktreeEntry` is in the return type of `GitOps::worktree_list()` — a public trait method; demoting to `pub(crate)` would create a private-type-in-public-interface and could break external callers that consume worktree list results; documenting the fields is simpler and preserves the API | Yes — if worktree API is refactored to hide GitWorktreeEntry behind an iterator or opaque type |
| D069 | M003/S05 | pattern | smelt-example lives at /tmp/smelt-example/ outside workspace | Standalone Cargo project at `/tmp/smelt-example/`; not added to workspace `Cargo.toml`; path dep uses absolute path to smelt-core | Strongest proof of external embedding: matches exactly how Assay or another consumer would depend on smelt-core; workspace membership would make it part of the build graph and remove the "external" proof; absolute path avoids relative path ambiguity when running `cargo test` from /tmp | No |
| D070 | M003/S06 | convention | Doc link backtick-only for pub(crate) types | Use `` `TypeName` `` (backtick-only) instead of `` [`TypeName`] `` for types that are `pub(crate)` or otherwise inaccessible from public API docs | `rustdoc` emits `rustdoc::unresolved_doc_links` warning for `[`Type`]` syntax when the type cannot be linked (e.g., feature-gated types like `GitHubForge` when `forge` feature is absent, or `pub(crate)` types like `SmeltManifestSession`); backtick-only renders as code without triggering link resolution | No |
| D071 | M003/S06 | pattern | GitCli::run() delegates to run_in() for DRY git execution | `run()` body replaced with `self.run_in(&self.repo_root, args).await`; all callers that need repo-root git operations use `run()`; callers that need a different working directory call `run_in()` directly | `run()` previously duplicated the full `Command::new("git")` / output handling block from `run_in()`; any future change to error handling, timeout, or env vars would need updating in two places; delegation is the canonical pattern for this type of specialisation | No |
| D072 | M004 | arch    | ComposeProvider generates compose file at runtime | Smelt generates `docker-compose.yml` in a `tempfile::TempDir` from manifest `[[services]]` entries; no user-provided compose file is accepted or augmented | Assay generates the manifest including service definitions; Smelt is the file generator, not a consumer of pre-written compose files; temp dir avoids polluting the host repo with ephemeral compose files | No |
| D073 | M004 | pattern | `[[services]]` uses full passthrough via `IndexMap<String, toml::Value>` | Each `[[services]]` entry has required `name` and `image` fields extracted explicitly; all other fields stored as `IndexMap<String, toml::Value>` and serialized directly to YAML via `serde_yaml` | Full compose-service passthrough (user decision); avoids maintaining a copy of the Compose service schema; `IndexMap` preserves field order for deterministic YAML output; only `name` and `image` are validated | Yes — if type-safe compose service schema is needed later |
| D074 | M004 | arch    | Credentials injected into smelt-agent service only | `[credentials.env]` env vars forwarded to smelt-agent service in the generated compose file; service containers receive only their own `[[services]] environment` block | Mirrors D014 (forge tokens never enter container); service containers should not have access to AI provider API keys or other host secrets | No |
| D075 | M004 | pattern | `runtime = "compose"` dispatches to `ComposeProvider` in run.rs | `match manifest.environment.runtime.as_str() { "docker" => DockerProvider, "compose" => ComposeProvider, _ => validation error }` — dispatch at the same point as existing `DockerProvider` construction | Consistent with D004 (RuntimeProvider trait as the extension point); no CLI flag needed — runtime choice lives in the manifest, which Assay controls | No |
| D076 | M004 | library | `serde_yaml` added to smelt-core production deps for compose file generation | `serde_yaml` is the standard Rust YAML serializer; handles TOML-to-YAML type mapping correctly (arrays, integers, booleans); no alternative approach (hand-built YAML strings are brittle; `docker compose convert` is fragile) | Production dep, not dev-only — `generate_compose_file()` is called in the normal `smelt run` path, not just in tests | No |
| D077 | M004/S01 | pattern | `runtime` value validation added to `validate()`, not only Phase 3 of `run.rs` | `validate()` rejects `runtime` values other than `"docker"` and `"compose"` with a collected error; Phase 3's explicit docker-only check remains until S04 dispatch is updated | Per D018 (collect-all-errors): catching unknown runtimes at validation time gives the user the full error list upfront rather than a mid-execution abort; Phase 3 check is safe to leave unchanged for S01 since the dry-run path bypasses it | Yes — Phase 3 check superseded by match dispatch in S04 |
| D078 | M004/S02 | pattern | `environment:` key on smelt-agent omitted when `extra_env` is empty | `generate_compose_file()` only emits the `environment:` block on the `smelt-agent` service when `extra_env` is non-empty; empty map → no key at all | Follows the same logic as `depends_on:` (omitted when no services); empty YAML sections add noise with no functional value; Docker Compose treats absent `environment:` and `environment: {}` identically | No |
| D079 | M004/S03 | pattern | `serde_json` promoted from optional to unconditional dep in smelt-core | Change `serde_json = { version = "1", optional = true }` to `serde_json = "1"` in `[dependencies]`; remove `"dep:serde_json"` from `forge` feature; remove the redundant `[dev-dependencies]` entry | `docker compose ps --format json` NDJSON parsing requires `serde_json` in production code (not just tests); making it unconditional is simpler than a new feature flag; `serde_json` is already a transitive dep of most Rust projects so compile-time impact is minimal | No |
| D080 | M004/S03 | pattern | `tempfile` promoted from dev-dep to production dep in smelt-core | Move `tempfile.workspace = true` from `[dev-dependencies]` to `[dependencies]` | `ComposeProjectState._temp_dir: TempDir` lives in production code — the TempDir must outlive the provision→teardown lifecycle; can't use a dev-only dep for a production struct field | No |
| D081 | M004/S03 | pattern | Healthcheck timeout: 60 polls × 2s = 120s, no manifest field | Constant `MAX_HEALTHCHECK_POLLS: u32 = 60` and `POLL_INTERVAL: Duration = 2s` in compose.rs; `unhealthy` state surfaces as an immediate error (not waiting for timeout) | Research found no manifest timeout field for compose; 120s matches the implicit assumption in DockerProvider; `unhealthy` is distinct from `starting` — fail fast on unhealthy, wait on starting | Yes — if manifest-level compose timeout is added |
| D082 | M004/S03/T02 | arch | smelt-agent uses Docker Compose default project network — no explicit `networks:` key | Removed custom `networks: [smelt-<project>]` from agent service and top-level `networks:` section in `generate_compose_file`; rely on Docker Compose auto-created default project network | Custom named network isolated smelt-agent from user services (postgres, etc.) — user services without explicit network config go on the default network; the agent was on a different network and DNS name resolution failed. Default network gives all services shared DNS automatically. | No |
| D083 | M004/S03/T02 | pattern | smelt-agent compose service always has `command: [sleep, "3600"]` | Added `command: [sleep, "3600"]` to agent_map in `generate_compose_file` before `volumes:` | `alpine:3` exits immediately without a command; `docker compose ps --format json` only shows running containers — exited containers disappear from output within seconds, making agent container ID capture impossible. Consistent with `DockerProvider` which also uses `sleep 3600`. | No |
| D084 | M004/S04 | pattern | `enum AnyProvider { Docker(DockerProvider), Compose(ComposeProvider) }` in `run.rs` for provider dispatch | Local enum in `run.rs` with a `RuntimeProvider` impl delegating all 5 methods via match arms; `Box::pin` fallback for RPITIT return type unification when compiler rejects `async fn` in enum impl | D019 (RPITIT) makes `RuntimeProvider` non-object-safe — `Box<dyn RuntimeProvider>` is structurally impossible; enum delegation is idiomatic Rust for this pattern; local to `run.rs` (binary crate) so no `missing_docs` lint applies; all existing call sites unchanged — `provider.provision/exec/teardown` calls go through the same method names | No |
| D085 | M005 | arch | K8s repo delivery via init container git clone into emptyDir | Alpine/git init container clones the repo via SSH into `/workspace` emptyDir shared with the main agent container; replaces bind-mount (D013) for the K8s path | Bind-mount (D013) is impossible for Pods scheduled on remote nodes; init container + emptyDir is the standard K8s pattern for repo delivery; pure K8s, no host dependency, works on any node | No |
| D086 | M005 | arch | K8s result collection via push-from-Pod + host-side git fetch | Agent container pushes result branch from inside the Pod (`SMELT_GIT_REMOTE` env var); `run.rs` Phase 8 detects `runtime == "kubernetes"` and runs `git fetch origin <branch>` on the host before calling ResultCollector | ResultCollector reads local git refs (D032); for K8s there is no bind-mount so local refs don't exist after exec; push-from-Pod + fetch keeps ResultCollector unchanged and the PR creation path identical | No |
| D087 | M005 | pattern | SSH Secret injected at provision time; deleted at teardown | Smelt creates `smelt-ssh-<job-name>` K8s Secret from env var at provision; mounts at `/root/.ssh/id_rsa` with `defaultMode: 256` (0o400); deleted by `teardown()` idempotently (404 = already gone) | SSH requires 0o400 file mode — K8s Secrets default to 0o444 which SSH rejects; Smelt owns the Secret lifecycle so credential cleanup is guaranteed even on error paths (D023) | No |
| D088 | M005 | arch | `kube` crate (ws feature) for K8s exec; no kubectl shell-out | Pod exec via `AttachedProcess` WebSocket from the `kube` crate (`ws` feature flag); `k8s-openapi` for typed resource definitions | kubectl shell-out creates a runtime dependency on the kubectl binary; `kube` crate gives programmatic access matching the library API requirement (R005 pattern); WebSocket exec is the standard K8s attach model | No |
| D089 | M005 | pattern | `SMELT_K8S_TEST=1` env var gates K8s integration tests | Integration tests in `k8s_lifecycle.rs` are annotated `#[ignore]` and skipped unless `SMELT_K8S_TEST=1` is set; requires a kind cluster to be running | Mirrors `DOCKER_AVAILABLE` skip pattern from docker_lifecycle.rs (D024); keeps `cargo test --workspace` green in environments without a K8s cluster; CI can set `SMELT_K8S_TEST=1` when a kind cluster is available | No |
| D090 | M005/S01 | pattern | `generate_pod_spec()` `ssh_private_key` param reserved but unused in S01 | The `ssh_private_key: &str` parameter is accepted by `generate_pod_spec()` but suppressed with `let _ = ssh_private_key;` in S01 — only the Secret volume mount name is included in the Pod spec; actual Secret creation (placing the key bytes into K8s) is S02's responsibility | S01 is contract-only — `generate_pod_spec` returns the Pod spec; the Secret that will hold the key is created separately by `KubernetesProvider::provision()` in S02; forward-declaring the param in S01 locks the function signature for S02 without introducing unused-field dead code | Yes — if Secret creation is merged into generate_pod_spec in S02 |
| D091 | M005/S01 | pattern | Pod spec snapshot tests use `serde_json::to_string_pretty()` for deterministic output | `k8s_openapi` structs do not implement `Display`; snapshot assertions use `serde_json::to_string_pretty(&pod)` and check for key substring presence rather than exact string equality | `serde_json` is already an unconditional dep (D079); JSON output is deterministic for a given struct state; substring assertions are more resilient to field ordering changes in `k8s_openapi` updates than exact-match snapshots | Yes — if insta or similar snapshot library is adopted |
| D092 | M005/S01 | pattern | `test_validate_runtime_unknown_rejected` updated to use `"podman"` | The existing test used `"kubernetes"` as the unknown runtime value; after adding `"kubernetes"` to VALID_RUNTIMES the test would pass for the wrong reason — updated to `"podman"` which remains an invalid value | D018 (collect-all-errors) requires the allowlist test to always test a genuinely unknown value; `"podman"` is the clearest near-miss alternative that will not be added in M005 | No |
| D093 | M005/S02 | pattern | `exec()` and `exec_streaming()` use sequential stdout→stderr reads, not `tokio::join!` | `AttachedProcess` stdout/stderr are read sequentially (stdout loop completes, then stderr loop) rather than concurrently via `tokio::join!` | `exec_streaming()` takes `FnMut(&str) + Send + 'static` callback (D049); sharing a `FnMut` across two concurrent `tokio::join!` branches requires `&mut` aliasing which is unsound; sequential reads are safe, sufficient for K8s exec streams, and avoid adding a `tokio::sync::Mutex` wrapper around the callback | Yes — if truly concurrent stdout+stderr streaming is needed (requires Arc<Mutex<F>> callback wrapper) |
| D094 | M005/S02 | pattern | `teardown()` derives `secret_name` from ContainerId, not from PodState lookup | `teardown()` parses `<ns>/<pod-name>` from `ContainerId`; derives `secret_name = format!("smelt-ssh-{}", pod_name.trim_start_matches("smelt-"))` from the pod name rather than requiring a PodState HashMap lookup | PodState lookup is cleaner but adds `Mutex` contention and fails silently if provision() was never called (e.g. double-teardown); formula derivation matches `provision()`'s formula and makes teardown self-contained; double-teardown is safe because both delete calls tolerate 404 (D023) | Yes — if PodState should be used for richer cleanup metadata |
| D095 | M005/S03 | pattern | `git fetch origin +<branch>:<branch>` (force-refspec) populates local branch directly | Phase 8 kubernetes fetch uses `+<target>:<target>` refspec (not `<target>`); the `+` prefix force-updates the local ref even if it already exists; `<branch>:<branch>` (remote:local) creates the local branch directly without needing a separate `git checkout -b` | `git fetch origin <branch>` only creates `origin/<branch>` (remote-tracking ref); `ResultCollector::collect()` checks `branch_exists(target_branch)` on the local ref, not the remote-tracking ref; without `+` a second run fails with "rejected" if the local branch exists; force-update is safe because the K8s push is authoritative for that branch | Yes — if branch protection on the local ref is needed |
| D096 | M005/S03 | pattern | `SMELT_TEST_GIT_REMOTE` env var gates the push-from-Pod integration test | `test_k8s_push_from_pod_result_collection` requires both `SMELT_K8S_TEST=1` (cluster) and `SMELT_TEST_GIT_REMOTE=<ssh-url>` (reachable remote); test skips (eprintln + return) when either is absent | kind Pods cannot reach host filesystem paths — a bare repo at `/tmp/foo.git` is not accessible from inside a Pod container; only network-accessible SSH remotes work; mirrors the `SMELT_TEST_SSH_KEY` pattern from S02; graceful skip keeps `cargo test --workspace` green everywhere | No |
| D097 | M005/S03 | pattern | Host-side collection uses `git clone <remote> <tempdir>` not `git init` + `git remote add` | The push-from-Pod integration test clones the remote repo into a tempdir to get an origin-configured working copy; this is the starting point for `fetch_ref` + `ResultCollector::collect()` | `git init` + `git remote add origin <url>` leaves no upstream tracking — `fetch_ref` still works but there is no base_ref to compare against without a prior HEAD; cloning gives a real HEAD that predates the test push, making `base_ref = HEAD` of clone a valid base for ResultCollector; realistic setup matches what `smelt run` does in production | Yes — if a bare-remote helper is preferred over a full clone for test isolation |
| D098 | M006 | arch | `smelt serve` job execution model: in-process tokio tasks, not subprocess | Each dispatched job runs as a `tokio::spawn` task calling `run_with_cancellation()` within the same process, not as a spawned `smelt run` subprocess | In-process tasks share the tokio runtime and allow direct `Arc<Mutex<ServerState>>` updates without IPC; subprocess would require stdin/stdout protocol and piping; D037 cancellation abstraction was designed for this use case; subprocess is a fallback if in-process teardown proves unreliable | Yes — if per-job crash isolation requires subprocess boundaries |
| D099 | M006 | arch | `CancellationToken` (tokio-util) for broadcast cancellation (supersedes D037 for multi-job context) | `tokio_util::CancellationToken` cloned per job; parent token cancelled on Ctrl+C broadcasts to all child tokens; replaces the generic-future pattern from D037 for the multi-job case | D037 was explicitly marked "revisable if CancellationToken needed across task trees"; multi-job dispatch is exactly that case; CancellationToken provides structured cancellation with `child_token()` and `cancel()` semantics; the single-job `smelt run` path continues using the existing generic-future pattern unchanged | No — CancellationToken is the correct choice for tree-structured cancellation |
| D100 | M006 | pattern | Queue pickup via file-move: manifest → `queue_dir/dispatched/<ts>-<name>.toml` | DirectoryWatcher moves a discovered `.toml` from `queue_dir/` to `queue_dir/dispatched/` atomically before enqueuing; manifests in `queue_dir/` (not yet moved) are re-picked up on restart; in-flight jobs are NOT recovered on restart | Atomic rename gives "exactly-once pickup" semantics without a database; restart recovery of queued-but-not-dispatched manifests is free (file is still in `queue_dir/`); in-flight recovery would require persisting job state outside RunState — deferred to R028 | Yes — if persistent queue (R028) is implemented later |
| D101 | M006 | library | `axum` added for HTTP API in `smelt-cli`; `ratatui` + `crossterm` added for TUI | `axum` for the REST API (tokio-native, ergonomic routing, tower middleware); `ratatui` + `crossterm` for the terminal dashboard (industry standard, `std::thread::spawn` backend pattern) | `axum` is the de facto tokio HTTP framework; `ratatui` is actively maintained and used by Symphony for the same use case; both are production-quality Rust crates with no surprising transitive deps; `warp` and `tui` alternatives considered and rejected (warp is less ergonomic; tui is the old name for ratatui) | Yes — if a lighter HTTP solution is preferred later |
| D102 | M006 | pattern | `ServerConfig` is a separate TOML file (`server.toml`), not embedded in job manifests | `smelt serve --config server.toml` reads daemon-wide config (queue_dir, max_concurrent, retry, port, host) from a separate file; job manifests remain unchanged | Daemon config and job config are different concerns with different lifetimes; embedding daemon config in manifests would couple them and break the existing `smelt run` path; separate file matches `smelt-config.toml` precedent (D020) and allows the same manifest to be used with both `smelt run` and `smelt serve` without modification | No |
| D103 | M006/S02 | pattern | `JobId` uses atomic `u64` counter (not UUID) for deterministic test IDs | `JobId` wraps a `String` formatted as `job-<N>` from a global `AtomicU64`; predictable ordering makes queue tests deterministic without UUID v4 randomness | UUID v4 would make test assertions non-deterministic (comparing opaque IDs); atomic counter produces `job-1`, `job-2`, etc. which are easy to assert in FIFO tests; a future production deployment could switch to UUID if uniqueness across process restarts matters | Yes — if cross-process uniqueness is needed |
| D104 | M006/S02 | pattern | `ServerState::complete()` sets Retrying in-place (not re-enqueue) | When a job fails and is retry-eligible, `complete()` sets `status = Retrying` on the existing VecDeque entry; `try_dispatch()` picks up both `Queued` and `Retrying` jobs | Re-enqueuing would create duplicate entries (one Retrying, one Queued) and complicate cancel/status-lookup semantics; in-place mutation keeps a single source of truth per job; `retry_eligible()` is a simple check on `status == Retrying && attempt < max_attempts` | No |
| D105 | M006/S02 | pattern | HTTP POST handler persists TOML body via `std::mem::forget(TempPath)` | POST `/api/v1/jobs` writes the TOML body to a `NamedTempFile`, converts to `TempPath`, then calls `std::mem::forget()` so the file survives for `dispatch_loop` to read later | `TempPath` destructor deletes the file; `dispatch_loop` reads `manifest_path` asynchronously after the HTTP handler returns; `forget` is the simplest way to decouple file lifetime from handler scope; leaked temp files are acceptable for a daemon process; a production server would need periodic cleanup | Yes — if temp file cleanup mechanism is added |
| D106 | M006/S03 | pattern | TUI shutdown coordination via `Arc<AtomicBool>` (not MPSC channel or tokio oneshot) | `shutdown: Arc<AtomicBool>` shared between tokio runtime and TUI background thread; TUI sets true on `q`/Ctrl+C; main sets true after tokio::select! exits; tokio arm polls shutdown every 100ms to detect TUI-initiated shutdown | MPSC channel requires blocking recv in the TUI thread (fine) but adds Send bounds complexity for the tokio polling arm; tokio oneshot is not Clone so can't be shared with the thread after sending; AtomicBool is the simplest shared signal between a std::thread and a tokio runtime — no Send/Clone issues, no blocking | Yes — if a more structured shutdown protocol is needed |
| D107 | M006/S03 | pattern | Tracing subscriber branched in `main()` before command dispatch: file appender for TUI mode, stderr for all others | `tracing_subscriber` is initialized inside a `match &cli.command` before calling `execute()` — the TUI branch uses `tracing_appender::rolling::never(".smelt", "serve.log")` as writer; all other branches (including `--no-tui`) use the existing stderr writer | `tracing_subscriber::fmt().init()` can only be called once per process; branching before dispatch ensures the correct writer is chosen before any component starts; the subscriber must be chosen before the TUI enters raw mode so no tracing output reaches stderr after raw mode is enabled | No — single-init constraint makes this the only safe approach |
| D108 | M007 | arch | Queue persistence uses a TOML file in queue_dir, not Redis/SQLite/external service | `queue_dir/.smelt-queue-state.toml` is written atomically on every state transition; loaded on startup via `ServerState::load_or_new()`; no new service dependency | Filesystem-only approach matches the existing queue_dir convention (D100); operator can make it durable by pointing queue_dir at a Docker named volume or K8s PV; adding Redis/SQLite would require a new service dependency and increase operational complexity for zero benefit at this scale | Yes — if true ACID transactions or cross-node queue sharing is needed |
| D109 | M007 | pattern | In-flight jobs at crash time are re-queued on restart (not marked Failed) | `ServerState::load_or_new()` transitions any job with status Dispatching or Running back to Queued; attempt count is preserved | Re-queueing is idempotent and safe (worst case: a job runs twice, which is already the case with retry logic); marking as Failed would silently drop work and require operator intervention to re-submit; since `smelt run` is designed to be re-runnable (idempotent container provisioning), re-queueing is the correct recovery action | Yes — if idempotency of smelt run cannot be guaranteed in all runtimes |
| D110 | M007 | pattern | `Instant` fields in `QueuedJob` replaced with `u64` Unix epoch seconds for serializability | `queued_at: u64` and `started_at: Option<u64>` replace `Instant` fields; `elapsed_secs()` computes `SystemTime::now().duration_since(UNIX_EPOCH).as_secs() - queued_at`; HTTP API and TUI updated accordingly | `std::time::Instant` is not serializable and has no meaningful cross-process value; `SystemTime`/`u64` round-trips through TOML without loss; u64 epoch seconds is compact, portable, and human-readable in the state file | No |
| D113 | M007/S01 | pattern | `JobId` serde representation | `#[serde(transparent)]` wrapping | Serializes as a plain string in TOML/JSON round-trips; without `transparent`, `JobId` would serialize as `{ "0": "job-1" }` (newtype wrapper object) which is not TOML-compatible as a map key or scalar value | No |
| D114 | M007/S02 | pattern | `QueueState` wrapper struct in `queue.rs` (not a new file) | `#[derive(Serialize, Deserialize)] struct QueueState { jobs: Vec<QueuedJob> }` added to `queue.rs` | TOML requires a top-level table; bare `Vec<QueuedJob>` cannot be serialized as TOML root; wrapper struct maps to `[[jobs]]` array-of-tables naturally; no new file needed — `queue.rs` is the right home for queue-local types | No |
| D115 | M007/S02 | pattern | `ServerState::new()` left unchanged; `new_with_persistence(max_concurrent, queue_dir)` added alongside | `new()` keeps `queue_dir: None`; `new_with_persistence()` sets `queue_dir: Some(queue_dir)` | Preserves all 46 existing tests without modification; S03's `load_or_new()` will use `new_with_persistence` internally; adding `Option<PathBuf>` to `new()` signature would require updating every test callsite | No |
| D116 | M007/S02 | pattern | `try_dispatch` → Dispatching transition does NOT write state file | Only `enqueue`, `complete`, and `cancel` call `write_queue_state`; `try_dispatch` is omitted | Dispatching is a transient state (job transitions to Running or fails within milliseconds); omitting the write keeps the critical dispatch path tight; critical durable transitions (job enters queue, job completes/retries, job cancelled) are always persisted | Yes — if Dispatching state recovery is needed in future |
| D111 | M008 | arch | SSH dispatch uses subprocess `ssh`/`scp` rather than a Rust SSH library crate | Manifest delivery via `scp` subprocess; remote execution via `ssh` subprocess; no `openssh` or `ssh2` crate dependency | Consistent with D002 (shell-out pattern) and the existing `git` CLI approach; avoids native library linking (libssh2); subprocess `ssh` respects the user's `~/.ssh/config`, known_hosts, and ssh-agent automatically; `openssh` crate is tokio-native but adds a new async dependency; subprocess is simpler and sufficient for sequential manifest-deliver + exec + scp-back | Yes — if connection multiplexing or non-interactive auth is needed |
| D112 | M008 | pattern | Worker `[[workers]]` entries use `key_env` field (name of env var holding SSH key path), not the key value directly | `key_env: String` in `WorkerConfig` — the env var names the path to the SSH private key file; dispatcher passes `-i $KEY_PATH` to `ssh`/`scp` subprocesses | Following D014 (credential injection via env var passthrough) and existing token_env pattern in ForgeConfig; private keys never appear in config files; operator sets e.g. `key_env = "WORKER_SSH_KEY"` and `export WORKER_SSH_KEY=~/.ssh/worker_rsa` in their environment | No |
| D120 | M007/S03 | pattern | `ServerState::load_or_new(queue_dir, max_concurrent)` — single entry point for startup | Takes `PathBuf` (not `Option<PathBuf>`); always creates `queue_dir: Some(...)` so post-startup mutations continue writing; remaps Dispatching/Running → Queued in place before constructing the VecDeque; delegates to `new_with_persistence` for the base state | A single constructor that always enables persistence on the serve startup path is simpler than a conditional that might produce `queue_dir: None`; `new()` and `new_with_persistence()` remain available for tests that don't need recovery | No |
| D121 | M008/S01 | pattern | `SshClient` trait uses generic `<C: SshClient>` at callsites, not `dyn SshClient` | Async `fn exec/probe` in trait returns `impl Future` via RPITIT — not object-safe; callsites use generic parameter (D060 pattern); `SubprocessSshClient` is a unit struct, cheaply cloned or passed by reference | `dyn SshClient` is structurally impossible with async methods (RPITIT makes the trait non-object-safe); `Box<Pin<dyn Future>>` wrapper would require rewriting trait signatures and adding `Send` bounds; generic parameter is idiomatic and consistent with D060 (`run_watch<F: ForgeClient>`); decision recorded at planning time to prevent S02 from accidentally using `dyn` | No |
