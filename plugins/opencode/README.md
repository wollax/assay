# Assay — OpenCode Plugin

Spec-driven workflows with gated quality checks for OpenCode.

## Installation

Register the plugin in your OpenCode configuration:

```json
{
  "plugins": ["@assay/opencode-plugin"]
}
```
