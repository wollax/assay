{
  "$id": "https://assay.dev/schemas/formatted-evidence.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "FormattedEvidence",
  "description": "The result of formatting gate evidence for PR consumption.",
  "type": "object",
  "properties": {
    "full_report": {
      "description": "Full untruncated markdown report for disk persistence.",
      "type": "string"
    },
    "pr_body": {
      "description": "Markdown formatted for the PR body, truncated to fit the character limit.",
      "type": "string"
    },
    "truncated": {
      "description": "Whether the PR body was truncated to fit the character limit.",
      "type": "boolean"
    }
  },
  "additionalProperties": false,
  "required": [
    "pr_body",
    "full_report",
    "truncated"
  ]
}
