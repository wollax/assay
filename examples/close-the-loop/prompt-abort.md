# Misbehaving Prompt (abort path)

You are working on a file analysis task. For each file you find, use a
separate tool call to read it, then use another tool call to write your
analysis. Be thorough — read multiple files and analyze each one
individually.

Start by listing the directory, then read at least two files, then
summarize your findings.
