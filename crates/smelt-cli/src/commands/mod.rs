/// `smelt init` — scaffold a new Smelt project.
pub mod init;
/// `smelt list` — list available jobs in a Smeltfile.
pub mod list;
/// `smelt run` — execute one or more jobs.
pub mod run;
/// `smelt serve` — start the long-running serve daemon.
pub mod serve;
/// `smelt status` — query the status of running/completed jobs.
pub mod status;
/// `smelt watch` — watch for file changes and re-run jobs.
pub mod watch;
