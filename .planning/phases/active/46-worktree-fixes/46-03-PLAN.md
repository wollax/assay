---
phase: 46-worktree-fixes
plan: 03
type: execute
wave: 3
depends_on:
  - 46-02
files_modified:
  - crates/assay-core/src/worktree.rs
  - crates/assay-cli/src/commands/worktree.rs
  - crates/assay-mcp/src/server.rs
autonomous: true
source_issue: "WFIX-03"

must_haves:
  truths:
    - "list() returns WorktreeListResult { entries, warnings } instead of Vec<WorktreeInfo>"
    - "WorktreeListResult is defined in assay-core, NOT assay-types"
    - "Prune failures are captured as warnings, not silently discarded"
    - "CLI callsites destructure and ignore warnings (no CLI warning display needed)"
    - "MCP callsite includes warnings in response via WorktreeListResponse"
    - "warnings field is skip_serializing_if empty in MCP response"
  artifacts:
    - "crates/assay-core/src/worktree.rs with WorktreeListResult struct and updated list()"
    - "crates/assay-cli/src/commands/worktree.rs with destructured list() calls"
    - "crates/assay-mcp/src/server.rs with WorktreeListResponse and warnings propagation"
  key_links:
    - ".planning/phases/active/46-worktree-fixes/46-CONTEXT.md"
    - ".planning/phases/active/46-worktree-fixes/46-RESEARCH.md"
---

<objective>
Change `list()` to return a `WorktreeListResult` wrapper struct that surfaces prune failures as warnings instead of silently discarding them. Update all three callsites (2 CLI, 1 MCP).

Purpose: `git worktree prune` failures are currently swallowed with `let _ = ...`. Surfacing them as warnings in MCP responses (via the Phase 35 `warnings` field pattern) gives agents visibility into infrastructure issues without blocking the list operation.

Output: `WorktreeListResult` struct, updated `list()`, updated CLI destructuring, MCP response with optional warnings field.
</objective>

<execution_context>
<!-- Executor agent has built-in instructions for plan execution and summary creation -->
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/active/46-worktree-fixes/46-CONTEXT.md
@.planning/phases/active/46-worktree-fixes/46-RESEARCH.md

@crates/assay-core/src/worktree.rs
@crates/assay-cli/src/commands/worktree.rs
@crates/assay-mcp/src/server.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add WorktreeListResult and update list()</name>
  <files>crates/assay-core/src/worktree.rs</files>
  <action>
1. Define `WorktreeListResult` struct near the top of the module (after imports, before functions). It must be `pub`:

```rust
/// Result of listing worktrees, including any non-fatal warnings.
pub struct WorktreeListResult {
    /// The worktree entries found.
    pub entries: Vec<WorktreeInfo>,
    /// Non-fatal warnings (e.g., prune failures).
    pub warnings: Vec<String>,
}
```

Do NOT add Serialize/Deserialize/JsonSchema — this is an internal type. Do NOT put it in assay-types.

2. Change `list()` return type from `Result<Vec<WorktreeInfo>>` to `Result<WorktreeListResult>`.

3. Replace `let _ = git_command(&["worktree", "prune"], project_root);` with:
```rust
let mut warnings = Vec::new();
if let Err(e) = git_command(&["worktree", "prune"], project_root) {
    warnings.push(format!("git worktree prune failed: {e}"));
}
```

4. Change the return from `Ok(entries)` (or similar) to `Ok(WorktreeListResult { entries, warnings })`.

5. Make sure `WorktreeListResult` is exported from the module (add to any `pub use` if needed).

6. Update ALL calls to `list()` in the integration test module — search for every `list(&root)` call and append `.entries`. The `test_create_list_status_cleanup` test calls `list(&root)` twice (both usages need `.entries` appended). Scan the entire test module to ensure no `list()` calls are missed.
  </action>
  <verify>
`cargo test --package assay-core -- worktree` — all tests pass.
`cargo check --package assay-core` — no compilation errors.
  </verify>
  <done>
`list()` returns `WorktreeListResult`. Prune failures are captured as warnings. All in-crate tests updated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CLI and MCP callsites</name>
  <files>crates/assay-cli/src/commands/worktree.rs, crates/assay-mcp/src/server.rs</files>
  <action>
**CLI (2 callsites in `crates/assay-cli/src/commands/worktree.rs`):**

1. `handle_worktree_list` (around line 173) — Change `let entries = list(&root)?;` to `let result = list(&root)?; let entries = result.entries;`. Warnings are ignored for now (CLI list doesn't display warnings). **Important:** The JSON serialization path (`serde_json::to_string_pretty(&entries)` around line 176) must continue to serialize `entries` (the `Vec<WorktreeInfo>`), NOT the `result` struct. The CLI JSON output must remain a bare array of `WorktreeInfo` objects.

2. `handle_worktree_cleanup_all` (around line 366) — Change `let entries = list(root)?;` to `let result = list(root)?; let entries = result.entries;`. Warnings are ignored.

Update the import to include `WorktreeListResult` if needed (or it may already be covered by the existing `worktree::list` import).

**MCP (1 callsite in `crates/assay-mcp/src/server.rs`):**

1. Find the `worktree_list` tool handler (around line 2019). Change `let entries = worktree::list(&project_root)?;` to `let result = worktree::list(&project_root)?;`.

2. Define a local `WorktreeListResponse` struct in server.rs (near other response structs):
```rust
#[derive(Serialize)]
struct WorktreeListResponse {
    entries: Vec<WorktreeInfo>,
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    warnings: Vec<String>,
}
```

3. Serialize `WorktreeListResponse { entries: result.entries, warnings: result.warnings }` instead of serializing entries directly.

Verify the import paths are correct — `WorktreeInfo` should already be imported from assay-types.
  </action>
  <verify>
```bash
cargo check --workspace
cargo test --workspace
```
All callsites compile. All tests pass. MCP response includes `warnings` field when non-empty, omits it when empty.
  </verify>
  <done>
All 3 callsites updated. CLI destructures and ignores warnings. MCP propagates warnings to response with `skip_serializing_if` for clean output.
  </done>
</task>

</tasks>

<verification>
```bash
just ready
```
Full check suite passes: formatting, linting, all tests, cargo-deny.
</verification>

<success_criteria>
1. `list()` returns `WorktreeListResult { entries, warnings }` — not `Vec<WorktreeInfo>`
2. `WorktreeListResult` is in `assay-core`, not `assay-types` — no Serialize/Deserialize derives
3. Prune failures produce a warning string like `"git worktree prune failed: ..."` instead of being silently discarded
4. CLI callsites destructure and use `entries` only — no behavioral change for CLI users
5. MCP response includes `warnings` field (empty array omitted via `skip_serializing_if`)
6. `just ready` passes
</success_criteria>

<output>
After completion, create `.planning/phases/active/46-worktree-fixes/46-03-SUMMARY.md`
</output>
