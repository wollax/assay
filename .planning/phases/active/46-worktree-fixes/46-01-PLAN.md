---
phase: 46-worktree-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/assay-core/src/worktree.rs
autonomous: true
source_issue: "WFIX-01"

must_haves:
  truths:
    - "resolve_worktree_dir() canonicalizes its return value via std::fs::canonicalize"
    - "Non-existent paths canonicalize the parent and append the leaf segment"
    - "Existing unit tests continue to pass (they use fake paths, hitting the fallback)"
    - "New integration test proves symlink/relative path resolution with real TempDir"
  artifacts:
    - "crates/assay-core/src/worktree.rs with canonicalize_best_effort logic at end of resolve_worktree_dir()"
  key_links:
    - ".planning/phases/active/46-worktree-fixes/46-CONTEXT.md"
    - ".planning/phases/active/46-worktree-fixes/46-RESEARCH.md"
---

<objective>
Add best-effort path canonicalization to `resolve_worktree_dir()` so that worktree base directory paths resolve symlinks and `..` segments, matching the canonical paths returned by `git worktree list --porcelain`.

Purpose: Fixes path mismatch between `resolve_worktree_dir()` output and git's canonical worktree paths — the root cause of `cleanup --all` failing on symlinked or relative paths (e.g., macOS `/tmp` -> `/private/tmp`).

Output: Modified `resolve_worktree_dir()` with canonicalization, plus integration test proving it works with real filesystem paths.
</objective>

<execution_context>
<!-- Executor agent has built-in instructions for plan execution and summary creation -->
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/active/46-worktree-fixes/46-CONTEXT.md
@.planning/phases/active/46-worktree-fixes/46-RESEARCH.md

@crates/assay-core/src/worktree.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add canonicalization to resolve_worktree_dir()</name>
  <files>crates/assay-core/src/worktree.rs</files>
  <action>
At the end of `resolve_worktree_dir()`, after the `resolved` PathBuf is computed (the if/else for absolute vs relative), add best-effort canonicalization before returning:

```rust
// Canonicalize to resolve symlinks and `..` segments.
if resolved.exists() {
    std::fs::canonicalize(&resolved).unwrap_or(resolved)
} else if let (Some(parent), Some(leaf)) = (resolved.parent(), resolved.file_name()) {
    std::fs::canonicalize(parent)
        .map(|p| p.join(leaf))
        .unwrap_or(resolved)
} else {
    resolved
}
```

This replaces the current bare `resolved` return. The logic:
1. Full path exists -> canonicalize it directly
2. Parent exists but leaf doesn't (worktree dir not yet created) -> canonicalize parent, append leaf
3. Neither exists -> return as-is (fallback for fake/test paths)

Do NOT change the function signature. All 4 callsites are transparent.
  </action>
  <verify>
Run existing unit tests: `cargo test --package assay-core -- resolve_worktree_dir`
All 5 existing tests must pass. They use fake paths (`/home/user/myproject`), so canonicalize falls through to the fallback path — no assertion changes needed.
  </verify>
  <done>
`resolve_worktree_dir()` returns canonicalized paths when the filesystem supports it, falling back to the raw path otherwise. Existing unit tests pass unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add integration test for canonicalization</name>
  <files>crates/assay-core/src/worktree.rs</files>
  <action>
Add two integration tests to the `integration_tests` module in `crates/assay-core/src/worktree.rs`:

1. `test_resolve_worktree_dir_canonicalizes_dotdot_segments` — Create a real TempDir, set up a Config with a relative `../{name}-worktrees` path, call `resolve_worktree_dir()`, and assert the result has no `..` segments (compare against manually canonicalized expected path). Use `#[serial]` since env vars may be involved.

2. `test_resolve_worktree_dir_canonicalizes_symlinks` — Create a real TempDir, create a symlink inside it, use that symlink as the worktree base dir via config or env var, call `resolve_worktree_dir()`, and assert the result points to the real path (not the symlink). Use `#[serial]`.

Both tests must canonicalize the TempDir path itself before comparison (macOS `/var/folders` -> `/private/var/folders`).
  </action>
  <verify>
Run all worktree tests: `cargo test --package assay-core -- worktree`
All tests (existing + new) must pass.
  </verify>
  <done>
Two new integration tests prove that `resolve_worktree_dir()` resolves `..` segments and symlinks to canonical paths using real filesystem operations.
  </done>
</task>

</tasks>

<verification>
```bash
just test
just lint
```
All existing tests pass. New integration tests pass. No clippy warnings.
</verification>

<success_criteria>
1. `resolve_worktree_dir()` returns canonical paths when filesystem exists — symlinks resolved, `..` segments collapsed
2. Non-existent paths handled gracefully: parent canonicalized + leaf appended, or fallback to raw path
3. All 5 existing unit tests pass without modification
4. 2 new integration tests verify canonicalization with real TempDir
5. No signature change — all 4 callsites unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/active/46-worktree-fixes/46-01-SUMMARY.md`
</output>
