---
phase: 32-cli-polish
plan: 04
type: execute
wave: 2
depends_on: ["32-02"]
files_modified:
  - crates/assay-cli/src/commands/gate.rs
autonomous: true
must_haves:
  truths:
    - Enforcement check logic exists in exactly one place (not duplicated between handle_gate_run_all and handle_gate_run)
    - StreamConfig construction exists in exactly one place (shared builder or helper)
    - StreamCounters initialization exists in exactly one place (Default::default() or shared constructor)
    - gate_blocked() method (from Plan 02) is used for exit code determination
  artifacts:
    - Shared enforcement check block (function or inline via gate_blocked())
    - Deduplicated StreamConfig construction
    - Deduplicated StreamCounters initialization
    - Collapsed color branch in gate.rs history table status column
  key_links:
    - Uses StreamCounters::gate_blocked() added in Plan 02
    - Uses StreamCounters::tally() added in Plan 02
    - handle_gate_run and handle_gate_run_all both delegate to shared helpers
---

<objective>
Deduplicate the enforcement check block and StreamConfig/StreamCounters construction between handle_gate_run_all and handle_gate_run.

Purpose: The same enforcement check logic and struct construction code appears in both handlers. Extracting to shared helpers prevents drift and reduces maintenance burden.
Output: Single-source enforcement check and config/counter construction used by both gate run handlers.
</objective>

<execution_context>
<!-- Executor agent has built-in instructions -->
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/pending/32-cli-polish/32-RESEARCH.md
@.planning/phases/pending/32-cli-polish/32-CONTEXT.md

@crates/assay-cli/src/commands/gate.rs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Deduplicate StreamConfig construction (CLI-03 partial)</name>
  <files>crates/assay-cli/src/commands/gate.rs</files>
  <action>
1. Identify the StreamConfig construction at both sites (~lines 369-374 and ~502-507). They should be identical or near-identical, taking color, verbose, timeout, and config_timeout.

2. Add a constructor or factory function:
```rust
impl StreamConfig {
    fn new(color: bool, verbose: bool, timeout: Option<u64>, config_timeout: Option<u64>) -> Self {
        Self { color, verbose, timeout, config_timeout }
    }
}
```

3. Replace both construction sites with `StreamConfig::new(...)`.

4. For StreamCounters initialization, ensure both sites use `StreamCounters::default()` (the Default derive from Plan 02). If Plan 02 didn't add Default, add it here.
  </action>
  <verify>
```bash
just test && just lint
```
  </verify>
  <done>
- StreamConfig construction exists in one place (constructor)
- StreamCounters initialization uses Default::default() at both sites
- No functional change to behavior
  </done>
</task>

<task type="auto">
  <name>Task 2: Deduplicate enforcement check block (CLI-03)</name>
  <files>crates/assay-cli/src/commands/gate.rs</files>
  <action>
1. Identify the duplicated enforcement check block at ~line 436 (handle_gate_run_all) and ~line 539 (handle_gate_run). This block checks whether any required criteria failed and determines the exit code.

2. Both sites should now use `counters.gate_blocked()` (from Plan 02). If there is additional logic beyond the simple failed > 0 check (e.g., printing a summary message, computing exit code), extract it to a shared helper:

```rust
/// Determine the exit code after gate execution.
/// Returns 1 if any required criteria failed, 0 otherwise.
fn gate_exit_code(counters: &StreamCounters) -> i32 {
    if counters.gate_blocked() { 1 } else { 0 }
}
```

3. If the enforcement block includes printing (e.g., a "gate blocked" message), include that in the shared function or extract a `print_enforcement_result()` helper that both handlers call.

4. Replace the duplicated blocks in both handlers with calls to the shared helper(s).

5. Verify both handlers produce identical behavior to before the refactor.
  </action>
  <verify>
```bash
just test && just lint
```
  </verify>
  <done>
- Enforcement check logic exists in exactly one place
- Both handle_gate_run and handle_gate_run_all use the shared helper
- Exit code behavior is identical to before
- All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 3: Collapse gate.rs history color branch (CLI-04 partial)</name>
  <files>crates/assay-cli/src/commands/gate.rs</files>
  <action>
1. Find the color branch in the history table output (around lines ~687-688) where the status column width differs between color and plain output.

2. Apply the same pattern used in spec.rs: compute the status width once based on the `color` flag, then use a single format string:
```rust
let status_w = if color { base_status_w + ANSI_COLOR_OVERHEAD } else { base_status_w };
```

3. Replace the two-branch if/else with a single format string using the computed width.
  </action>
  <verify>
```bash
just test && just lint
```
  </verify>
  <done>
- gate.rs history table has one format path for status column (no color branching)
- Output is visually identical to before
  </done>
</task>
</tasks>

<verification>
```bash
just ready
```
Full check suite passes: fmt-check, lint, test, deny.
</verification>

<success_criteria>
- StreamConfig construction deduplicated via constructor
- StreamCounters initialization deduplicated via Default
- Enforcement check block exists in one place
- Both gate run handlers delegate to shared helpers
- `just ready` passes
</success_criteria>

<output>
After completion, create `.planning/phases/32-cli-polish/32-04-SUMMARY.md`
</output>
