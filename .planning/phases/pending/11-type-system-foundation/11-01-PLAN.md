---
phase: 11-type-system-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/assay-types/src/lib.rs
  - crates/assay-types/src/gate_run.rs
  - crates/assay-core/src/gate/mod.rs
  - crates/assay-mcp/src/server.rs
autonomous: true
must_haves:
  truths:
    - GateRunSummary and CriterionResult are defined in assay-types, not assay-core
    - No re-exports of GateRunSummary or CriterionResult from assay-core
    - All Option fields have skip_serializing_if + default paired together
    - All Vec fields on Review and Workflow have skip_serializing_if + default
    - Uncommitted files (feature_spec.rs, gates_spec.rs, modified files) are integrated
  artifacts:
    - crates/assay-types/src/gate_run.rs with GateRunSummary + CriterionResult (Serialize, Deserialize, JsonSchema derives)
  key_links:
    - assay-core::gate::evaluate_all returns assay_types::GateRunSummary (not a local type)
    - assay-mcp::server imports GateRunSummary/CriterionResult from assay_types
    - CriterionResult.result references assay_types::GateResult (no circular deps)
---

<objective>
Relocate GateRunSummary and CriterionResult from assay-core to assay-types and enforce serde hygiene on all domain types.

Purpose: This is the highest-churn refactor in v0.2.0 — every downstream phase depends on these types being in assay-types with proper Deserialize + JsonSchema derives. Serde hygiene prevents JSON bloat and ensures backward compatibility.

Output: gate_run.rs module in assay-types with relocated types; serde hygiene on Review and Workflow; all uncommitted type work integrated.
</objective>

<execution_context>
<!-- Executor agent has built-in instructions for plan execution and summary creation -->
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/pending/11-type-system-foundation/11-RESEARCH.md
@.planning/phases/pending/11-type-system-foundation/11-CONTEXT.md
@crates/assay-types/src/lib.rs
@crates/assay-types/src/gate.rs
@crates/assay-core/src/gate/mod.rs
@crates/assay-mcp/src/server.rs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create gate_run.rs and relocate types</name>
  <files>crates/assay-types/src/gate_run.rs, crates/assay-types/src/lib.rs, crates/assay-core/src/gate/mod.rs, crates/assay-mcp/src/server.rs</files>
  <action>
  1. Create `crates/assay-types/src/gate_run.rs` with `GateRunSummary` and `CriterionResult`:
     - Copy struct definitions from `crates/assay-core/src/gate/mod.rs` (lines 46-69)
     - Add derives: `Debug, Clone, Serialize, Deserialize, JsonSchema` (adding Deserialize + JsonSchema)
     - Add serde attrs: `CriterionResult.result` gets `#[serde(default, skip_serializing_if = "Option::is_none")]`
     - Add serde attrs: `GateRunSummary.results` gets `#[serde(default, skip_serializing_if = "Vec::is_empty")]`
     - Do NOT add `#[serde(deny_unknown_fields)]` — these are output types, not user-authored configs
     - Add `inventory::submit!` entries for both types (names: "gate-run-summary", "criterion-result")
     - Import `crate::GateResult` for the Option<GateResult> field
  2. In `crates/assay-types/src/lib.rs`:
     - Add `pub mod gate_run;`
     - Add `pub use gate_run::{CriterionResult, GateRunSummary};`
  3. In `crates/assay-core/src/gate/mod.rs`:
     - Remove the `GateRunSummary` struct definition (lines 42-60) and `CriterionResult` struct definition (lines 62-69)
     - Remove the `use serde::Serialize;` import (no longer needed after type removal — check if anything else uses it)
     - Add `use assay_types::{CriterionResult, GateRunSummary};` to imports
  4. In `crates/assay-mcp/src/server.rs`:
     - Find test imports referencing `assay_core::gate::{CriterionResult, GateRunSummary}` and change to `assay_types::{CriterionResult, GateRunSummary}`
  5. Verify no other files import GateRunSummary or CriterionResult from assay-core (grep to confirm)
  </action>
  <verify>
  - `cargo check --workspace` compiles without errors
  - `grep -r "assay_core::gate::GateRunSummary\|assay_core::gate::CriterionResult" crates/` returns no results
  - `grep -r "use assay_types.*GateRunSummary" crates/` shows imports in assay-core and assay-mcp
  </verify>
  <done>GateRunSummary and CriterionResult are defined in assay-types/src/gate_run.rs with Serialize+Deserialize+JsonSchema derives. All consumers import from assay_types. No re-exports from assay-core. Workspace compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Serde hygiene on Review and Workflow</name>
  <files>crates/assay-types/src/lib.rs</files>
  <action>
  1. In `crates/assay-types/src/lib.rs`, update the `Review` struct:
     - Add `#[serde(default, skip_serializing_if = "Vec::is_empty")]` to `comments: Vec<String>`
  2. Update the `Workflow` struct:
     - Add `#[serde(default, skip_serializing_if = "Vec::is_empty")]` to `specs: Vec<Spec>`
     - Add `#[serde(default, skip_serializing_if = "Vec::is_empty")]` to `gates: Vec<Gate>`
  3. Verify that all uncommitted changes (feature_spec.rs, gates_spec.rs, and modifications to error.rs, gate/mod.rs, init.rs, spec/mod.rs, server.rs, lib.rs) are coherent and consistent with these hygiene patterns. The uncommitted files already follow serde conventions — confirm and leave them as-is.
  </action>
  <verify>
  - `cargo check --workspace` compiles
  - Quick serde test: serialize a Review with empty comments and confirm "comments" key is absent from JSON output
  - Quick serde test: serialize a Workflow with empty specs/gates and confirm those keys are absent from JSON output
  - Deserializing JSON without those fields succeeds (backward compat via #[serde(default)])
  </verify>
  <done>Review.comments and Workflow.{specs,gates} have skip_serializing_if + default. Serializing with empty values omits the keys. Deserializing without those keys succeeds.</done>
</task>
</tasks>

<verification>
- `cargo check --workspace` passes
- No references to `assay_core::gate::GateRunSummary` or `assay_core::gate::CriterionResult` exist anywhere
- `GateRunSummary` and `CriterionResult` import from `assay_types` in all consuming crates
- `Review` with empty comments serializes without "comments" key
- `Workflow` with empty specs/gates serializes without those keys
</verification>

<success_criteria>
1. GateRunSummary and CriterionResult are defined in assay-types with Deserialize + JsonSchema derives
2. All consumers import from assay_types (no assay-core re-exports)
3. Optional/Vec fields on Review and Workflow use skip_serializing_if + default
4. `cargo check --workspace` passes
</success_criteria>

<output>
After completion, create `.planning/phases/11-type-system-foundation/11-01-SUMMARY.md`
</output>
