---
phase: 45-tech-debt-cleanup
plan: 07
type: execute
wave: 2
depends_on: []
files_modified:
  - crates/assay-cli/src/commands/gate.rs
  - crates/assay-cli/src/commands/mod.rs
  - crates/assay-cli/src/commands/spec.rs
  - crates/assay-cli/src/commands/worktree.rs
autonomous: true
source_issue: "DEBT-01"
must_haves:
  truths:
    - format_relative_time deduplication resolved (shared inner function)
    - format_criteria returns &'static str
    - format_spec_not_found uses simplified vec construction
    - column_gap value has explanatory comment
    - StreamConfig has doc comment
    - StreamCounters.failed doc is clarified
    - deterministic_results variable renamed for clarity
    - Worktree CLI fixes applied (list flag, cleanup path, status unwrap)
    - Test improvements for format functions, TOML errors, timeouts, and spec-not-found
    - just ready passes
  artifacts:
    - crates/assay-cli/src/commands/gate.rs (format dedup, criteria type, variable rename)
    - crates/assay-cli/src/commands/mod.rs (column_gap, StreamConfig doc, StreamCounters doc)
    - crates/assay-cli/src/commands/spec.rs (format_spec_not_found fixes)
    - crates/assay-cli/src/commands/worktree.rs (CLI fixes)
    - .planning/issues/closed/ (resolved issues moved here)
  key_links:
    - CLI changes are cosmetic — no impact on assay-core or assay-mcp
    - Worktree CLI fixes are independent of worktree type changes in Plan 02
---

<objective>
Apply cosmetic and correctness fixes to the assay-cli commands module. Doc comments, deduplication, naming, type fixes, and test improvements.

Purpose: Polish the CLI layer before v0.4.0 ships. All changes are within assay-cli — no cross-crate dependencies.

Output: ~18 issues resolved in assay-cli.
</objective>

<execution_context>
<!-- Executor agent has built-in instructions for plan execution and summary creation -->
</execution_context>

<context>
@.planning/phases/pending/45-tech-debt-cleanup/45-CONTEXT.md
@.planning/phases/pending/45-tech-debt-cleanup/45-RESEARCH.md

@crates/assay-cli/src/commands/gate.rs
@crates/assay-cli/src/commands/mod.rs
@crates/assay-cli/src/commands/spec.rs
@crates/assay-cli/src/commands/worktree.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Code fixes and doc improvements</name>
  <files>
    crates/assay-cli/src/commands/gate.rs
    crates/assay-cli/src/commands/mod.rs
    crates/assay-cli/src/commands/spec.rs
    crates/assay-cli/src/commands/worktree.rs
  </files>
  <action>
**1a. `format_relative_time` deduplication** (issue: `format-relative-time-dedup`):

Find duplicated time formatting logic and extract a shared inner function. There may be two copies (e.g., for gate history and session listing).

**1b. `format_criteria` return type** (issue: `format-criteria-type-static-str`):

Change return type from `String` to `&'static str` if the function returns string literals.

**1c. `format_spec_not_found` simplification** (issue: `format-spec-not-found-vec-simplify`):

Simplify the Vec construction in the spec-not-found formatting function.

**1d. `column_gap` invisible value** (issue: `column-gap-invisible-value`):

Add an explanatory comment next to the `column_gap` value explaining what it controls.

**1e. `StreamConfig` doc** (issue: `stream-config-new-missing-doc`):

Add doc comment to `StreamConfig::new` or the `StreamConfig` struct.

**1f. `StreamCounters.failed` doc** (issue: `stream-counters-failed-doc-unclear`):

Clarify the doc comment on the `failed` field to explain what it counts.

**1g. `deterministic_results` rename** (issue: `deterministic-results-variable-name`):

Rename the variable to something clearer. Read the code to understand what it represents and choose an accurate name.

**1h. Worktree CLI fixes:**
- `worktree-list-ignores-worktree-dir-flag`: Remove the ignored flag or document why it exists
- `worktree-cleanup-all-path`: Use canonical git path for cleanup --all
- `worktree-status-unwrap-or-false`: Replace `unwrap_or(false)` with proper error handling that distinguishes error types

**1i. Move resolved issues to `.planning/issues/closed/`.**
  </action>
  <verify>
`rtk cargo check` succeeds. `rtk cargo test -p assay-cli` passes.
  </verify>
  <done>
Code fixes and doc improvements applied across CLI commands.
  </done>
</task>

<task type="auto">
  <name>Task 2: Test improvements</name>
  <files>
    crates/assay-cli/src/commands/gate.rs
    crates/assay-cli/src/commands/mod.rs
    crates/assay-cli/src/commands/spec.rs
  </files>
  <action>
**2a. Add/strengthen tests:**

- `format-spec-not-found-boundary-test`: Add boundary test for spec-not-found with 0 specs, 1 spec, many specs
- `gate-history-params-missing-name-test`: Test gate history params when name is missing
- `test-unwrap-expect-messages`: Replace bare `.unwrap()` calls in tests with `.expect("descriptive message")` — scan all test functions in the commands module
- `format-command-error-test-weak-assert`: Strengthen the format_command_error test to assert on specific output content, not just that it doesn't panic
- `format-toml-error-multiline-weak-assert`: Strengthen the format_toml_error test for multiline errors — assert on specific content
- `timeout-test-assertion-escape-hatch`: Strengthen timeout test assertion to not have an escape hatch (remove any `|| true` or similar)
- `failure-reason-test-helper`: Extract a test helper function for constructing failure reason test fixtures
- `multiline-stdout-fallback-test`: Add test for multiline stdout fallback formatting

**2b. Run `just ready`.**

**2c. Move resolved issues to `.planning/issues/closed/`** — all issues mentioned above.
  </action>
  <verify>
`just ready` passes. All strengthened tests assert on specific content.
  </verify>
  <done>
Test coverage improved across CLI commands. ~18 total issues resolved.
  </done>
</task>

</tasks>

<verification>
```bash
just ready    # Full check suite: fmt-check + lint + test + deny
```

Verify:
- format_relative_time is deduplicated
- format_criteria returns &'static str
- StreamConfig and StreamCounters have clear docs
- Test assertions are specific (not just "doesn't panic")
</verification>

<success_criteria>
1. ~18 assay-cli issues resolved and moved to `.planning/issues/closed/`
2. No bare `.unwrap()` in test code (replaced with `.expect()`)
3. Format functions deduplicated and type-corrected
4. Worktree CLI edge cases fixed
5. `just ready` passes
</success_criteria>

<output>
After completion, create `.planning/phases/45-tech-debt-cleanup/45-07-SUMMARY.md`
</output>
