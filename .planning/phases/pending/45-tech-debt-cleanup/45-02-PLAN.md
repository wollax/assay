---
phase: 45-tech-debt-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/assay-types/src/work_session.rs
  - crates/assay-types/src/evaluator.rs
  - crates/assay-types/src/gate_run.rs
  - crates/assay-types/src/lib.rs
  - crates/assay-types/src/worktree.rs
autonomous: true
source_issue: "DEBT-01"
must_haves:
  truths:
    - CriterionOutcome derives Hash
    - DiffTruncation derives Hash
    - EvaluatorCriterionResult and EvaluatorSummary derive Hash
    - PhaseTransition derives Hash
    - RecoverySummary derives Clone and PartialEq
    - SessionsConfig derives Default
    - SessionPhase has #[non_exhaustive] attribute
    - GateEvaluateResponse.diff_truncation has #[serde(default)]
    - DiffTruncation byte fields use u64 instead of usize
    - stale_threshold field renamed to stale_threshold_secs with serde alias for backward compat
    - stale_threshold_secs=0 is validated (rejected) at config load time
    - All doc fixes applied (gate-runs-id-format, evaluator-criterion-result-name, sessions-config-doc-phase-ref)
    - WorktreeInfo and WorktreeStatus have #[serde(deny_unknown_fields)]
    - WorktreeInfo ahead/behind fields use u32 instead of usize
    - just ready passes with no regressions
  artifacts:
    - crates/assay-types/src/work_session.rs (derives, non_exhaustive, rename, validation)
    - crates/assay-types/src/evaluator.rs (derives, doc fixes)
    - crates/assay-types/src/gate_run.rs (DiffTruncation Hash + u64, doc)
    - crates/assay-types/src/worktree.rs (deny_unknown_fields, u32)
    - .planning/issues/closed/ (resolved issues moved here)
  key_links:
    - Plan 06 (session/recovery) depends on SessionPhase non_exhaustive being in place
    - Plan 08 (assay-mcp) depends on stale_threshold_secs rename being in place
    - DiffTruncation u64 change may affect assay-core gate code (extract_diff_files uses these)
---

<objective>
Apply type-level fixes across assay-types: missing derives, serde attributes, field type corrections, doc fixes, and the stale_threshold rename. These are purely additive/safe changes to the types crate.

Purpose: Clean up v0.4.0 type definitions so downstream plans (evaluator, sessions, MCP) build on a solid foundation. Lowest risk plan — mostly adding derives and attributes.

Output: ~15 issues resolved, all in assay-types.
</objective>

<execution_context>
<!-- Executor agent has built-in instructions for plan execution and summary creation -->
</execution_context>

<context>
@.planning/phases/pending/45-tech-debt-cleanup/45-CONTEXT.md
@.planning/phases/pending/45-tech-debt-cleanup/45-RESEARCH.md

@crates/assay-types/src/work_session.rs
@crates/assay-types/src/evaluator.rs
@crates/assay-types/src/gate_run.rs
@crates/assay-types/src/lib.rs
@crates/assay-types/src/worktree.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Missing derives and serde attributes</name>
  <files>
    crates/assay-types/src/work_session.rs
    crates/assay-types/src/evaluator.rs
    crates/assay-types/src/gate_run.rs
    crates/assay-types/src/worktree.rs
  </files>
  <action>
**1a. Add missing derives:**

- `CriterionOutcome` (evaluator.rs): add `Hash` to derive list
- `DiffTruncation` (gate_run.rs): add `Hash` to derive list
- `EvaluatorCriterionResult` (evaluator.rs): add `Hash` to derive list. NOTE: Only add Hash if all fields are Hash-compatible. If `reasoning: String` or other String fields exist, Hash on String is fine. If any float fields exist, skip Hash for that type and note in summary.
- `EvaluatorSummary` (evaluator.rs): add `Hash` to derive list (same caveat about floats)
- `PhaseTransition` (work_session.rs): add `Hash` to derive list. NOTE: If PhaseTransition contains `DateTime` or `Duration` fields, Hash may not be derivable — check the type and skip if needed, noting in summary.
- `RecoverySummary` (work_session.rs): add `Clone` and `PartialEq` to derive list
- `SessionsConfig` (work_session.rs): add `Default` to derive list (all fields must have sensible defaults — verify before adding)

**1b. Add `#[non_exhaustive]` to `SessionPhase`:**

```rust
#[non_exhaustive]
pub enum SessionPhase { ... }
```

After adding, run `rtk cargo build` to find all match sites in the workspace. Add `_ => unreachable!("unexpected SessionPhase variant")` or appropriate wildcard arms to any matches that the compiler flags. All match sites are within the workspace, so the compiler will catch them.

**1c. Add `#[serde(default)]` to `GateEvaluateResponse.diff_truncation`:**

Find the `diff_truncation` field on `GateEvaluateResponse` and ensure it has `#[serde(default)]` so that responses without this field deserialize correctly.

**1d. Add `#[serde(deny_unknown_fields)]` to worktree types:**

Add `#[serde(deny_unknown_fields)]` to `WorktreeInfo` and `WorktreeStatus` structs in `crates/assay-types/src/worktree.rs`.

**1e. After all changes, run `rtk cargo check` to verify compilation.**

Move resolved issues to `.planning/issues/closed/`:
- `criterion-outcome-missing-hash-derive`
- `diff-truncation-missing-hash-derive`
- `evaluator-result-types-missing-hash-derive`
- `phase-transition-hash-derive`
- `recovery-summary-missing-derives`
- `sessions-config-default-derive`
- `session-phase-non-exhaustive`
- `gate-evaluate-response-missing-serde-default`
- `worktree-missing-deny-unknown-fields` (if it exists in open issues — check via `ls`)
  </action>
  <verify>
`rtk cargo check` succeeds. `rtk cargo test -p assay-types` passes.
  </verify>
  <done>
All derive additions compile. SessionPhase is non_exhaustive with all match sites updated. Serde attributes added. Worktree types deny unknown fields.
  </done>
</task>

<task type="auto">
  <name>Task 2: Field type fixes, rename, validation, and doc fixes</name>
  <files>
    crates/assay-types/src/gate_run.rs
    crates/assay-types/src/work_session.rs
    crates/assay-types/src/evaluator.rs
    crates/assay-types/src/worktree.rs
  </files>
  <action>
**2a. DiffTruncation byte fields: `usize` to `u64`:**

In `DiffTruncation`, change `original_bytes: usize` and `truncated_bytes: usize` to `u64`. This is a breaking type change but pre-1.0 and correct (byte sizes should be platform-independent for serialization).

Search for all usages of these fields in the workspace (`rtk grep "original_bytes\|truncated_bytes"`) and fix any type mismatches (likely `as u64` casts at assignment sites in assay-core).

**2b. WorktreeInfo `ahead`/`behind`: `usize` to `u32`:**

Change `ahead: usize` and `behind: usize` to `u32` in `WorktreeInfo`. Search for usages and fix type mismatches.

**2c. Rename `stale_threshold` to `stale_threshold_secs`:**

In `SessionsConfig`:
```rust
#[serde(alias = "stale_threshold")]
pub stale_threshold_secs: u64,
```

The `alias` ensures backward compatibility — existing config files with `stale_threshold` still deserialize correctly.

Search for all references to `stale_threshold` in the workspace and update to `stale_threshold_secs` (code references, not config files).

**2d. Validate `stale_threshold_secs` is non-zero:**

Add a `validate()` method to `SessionsConfig` (or extend existing validation) that rejects `stale_threshold_secs == 0`. Find where `SessionsConfig` is loaded/used and add a validation call. If there's an existing validation pattern in the codebase, follow it.

**2e. Doc fixes:**

- `gate_runs` field on `WorkSession` or `GateRunRecord`: add doc comment documenting the ID format (e.g., `/// Gate run IDs in format "gate-run-<timestamp>-<hash>"` or whatever the actual format is — check the code that generates IDs)
- `EvaluatorCriterionResult.name`: fix doc to not overstate — update to accurately describe what the field contains
- `SessionsConfig`: fix doc reference to `agent_running` phase case

**2f. Run `just ready` to verify everything compiles and passes.**

Move resolved issues to `.planning/issues/closed/`:
- `diff-truncation-usize-vs-u64`
- `worktree-usize-serialization` (if it exists)
- `stale-threshold-secs-naming`
- `stale-threshold-accepts-zero`
- `gate-runs-id-format-doc`
- `evaluator-criterion-result-name-doc-overstates`
- `sessions-config-doc-phase-ref`
  </action>
  <verify>
`just ready` passes. `rtk grep "stale_threshold[^_]" crates/` returns only serde alias lines, not bare field references.
  </verify>
  <done>
Field types corrected (u64, u32). stale_threshold renamed with alias. Validation added. Doc fixes applied. All ~15 issues resolved and moved to closed.
  </done>
</task>

</tasks>

<verification>
```bash
just ready    # Full check suite: fmt-check + lint + test + deny
```

Verify:
- All new derives compile without errors
- SessionPhase is #[non_exhaustive] and all match sites have wildcard arms
- DiffTruncation uses u64 for byte fields
- WorktreeInfo uses u32 for ahead/behind
- stale_threshold_secs has serde alias for backward compat
- Schema snapshots regenerated if needed
</verification>

<success_criteria>
1. ~15 assay-types issues resolved and moved to `.planning/issues/closed/`
2. All derive additions, serde attributes, and field type changes compile cleanly
3. Backward compatibility maintained via serde aliases
4. `just ready` passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/45-tech-debt-cleanup/45-02-SUMMARY.md`
</output>
