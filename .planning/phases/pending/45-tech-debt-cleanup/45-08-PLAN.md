---
phase: 45-tech-debt-cleanup
plan: 08
type: execute
wave: 3
depends_on: [02, 05, 06]
files_modified:
  - crates/assay-mcp/src/server.rs
autonomous: true
source_issue: "DEBT-01"
must_haves:
  truths:
    - GateFinalizeResponse struct exists and is returned by gate_finalize handler
    - GateHistoryEntry has required_passed and advisory_passed count fields
    - gate_history logs instead of silently skipping malformed entries
    - gate_history does not load config unnecessarily
    - history_save_failure error is surfaced to caller
    - session timeout dead wd_string capture removed
    - session_response warnings field is populated (not always empty)
    - session_create doc examples are current
    - evaluate_criterion_result outcome/enforcement re-serialization fixed
    - spawn_blocking uses idiomatic shadowing for clones
    - zero timeout is validated (rejected) in MCP server
    - just ready passes
  artifacts:
    - crates/assay-mcp/src/server.rs (gate_finalize, gate_history, session handlers, evaluator handler)
    - crates/assay-types/src/ (GateFinalizeResponse if added to types crate)
    - .planning/issues/closed/ (resolved issues moved here)
  key_links:
    - Depends on Plan 02 for stale_threshold_secs rename
    - Depends on Plan 05 for EvaluatorError Io variant and map_evaluator_output Duration signature
    - Depends on Plan 06 for session path validation and RecoverySummary truncated field
    - GateFinalizeResponse is a new type — add to assay-types if cross-crate, or keep in assay-mcp if internal
---

<objective>
Fix gate and session MCP tool handlers in assay-mcp: create GateFinalizeResponse type, add history counts, fix error handling, and clean up session handlers.

Purpose: Harden the MCP server's gate and session tool handlers — the primary interface agents interact with. This plan handles code fixes; Plan 09 handles doc fixes and tests.

Output: ~15 code-change issues resolved in assay-mcp server.rs.
</objective>

<execution_context>
<!-- Executor agent has built-in instructions for plan execution and summary creation -->
</execution_context>

<context>
@.planning/phases/pending/45-tech-debt-cleanup/45-CONTEXT.md
@.planning/phases/pending/45-tech-debt-cleanup/45-RESEARCH.md

@crates/assay-mcp/src/server.rs
@crates/assay-types/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate tool handler fixes</name>
  <files>
    crates/assay-mcp/src/server.rs
    crates/assay-types/src/lib.rs
  </files>
  <action>
**1a. Create `GateFinalizeResponse`** (issue: `gate-finalize-untyped-response`, Important):

Define a typed response struct for `gate_finalize`. Check what the handler currently returns (likely a raw `serde_json::Value` or ad-hoc JSON). Create:
```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GateFinalizeResponse {
    pub session_id: String,
    pub final_phase: String,  // or SessionPhase
    pub warnings: Vec<String>,
    // ... other fields the handler currently returns
}
```

Place in assay-types if it's a public API type, or in assay-mcp if internal. Follow the pattern of other response types (e.g., `GateEvaluateResponse`).

**1b. Add passed counts to `GateHistoryEntry`** (issue: `gate-history-entry-missing-passed-counts`, Important):

Add `required_passed: u32` and `advisory_passed: u32` to `GateHistoryEntry`. Populate from the `GateRunRecord` when building history entries.

**1c. Fix `gate_history` silent entry skip** (issue: `gate-history-silent-entry-skip`):

Replace silent `continue` when encountering malformed entries with `tracing::warn!("skipping malformed gate history entry: {}", reason)`.

**1d. Remove unnecessary config load in `gate_history`** (issue: `gate-history-unused-config-load`):

Find and remove the config load that isn't used by the gate_history handler.

**1e. Surface `history_save_failure` error** (issue: `history-save-failure-not-surfaced`):

Find where history save errors are swallowed and propagate them to the caller or add them to the response `warnings` field.

**1f. Remove dead `wd_string` capture in session timeout** (issue: `session-timeout-dead-wd-capture`):

Find the dead variable capture in the timeout task and remove it.

**1g. Move resolved issues to `.planning/issues/closed/`.**
  </action>
  <verify>
`rtk cargo check` succeeds. `rtk cargo test -p assay-mcp` passes.
  </verify>
  <done>
Gate handler fixes applied: typed response, history counts, error surfacing, dead code removal.
  </done>
</task>

<task type="auto">
  <name>Task 2: Session and evaluator handler fixes</name>
  <files>
    crates/assay-mcp/src/server.rs
  </files>
  <action>
**2a. Fix `session_response` warnings always empty** (issue: `session-response-warnings-always-empty`):

Investigate why the warnings field is always empty. The Phase 35 OBS-01 requirement added warnings fields to mutating MCP responses. Find where warnings should be populated and ensure they flow through.

**2b. Update stale doc examples in session_create** (issues: `session-create-agent-command-example-stale`, `session-create-agent-model-example-stale`):

Update the tool description's examples to match current field names and valid values.

**2c. Fix `evaluate_criterion_result` re-serialization** (issue: `evaluate-criterion-result-freeform-enum-strings`):

The `outcome` and `enforcement` fields in `EvaluateCriterionResult` may be re-serialized as freeform strings instead of enum values. Fix to use proper enum serialization.

**2d. Use idiomatic shadowing for `spawn_blocking` clones** (issue: `spawn-blocking-clone-naming-convention`):

Replace `let config_clone = config.clone()` with idiomatic:
```rust
let config = config.clone();
```
(shadow the outer binding). Apply to all spawn_blocking call sites that use `_clone` suffix.

**2e. Validate non-zero timeout** (issue: `zero-timeout-not-validated`, Medium):

In the MCP server, validate that timeout values are non-zero where applicable. Return an error tool response if zero is provided.

**2f. Run `just ready`.**

**2g. Move resolved issues to `.planning/issues/closed/`** — all issues mentioned above.
  </action>
  <verify>
`just ready` passes. Gate finalize returns typed response. History entries include passed counts.
  </verify>
  <done>
Session and evaluator handler fixes applied. ~15 total code-change issues resolved.
  </done>
</task>

</tasks>

<verification>
```bash
just ready    # Full check suite: fmt-check + lint + test + deny
```

Verify:
- gate_finalize returns GateFinalizeResponse (not raw JSON)
- GateHistoryEntry includes required_passed and advisory_passed
- gate_history logs on malformed entries
- Zero timeout is rejected
- spawn_blocking uses idiomatic shadowing
</verification>

<success_criteria>
1. ~15 assay-mcp code-change issues resolved and moved to `.planning/issues/closed/`
2. GateFinalizeResponse type created and wired into handler
3. GateHistoryEntry has passed count fields
4. Error handling improved (no silent drops, warnings populated)
5. `just ready` passes
</success_criteria>

<output>
After completion, create `.planning/phases/45-tech-debt-cleanup/45-08-SUMMARY.md`
</output>
