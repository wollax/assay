---
phase: 45-tech-debt-cleanup
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/assay-types/src/validation.rs
  - crates/assay-core/src/spec/mod.rs
  - crates/assay-core/src/spec/validate.rs
autonomous: true
source_issue: "DEBT-01"
must_haves:
  truths:
    - CycleDiagnostic derives Debug
    - Diagnostic derives Hash
    - ValidationResult and DiagnosticSummary derive Default
    - DiagnosticSummary info field naming inconsistency resolved
    - depends field validates for duplicate entries
    - depends field validates for empty/whitespace-only entries
    - finalize_as_timed_out duplication extracted to shared helper
    - persisted field derivation made robust (not fragile on warnings.is_empty())
    - build_summary refactored to DiagnosticSummary method with functional style
    - validate_spec documents FeatureSpec skip behavior
    - dfs_invariant_violation emits warning instead of silent continue
    - Test additions for detect_cycles, validate_spec, agent_report
    - just ready passes
  artifacts:
    - crates/assay-types/src/validation.rs (derives, Default, naming)
    - crates/assay-core/src/spec/mod.rs (validation logic)
    - crates/assay-core/src/spec/validate.rs (if separate file — check structure)
    - .planning/issues/closed/ (resolved issues moved here)
  key_links:
    - Diagnostic Hash derive is the canonical fix (duplicate issue closed in Plan 01)
    - Validation types are used by spec_validate MCP tool
---

<objective>
Fix spec validation types and logic in assay-types validation module and assay-core spec module. Derives, validation rules, refactors, and test additions.

Purpose: Harden the spec validation subsystem used by the spec_validate MCP tool. Mix of trivial (derives) and low-complexity (validation rules, refactors) fixes.

Output: ~15 issues resolved across assay-types validation and assay-core spec.
</objective>

<execution_context>
<!-- Executor agent has built-in instructions for plan execution and summary creation -->
</execution_context>

<context>
@.planning/phases/pending/45-tech-debt-cleanup/45-CONTEXT.md
@.planning/phases/pending/45-tech-debt-cleanup/45-RESEARCH.md

@crates/assay-types/src/validation.rs
@crates/assay-core/src/spec/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validation type fixes (assay-types)</name>
  <files>
    crates/assay-types/src/validation.rs
  </files>
  <action>
**1a. Add missing derives:**

- `CycleDiagnostic`: add `Debug` (issue: `cycle-diagnostic-missing-debug-derive`). Check if the type exists in validation.rs or in assay-core. Find it first.
- `Diagnostic`: add `Hash` (issue: `diagnostic-derive-hash`). Verify all fields are Hash-compatible.
- `ValidationResult`: add `Default` (issue: `validation-result-diagnostic-summary-derive-default`). Verify fields have defaults.
- `DiagnosticSummary`: add `Default` (issue: same as above).

**1b. Fix `DiagnosticSummary` info naming** (issue: `diagnostic-summary-info-naming`):

Read the issue file to understand what the naming inconsistency is. Apply the fix (likely renaming a field or method to be consistent with other summary types).

**1c. Move resolved issues to `.planning/issues/closed/`:**
- `cycle-diagnostic-missing-debug-derive`
- `diagnostic-derive-hash`
- `validation-result-diagnostic-summary-derive-default`
- `diagnostic-summary-info-naming`
  </action>
  <verify>
`rtk cargo check` succeeds. `rtk cargo test -p assay-types` passes.
  </verify>
  <done>
Validation types have correct derives and consistent naming.
  </done>
</task>

<task type="auto">
  <name>Task 2: Spec validation logic fixes and tests (assay-core)</name>
  <files>
    crates/assay-core/src/spec/mod.rs
    crates/assay-core/src/spec/validate.rs
  </files>
  <action>
NOTE: The spec validation code may be in `crates/assay-core/src/spec/mod.rs`, `validate.rs`, or `validation.rs`. Search with `rtk grep "depends\|validate_spec\|build_summary\|finalize_as_timed_out\|dfs" crates/assay-core/src/spec/` to locate the right files.

**2a. Validate duplicate `depends` entries** (issue: `depends-allows-duplicate-entries`):

In spec validation, add a check that `depends` lists don't contain duplicate entries. Emit a diagnostic warning or error.

**2b. Validate empty/whitespace `depends` entries** (issue: `empty-whitespace-depends-not-validated`):

Add a check that `depends` entries are not empty strings or whitespace-only.

**2c. Document `depends` field slug-keyed behavior** (issue: `depends-field-slug-keyed-undocumented`):

Add doc comment explaining that depends entries are slug-keyed (spec file names without extension).

**2d. Extract `finalize_as_timed_out` shared helper** (issue: `finalize-as-timed-out-duplication`):

Find the duplicated timed-out finalization logic and extract into a single function. This may be in `assay-core/src/gate/session.rs` — search for it.

**2e. Fix `persisted` field fragile derivation** (issue: `persisted-field-fragile-derivation`):

The `persisted` field is currently derived from `warnings.is_empty()` which is fragile. Fix to use an explicit flag or more robust derivation.

**2f. Refactor `build_summary` to DiagnosticSummary method** (issue: `build-summary-as-diagnostic-summary-method`):

Move `build_summary` from a free function to an `impl DiagnosticSummary` method. Also refactor from imperative loop to functional fold/iterator (issue: `build-summary-imperative-loop-vs-functional`).

**2g. Document FeatureSpec skip** (issue: `validate-spec-feature-spec-ignored-undocumented`):

Add doc comment explaining that `FeatureSpec` variants are intentionally skipped during validation.

**2h. Fix `dfs` invariant violation** (issue: `dfs-invariant-violation-silent-continue`):

Replace silent `continue` on invariant violation with a `tracing::warn!` or `debug_assert!`.

**2i. Add tests:**
- `detect-cycles-diamond-dag-untested`: Test diamond DAG (A→B, A→C, B→D, C→D) has no false cycle detection
- `detect-cycles-empty-and-multi-unknown-dep-untested`: Test empty deps and multiple unknown deps
- `detect-cycles-unknown-dep-index-only-tested-at-0`: Test unknown dep at non-zero index
- `agent-report-criteria-index-gt-0-untested`: Test agent report with criteria index > 0
- `build-summary-info-count-untested`: Test build_summary with info-level diagnostics
- Any `validate-spec-*` test gaps that are straightforward

**2j. Run `just ready`.**

**2k. Move resolved issues to `.planning/issues/closed/`** — all issues mentioned above.
  </action>
  <verify>
`just ready` passes. New validation rules reject duplicate and empty depends entries.
  </verify>
  <done>
Spec validation logic hardened with new validation rules, refactored build_summary, DFS warning, and comprehensive test additions. ~15 total issues resolved.
  </done>
</task>

</tasks>

<verification>
```bash
just ready    # Full check suite: fmt-check + lint + test + deny
```

Verify:
- `Diagnostic` is Hash-able
- Duplicate depends entries produce validation diagnostic
- Empty/whitespace depends entries produce validation diagnostic
- build_summary is a method on DiagnosticSummary
- DFS invariant violation emits warning
</verification>

<success_criteria>
1. ~15 spec validation issues resolved and moved to `.planning/issues/closed/`
2. New validation rules catch duplicate and empty depends entries
3. build_summary refactored to functional method
4. Test coverage added for cycle detection edge cases
5. `just ready` passes
</success_criteria>

<output>
After completion, create `.planning/phases/45-tech-debt-cleanup/45-04-SUMMARY.md`
</output>
