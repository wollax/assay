---
phase: 32-cli-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/assay-cli/src/main.rs
  - crates/assay-cli/src/commands/spec.rs
autonomous: true
must_haves:
  truths:
    - Gate top-level in main.rs has no after_long_help attribute (examples live only on GateCommand::Run)
    - Spec top-level in main.rs has no after_long_help attribute (examples live only on SpecCommand::Show)
    - Running "assay gate" shows subcommand help via clap default (no custom help text)
    - Running "assay spec" shows subcommand help via clap default (no custom help text)
    - spec.rs color branch for spec show uses a single println with computed type_w (no duplication)
  artifacts:
    - Simplified Gate variant in main.rs (no after_long_help)
    - Simplified Spec variant in main.rs (no after_long_help)
    - Collapsed color branch in spec.rs spec show output
  key_links:
    - GateCommand::Run retains its own after_long_help with full examples
    - SpecCommand::Show retains its own after_long_help with examples
    - Removing top-level help does NOT affect subcommand help
---

<objective>
Remove duplicated help text from top-level Gate and Spec commands, and collapse duplicated color branches in spec show and gate history output.

Purpose: Eliminate help text duplication (users see examples once, at the subcommand level) and simplify color-branching code.
Output: Cleaner main.rs with no duplicate help, simplified display code in spec.rs and gate.rs.
</objective>

<execution_context>
<!-- Executor agent has built-in instructions -->
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/pending/32-cli-polish/32-RESEARCH.md
@.planning/phases/pending/32-cli-polish/32-CONTEXT.md

@crates/assay-cli/src/main.rs
@crates/assay-cli/src/commands/spec.rs
@crates/assay-cli/src/commands/gate.rs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Remove duplicated help text (CLI-02)</name>
  <files>crates/assay-cli/src/main.rs</files>
  <action>
1. Remove the `after_long_help` attribute from the `Gate` variant in the `Command` enum (main.rs lines ~78-90). The gate examples should only live on `GateCommand::Run` in gate.rs.

Keep the `/// Manage quality gates` doc comment — only remove the `#[command(after_long_help = "...")]` attribute.

2. Remove the `after_long_help` attribute from the `Spec` variant in the `Command` enum (main.rs lines ~63-72). The spec examples should only live on `SpecCommand::Show` and `SpecCommand::List` in spec.rs.

Keep the `/// Manage spec files` doc comment — only remove the `#[command(after_long_help = "...")]` attribute.

3. Verify that `assay gate --help` still shows the subcommand list (Run, History, Finalize, Report) via clap's default behavior.

4. Verify that `assay gate run --help` still shows the full examples from GateCommand::Run's after_long_help.
  </action>
  <verify>
```bash
just build && cargo run -p assay-cli -- gate --help && cargo run -p assay-cli -- gate run --help
```
  </verify>
  <done>
- Gate variant has no after_long_help in main.rs
- Spec variant has no after_long_help in main.rs
- Subcommand help still works via clap defaults
- GateCommand::Run and SpecCommand::Show retain their own help text
  </done>
</task>

<task type="auto">
  <name>Task 2: Collapse color branch duplication (CLI-04)</name>
  <files>crates/assay-cli/src/commands/spec.rs, crates/assay-cli/src/commands/gate.rs</files>
  <action>
**spec.rs — spec show type column (lines ~185-206):**

1. Find the two `println!` branches that differ only by `type_w` width (one for color, one for plain). The color branch adds ANSI_COLOR_OVERHEAD to the width.

2. Collapse to a single block:
```rust
let type_w = if color { base_type_w + ANSI_COLOR_OVERHEAD } else { base_type_w };
println!("  {:<name_w$}  {:<type_w$}  ...", name, type_str, ...);
```

The exact field names and format args should match the existing code — just unify the two branches into one `println!` with a computed `type_w`.

Note: gate.rs color branch deduplication is handled in Plan 04 (Wave 2) to avoid file conflicts with Plan 02.
  </action>
  <verify>
```bash
just test && just lint
```
  </verify>
  <done>
- spec.rs has one println! for the criteria table row (no color branching)
- Output is visually identical to before
  </done>
</task>
</tasks>

<verification>
```bash
just ready
```
Full check suite passes: fmt-check, lint, test, deny.
</verification>

<success_criteria>
- No duplicated help text between main.rs top-level commands and subcommands
- Color branch duplication eliminated in spec.rs and gate.rs
- All help text accessible via --help at the appropriate subcommand level
- `just ready` passes
</success_criteria>

<output>
After completion, create `.planning/phases/32-cli-polish/32-03-SUMMARY.md`
</output>
