---
phase: 32-cli-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/assay-types/src/lib.rs
  - crates/assay-cli/src/commands/mod.rs
  - crates/assay-cli/src/commands/init.rs
  - crates/assay-cli/src/commands/spec.rs
autonomous: true
must_haves:
  truths:
    - colors_enabled() returns false when NO_COLOR is set (any value including empty)
    - colors_enabled() returns false when stdout is not a TTY
    - colors_enabled() returns true only when NO_COLOR is absent AND stdout is a TTY
    - DIRECTORY_SPEC_INDICATOR constant equals "[srs]" and has a doc comment
    - All former "[srs]" literals in init.rs and spec.rs reference the constant instead
    - COLUMN_GAP constant is defined in commands/mod.rs and used in format strings
  artifacts:
    - DIRECTORY_SPEC_INDICATOR constant in crates/assay-types/src/lib.rs
    - COLUMN_GAP constant in crates/assay-cli/src/commands/mod.rs
    - Updated colors_enabled() with is_terminal() check in commands/mod.rs
  key_links:
    - colors_enabled() is called from gate.rs, spec.rs, context.rs, worktree.rs (11 call sites unchanged)
    - context.rs --plain flag pattern preserved (let color = !plain && colors_enabled())
    - DIRECTORY_SPEC_INDICATOR replaces "[srs]" at init.rs:71, spec.rs:92, spec.rs:259
---

<objective>
Add TTY detection to NO_COLOR handling, extract the "[srs]" magic string to a named constant in assay-types, and define a COLUMN_GAP constant for CLI table formatting.

Purpose: Fix CLI correctness (NO_COLOR per spec + TTY convention), eliminate magic strings, and make column formatting data-driven.
Output: Three constants/improvements in shared modules that other plans and existing code consume.
</objective>

<execution_context>
<!-- Executor agent has built-in instructions -->
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/pending/32-cli-polish/32-RESEARCH.md
@.planning/phases/pending/32-cli-polish/32-CONTEXT.md

@crates/assay-types/src/lib.rs
@crates/assay-cli/src/commands/mod.rs
@crates/assay-cli/src/commands/init.rs
@crates/assay-cli/src/commands/spec.rs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add TTY detection to colors_enabled() (CLI-01)</name>
  <files>crates/assay-cli/src/commands/mod.rs</files>
  <action>
1. Add `use std::io::IsTerminal;` to the imports in commands/mod.rs.

2. Update `colors_enabled()` to also check TTY:
```rust
pub(crate) fn colors_enabled() -> bool {
    std::env::var_os("NO_COLOR").is_none() && std::io::stdout().is_terminal()
}
```

3. Keep the function as a per-call-site helper (not startup-resolved) — this preserves the `--plain` override pattern in context.rs (`let color = !plain && colors_enabled()`).

4. No changes to call sites — the function signature is unchanged.
  </action>
  <verify>
```bash
just test && just lint
```
  </verify>
  <done>
- colors_enabled() checks both NO_COLOR and is_terminal()
- All existing call sites work without changes
- context.rs --plain pattern preserved
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract [srs] magic string to constant (CLI-08)</name>
  <files>crates/assay-types/src/lib.rs, crates/assay-cli/src/commands/init.rs, crates/assay-cli/src/commands/spec.rs</files>
  <action>
1. Add to `crates/assay-types/src/lib.rs`:
```rust
/// Marker prefix for directory-based specs in CLI output (e.g., `[srs] auth-flow`).
///
/// Directory specs store criteria across multiple files rather than in a single TOML.
pub const DIRECTORY_SPEC_INDICATOR: &str = "[srs]";
```
Place it near the top of the file, after any existing module declarations / re-exports.

Note: assay-types has `#![deny(missing_docs)]` — the doc comment is required.

2. In `crates/assay-cli/src/commands/init.rs` (line ~71), replace the `"[srs]"` string literal with `assay_types::DIRECTORY_SPEC_INDICATOR`.

3. In `crates/assay-cli/src/commands/spec.rs` (lines ~92 and ~259), replace `"[srs]"` string literals with `assay_types::DIRECTORY_SPEC_INDICATOR`.

4. Verify all three replacements compile and produce identical output.
  </action>
  <verify>
```bash
just test && just lint
```
  </verify>
  <done>
- DIRECTORY_SPEC_INDICATOR constant exists in assay-types with doc comment
- No remaining "[srs]" string literals in init.rs or spec.rs
- Output is identical to before the change
  </done>
</task>

<task type="auto">
  <name>Task 3: Add COLUMN_GAP constant (CLI-07)</name>
  <files>crates/assay-cli/src/commands/mod.rs</files>
  <action>
1. Add to the shared constants section in `commands/mod.rs`:
```rust
/// Column separator used in CLI table output (two spaces).
pub(crate) const COLUMN_GAP: &str = "  ";
```

2. Search for hardcoded `"  "` column separators in format strings across commands/ and replace with `COLUMN_GAP` where they serve as column gaps in tabular output. Use `format!()` or string concatenation as appropriate.

Note: Not all `"  "` strings are column gaps — only replace those used as separators between distinct columns in tabular output (spec list, gate results, history tables). Leave indentation strings unchanged.
  </action>
  <verify>
```bash
just test && just lint
```
  </verify>
  <done>
- COLUMN_GAP constant defined in commands/mod.rs
- Hardcoded column separators in tabular output replaced with the constant
- Output is identical to before the change
  </done>
</task>
</tasks>

<verification>
```bash
just ready
```
Full check suite passes: fmt-check, lint, test, deny.
</verification>

<success_criteria>
- colors_enabled() respects both NO_COLOR and TTY detection
- "[srs]" magic string extracted to DIRECTORY_SPEC_INDICATOR constant in assay-types
- COLUMN_GAP constant defined and used for tabular column separators
- `just ready` passes
</success_criteria>

<output>
After completion, create `.planning/phases/32-cli-polish/32-01-SUMMARY.md`
</output>
