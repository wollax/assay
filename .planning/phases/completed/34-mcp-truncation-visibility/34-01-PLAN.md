---
phase: 34
plan: 1
wave: 1
depends_on: []
files_modified:
  - crates/assay-mcp/src/server.rs
autonomous: true
source_issue: null
must_haves:
  truths:
    - "CriterionSummary includes truncated (Option<bool>) and original_bytes (Option<u64>) fields"
    - "Passed/failed criteria populate truncated and original_bytes from GateResult"
    - "Skipped criteria omit truncated and original_bytes from JSON output"
    - "Truncation fields are present regardless of include_evidence flag"
    - "just ready passes (fmt-check + lint + test + deny)"
  artifacts:
    - path: "crates/assay-mcp/src/server.rs"
      provides: "CriterionSummary with truncation metadata fields and mapping logic"
  key_links:
    - from: "GateResult.truncated / GateResult.original_bytes"
      to: "CriterionSummary.truncated / CriterionSummary.original_bytes"
      via: "format_gate_response() match arms"
---

<objective>
Expose truncation metadata in MCP gate responses so agents can programmatically detect when command output was truncated, without parsing the in-band `[truncated: N bytes omitted]` text marker.

This closes the final integration gap between Phase 29 (truncation engine) and Phase 33 (MCP response formatting). All changes are in a single file: `crates/assay-mcp/src/server.rs`.
</objective>

<context>
@crates/assay-mcp/src/server.rs
@crates/assay-types/src/gate.rs
@.planning/phases/pending/34-mcp-truncation-visibility/34-RESEARCH.md
</context>

<tasks>

<task type="auto">
<name>Task 1: Add truncation fields to CriterionSummary and populate in format_gate_response</name>
<files>crates/assay-mcp/src/server.rs</files>
<action>
1. Add two fields to the `CriterionSummary` struct (after the `stderr` field, before the closing brace at line 368):

```rust
/// Whether stdout or stderr was truncated due to size limits.
/// Absent for skipped criteria.
#[serde(skip_serializing_if = "Option::is_none")]
truncated: Option<bool>,
/// Original combined byte count before truncation.
/// Absent when output was not truncated or criterion was skipped.
#[serde(skip_serializing_if = "Option::is_none")]
original_bytes: Option<u64>,
```

2. Update all three match arms in `format_gate_response()` (starts at line 1198):

   - **Skipped arm** (line 1199, `None =>`): Add `truncated: None, original_bytes: None,`
   - **Passed arm** (line 1210, `Some(gate_result) if gate_result.passed =>`): Add `truncated: Some(gate_result.truncated), original_bytes: gate_result.original_bytes,`
   - **Failed arm** (line 1229, `Some(gate_result) =>`): Add `truncated: Some(gate_result.truncated), original_bytes: gate_result.original_bytes,`

3. Update all existing `CriterionSummary` construction sites in tests to include the new fields:

   - Line 1979 (passed criterion): `truncated: Some(false), original_bytes: None,`
   - Line 1990 (failed criterion): `truncated: Some(false), original_bytes: None,`
   - Line 2001 (skipped criterion): `truncated: None, original_bytes: None,`
   - Line 2104 (evidence test criterion): `truncated: Some(false), original_bytes: None,`
</action>
<verify>
```bash
cargo build -p assay-mcp 2>&1 | head -20
```
</verify>
<done>
- CriterionSummary has `truncated: Option<bool>` and `original_bytes: Option<u64>` fields
- All three match arms in format_gate_response populate the fields correctly
- All existing test construction sites compile
</done>
</task>

<task type="auto">
<name>Task 2: Add dedicated truncation visibility test</name>
<files>crates/assay-mcp/src/server.rs</files>
<action>
Add a new test in the existing `#[cfg(test)]` module that verifies truncation field behavior across all three criterion states and both evidence modes.

The test should:

1. Construct a `GateRunResponse` with three criteria:
   - **Passed + truncated**: `truncated: Some(true), original_bytes: Some(524_288)`
   - **Failed + not truncated**: `truncated: Some(false), original_bytes: None`
   - **Skipped**: `truncated: None, original_bytes: None`

2. Assert Rust struct values:
   - `criteria[0].truncated == Some(true)` and `criteria[0].original_bytes == Some(524_288)`
   - `criteria[1].truncated == Some(false)` and `criteria[1].original_bytes == None`
   - `criteria[2].truncated == None` and `criteria[2].original_bytes == None`

3. Serialize to JSON and assert:
   - `json["criteria"][0]["truncated"] == true` and `json["criteria"][0]["original_bytes"] == 524_288`
   - `json["criteria"][1]["truncated"] == false` and `json["criteria"][1].get("original_bytes").is_none()`
   - `json["criteria"][2].get("truncated").is_none()` and `json["criteria"][2].get("original_bytes").is_none()`

4. Add a second test (or extend the first) that uses `format_gate_response` with a `GateRunSummary` where one criterion has `truncated: true, original_bytes: Some(524_288)`:
   - Call with `include_evidence=false` and verify truncation fields are still present
   - Call with `include_evidence=true` and verify truncation fields are still present
   - This confirms truncation metadata is independent of the evidence flag

Use the existing `sample_summary()` helper if available, or construct inline. Follow the test naming and style conventions already present in the file.
</action>
<verify>
```bash
cargo test -p assay-mcp 2>&1 | tail -20
```
</verify>
<done>
- New test(s) verify truncated/original_bytes for passed, failed, and skipped criteria
- JSON serialization correctness verified (skip_serializing_if works for None)
- Truncation fields confirmed independent of include_evidence flag
</done>
</task>

</tasks>

<verification>
```bash
just ready
```
</verification>

<success_criteria>
- [ ] CriterionSummary struct has `truncated: Option<bool>` and `original_bytes: Option<u64>` with skip_serializing_if
- [ ] format_gate_response populates fields from GateResult for passed/failed, uses None for skipped
- [ ] Truncation fields present in MCP response regardless of include_evidence flag
- [ ] Skipped criteria omit truncated and original_bytes from serialized JSON
- [ ] Dedicated test covers all three criterion states and JSON serialization
- [ ] `just ready` passes
</success_criteria>
