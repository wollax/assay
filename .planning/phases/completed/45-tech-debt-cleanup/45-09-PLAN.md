---
phase: 45-tech-debt-cleanup
plan: 09
type: execute
wave: 3
depends_on: [02, 08]
files_modified:
  - crates/assay-mcp/src/server.rs
autonomous: true
source_issue: "DEBT-01"
must_haves:
  truths:
    - server module doc does not hardcode tool count
    - gate_report session_id semantics clarified in doc
    - gate_report comment noise cleaned up
    - gate_history total_runs doc clarified
    - get_info session vs gate workflow doc clarified
    - first_nonempty_line trims whitespace correctly
    - spec_get does not silently swallow feature spec errors
    - spec_get avoids unnecessary clone in resolved block
    - spec_get duplicate description removed
    - mcp test assertions pinned (no disjunctions)
    - Test additions for spec_get directory format, multiline stdout, failure reason helper
    - worktree_list MCP adds project check
    - just ready passes
  artifacts:
    - crates/assay-mcp/src/server.rs (doc fixes, spec_get fixes, worktree fix, tests)
    - .planning/issues/closed/ (resolved issues moved here)
  key_links:
    - Depends on Plan 08 for gate handler changes (avoid merge conflicts)
    - Depends on Plan 02 for type changes that affect server.rs
    - spec_get error surfacing improves observability for agents
---

<objective>
Fix documentation, spec_get handler, and add test coverage in assay-mcp. These are doc fixes, minor correctness fixes, and test additions — separated from Plan 08 to keep server.rs changes manageable.

Purpose: Complete the assay-mcp cleanup with doc polish, spec_get correctness, and test hardening.

Output: ~13 issues resolved (doc fixes + spec_get + tests + worktree).
</objective>

<execution_context>
<!-- Executor agent has built-in instructions for plan execution and summary creation -->
</execution_context>

<context>
@.planning/phases/pending/45-tech-debt-cleanup/45-CONTEXT.md
@.planning/phases/pending/45-tech-debt-cleanup/45-RESEARCH.md

@crates/assay-mcp/src/server.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Doc fixes and spec_get correctness</name>
  <files>
    crates/assay-mcp/src/server.rs
  </files>
  <action>
**1a. Remove hardcoded tool count from module doc** (issues: `server-module-doc-tool-count`, `tool-count-in-docs-fragile`):

Find the module-level doc comment that mentions a specific number of tools (e.g., "12 MCP tools"). Remove the count or replace with "multiple" to avoid staleness.

**1b. Clarify `gate_report` session_id semantics** (issue: `gate-report-session-id-ambiguous`):

Add/update the doc comment on `gate_report`'s `session_id` parameter to clarify what it represents (session ID vs gate run ID).

**1c. Clean up `gate_report` comment noise** (issue: `gate-report-warnings-comment-noise`):

Remove or consolidate noisy inline comments in the gate_report handler.

**1d. Clarify `gate_history` total_runs doc** (issue: `gate-history-total-runs-doc-unclear`):

Fix the doc comment on `total_runs` to clearly state what it counts.

**1e. Clarify `get_info` session vs gate workflow doc** (issue: `get-info-session-vs-gate-workflow-clarity`):

Add doc clarity to `get_info` about the difference between session workflow and gate workflow.

**1f. Fix `first_nonempty_line` whitespace handling** (issue: `first-nonempty-line-whitespace`, Medium):

The `first_nonempty_line` function should trim whitespace when checking for emptiness (a line with only spaces should be treated as empty). Fix the predicate:
```rust
.find(|line| !line.trim().is_empty())
```

**1g. Fix `spec_get` silent feature spec error** (issue: `spec-get-silent-feature-spec-error`, Important):

Find where `spec_get` swallows errors from feature spec resolution. Surface the error to the caller (either as an error response or in the warnings field).

**1h. Avoid unnecessary clone in `spec_get` resolved block** (issue: `resolved-block-clone-to-move`):

Replace `.clone()` with a move where the value is not used after the clone.

**1i. Remove duplicate description in `spec_get`** (issue: `spec-get-resolve-duplicate-description`):

Find and remove the duplicate description text.

**1j. Add project check to `worktree_list` MCP** (issue: `worktree-list-mcp-no-project-check`):

Add a `load_config` call (or equivalent project validation) to the `worktree_list` handler, matching the pattern used by other MCP tools.

**1k. Move resolved issues to `.planning/issues/closed/`.**
  </action>
  <verify>
`rtk cargo check` succeeds. `rtk cargo test -p assay-mcp` passes.
  </verify>
  <done>
Doc fixes applied. spec_get errors surfaced. Worktree list validates project.
  </done>
</task>

<task type="auto">
  <name>Task 2: Test improvements</name>
  <files>
    crates/assay-mcp/src/server.rs
  </files>
  <action>
**2a. Pin MCP test assertions** (issue: `mcp-tests-no-specs-disjunction`):

Find test assertions that use disjunctions (e.g., `assert!(result == A || result == B)`) and pin to specific expected values. If the test is non-deterministic, fix the source of non-determinism instead.

**2b. Add `spec_get` directory format test** (issue: `spec-get-resolve-directory-format-test`):

Add a test for `spec_get` when the spec is in a directory format (directory with multiple files) vs single file format.

**2c. Add multiline stdout fallback test** (issue: `multiline-stdout-fallback-test`):

Add a test for formatting multiline stdout output in failure reasons.

**2d. Extract failure reason test helper** (issue: `failure-reason-test-helper`):

Extract a helper function for constructing failure reason test fixtures to reduce test code duplication.

**2e. Run `just ready`.**

**2f. Move resolved issues to `.planning/issues/closed/`** — all issues mentioned above.
  </action>
  <verify>
`just ready` passes. No disjunctive assertions remain in MCP tests.
  </verify>
  <done>
Test improvements applied. ~13 total issues resolved across both tasks.
  </done>
</task>

</tasks>

<verification>
```bash
just ready    # Full check suite: fmt-check + lint + test + deny
```

Verify:
- Module doc does not mention specific tool count
- first_nonempty_line trims whitespace
- spec_get surfaces feature spec errors
- MCP tests use pinned assertions
- worktree_list validates project context
</verification>

<success_criteria>
1. ~13 assay-mcp doc/test issues resolved and moved to `.planning/issues/closed/`
2. spec_get no longer silently swallows feature spec errors
3. first_nonempty_line correctly handles whitespace-only lines
4. MCP test assertions are deterministic (no disjunctions)
5. `just ready` passes
</success_criteria>

<output>
After completion, create `.planning/phases/45-tech-debt-cleanup/45-09-SUMMARY.md`
</output>
