---
phase: 45-tech-debt-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/assay-core/src/gate/mod.rs
  - crates/assay-cli/src/commands/gate.rs
autonomous: true
source_issue: "DEBT-01"
must_haves:
  truths:
    - TruncationResult derives Debug
    - truncate_head_tail accepts String by value or returns Cow<str> to avoid unnecessary allocation
    - truncate_omitted has debug_assert! for invariant
    - CommandErrorKind derives Hash
    - enriched_error display function renamed to format_enriched_error
    - is_executable filter logic extracted to helper method
    - levenshtein_distance collects chars upfront instead of repeated .chars().nth()
    - evidence_truncation magic number replaced with named constant
    - spec_entry criteria extraction duplication consolidated
    - Test additions for truncation (marker newline, multiline input, over-budget assertions), exit code boundaries, levenshtein transposition
    - just ready passes
  artifacts:
    - crates/assay-core/src/gate/mod.rs (truncation fixes, gate fixes)
    - crates/assay-cli/src/commands/gate.rs (CLI gate fixes)
    - .planning/issues/closed/ (resolved issues moved here)
  key_links:
    - Truncation functions are used by gate_evaluate in assay-mcp (Plan 08)
    - CommandErrorKind Hash enables use in HashSet/HashMap if needed
---

<objective>
Fix truncation utilities and gate command helpers in assay-core and assay-cli. These are self-contained fixes to the gate subsystem — derives, refactors, named constants, and test additions.

Purpose: Harden the gate/truncation code that gate_evaluate depends on. Low risk — mostly derives, small refactors, and test additions.

Output: ~15 issues resolved across assay-core gate and assay-cli gate commands.
</objective>

<execution_context>
<!-- Executor agent has built-in instructions for plan execution and summary creation -->
</execution_context>

<context>
@.planning/phases/pending/45-tech-debt-cleanup/45-CONTEXT.md
@.planning/phases/pending/45-tech-debt-cleanup/45-RESEARCH.md

@crates/assay-core/src/gate/mod.rs
@crates/assay-cli/src/commands/gate.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Truncation fixes and derives</name>
  <files>
    crates/assay-core/src/gate/mod.rs
  </files>
  <action>
**1a. Add `#[derive(Debug)]` to `TruncationResult`** (issue: `truncation-result-missing-debug`)

**1b. `truncate_head_tail` allocation fix** (issue: `truncate-head-tail-unnecessary-alloc`):

Review the function signature. If it takes `&str` and returns `String`, consider accepting `String` by value and returning it unchanged when no truncation is needed (avoids allocation on the passthrough path). Alternatively, return `Cow<'_, str>`. Pick whichever is simpler given the call sites. If the change ripples too far, skip and note in summary.

**1c. Add `debug_assert!` to `truncate_omitted`** (issue: `truncate-omitted-debug-assert`):

Add a debug assertion for the documented invariant (e.g., that omitted count + included count = total count, or whatever invariant the code assumes). Read the function to identify the invariant.

**1d. `evidence_truncation` magic number** (issue: `evidence-truncation-magic-number`):

Find the magic number used for evidence truncation budget and replace with a named constant (e.g., `const EVIDENCE_TRUNCATION_BUDGET: usize = 4096;` or whatever the value is).

**1e. `spec_entry` criteria extraction duplication** (issue: `spec-entry-criteria-extraction-dup`):

Find the duplicate match blocks for extracting criteria from spec entries. Consolidate into a single helper function.

**1f. Add tests** for:
- `truncate-marker-newline-test`: Test that truncation markers end with newlines
- `truncate-multiline-input-test`: Test truncation with multi-line input
- `truncate-over-budget-test-assertions`: Strengthen existing over-budget test with additional assertions (check exact output, not just length)

**1g. Move resolved issues to `.planning/issues/closed/`:**
- `truncation-result-missing-debug`
- `truncate-head-tail-unnecessary-alloc`
- `truncate-omitted-debug-assert`
- `truncate-marker-newline-test`
- `truncate-multiline-input-test`
- `truncate-over-budget-test-assertions`
- `evidence-truncation-magic-number`
- `spec-entry-criteria-extraction-dup`
  </action>
  <verify>
`rtk cargo test -p assay-core` passes. `rtk cargo check` succeeds.
  </verify>
  <done>
Truncation utilities hardened with Debug derive, allocation fix, debug_assert, named constant, and 3 new tests.
  </done>
</task>

<task type="auto">
  <name>Task 2: Gate command helpers and remaining fixes</name>
  <files>
    crates/assay-core/src/gate/mod.rs
    crates/assay-cli/src/commands/gate.rs
  </files>
  <action>
**2a. `CommandErrorKind` derive Hash** (issue: `command-error-kind-derive-hash`):

Add `Hash` to `CommandErrorKind`'s derive list. Verify all variants are Hash-compatible.

**2b. Rename enriched error display** (issue: `enriched-error-display-rename`):

Find the enriched error display function and rename to `format_enriched_error` (or `fmt_enriched_error` following Rust conventions). Update all call sites.

**2c. Extract `is_executable` filter helper** (issue: `is-executable-filter-repeated`):

Find the repeated `is_executable` filter logic and extract into a named helper method.

**2d. Levenshtein chars optimization** (issue: `levenshtein-collect-chars-upfront`):

In the levenshtein distance function, collect both strings into `Vec<char>` upfront instead of calling `.chars().nth(i)` repeatedly (which is O(n) per call on UTF-8 strings).

**2e. Add tests:**
- `classify-exit-code-boundary-tests`: Test exit code classification at boundaries (0, 1, 126, 127, 128, 255)
- `gate-exit-code-doc-cross-ref`: Add doc comment cross-referencing exit code classification (not a test — just a `/// See also: classify_exit_code` on relevant functions)
- `levenshtein-transposition-test`: Add test case for transposition (e.g., "ab" vs "ba")

**2f. Run `just ready` to verify everything passes.**

**2g. Move resolved issues to `.planning/issues/closed/`:**
- `command-error-kind-derive-hash`
- `enriched-error-display-rename`
- `is-executable-filter-repeated`
- `levenshtein-collect-chars-upfront`
- `classify-exit-code-boundary-tests`
- `gate-exit-code-doc-cross-ref`
- `levenshtein-transposition-test`
  </action>
  <verify>
`just ready` passes. All renamed functions are updated at call sites.
  </verify>
  <done>
Gate command helpers cleaned up. ~15 total issues resolved across both tasks and moved to closed.
  </done>
</task>

</tasks>

<verification>
```bash
just ready    # Full check suite: fmt-check + lint + test + deny
```

Verify:
- TruncationResult is Debug-printable
- CommandErrorKind is Hash-able
- Levenshtein uses Vec<char> for O(1) indexing
- New tests cover truncation edge cases and exit code boundaries
</verification>

<success_criteria>
1. ~15 assay-core/assay-cli gate issues resolved and moved to `.planning/issues/closed/`
2. No functional regressions — existing tests pass
3. New tests added for truncation and exit code boundaries
4. `just ready` passes
</success_criteria>

<output>
After completion, create `.planning/phases/45-tech-debt-cleanup/45-03-SUMMARY.md`
</output>
