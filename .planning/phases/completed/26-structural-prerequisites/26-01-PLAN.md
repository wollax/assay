---
phase: 26-structural-prerequisites
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/assay-cli/src/main.rs
  - crates/assay-cli/src/commands/mod.rs
  - crates/assay-cli/src/commands/checkpoint.rs
  - crates/assay-cli/src/commands/context.rs
  - crates/assay-cli/src/commands/gate.rs
  - crates/assay-cli/src/commands/init.rs
  - crates/assay-cli/src/commands/mcp.rs
  - crates/assay-cli/src/commands/spec.rs
autonomous: true
must_haves:
  truths:
    - "CLI compiles and all existing tests pass after extraction"
    - "Each subcommand group lives in its own module file"
    - "main.rs contains only Cli struct, top-level Command enum, and dispatch"
  artifacts:
    - path: "crates/assay-cli/src/commands/mod.rs"
      provides: "Module declarations, shared helpers (colors, formatting, ANSI constants)"
      min_lines: 100
    - path: "crates/assay-cli/src/commands/gate.rs"
      provides: "GateCommand enum + all gate handlers + StreamConfig + stream_criterion"
      min_lines: 400
    - path: "crates/assay-cli/src/commands/context.rs"
      provides: "ContextCommand + GuardCommand enums + context/guard handlers"
      min_lines: 300
    - path: "crates/assay-cli/src/commands/spec.rs"
      provides: "SpecCommand enum + spec handlers"
      min_lines: 150
    - path: "crates/assay-cli/src/commands/checkpoint.rs"
      provides: "CheckpointCommand enum + checkpoint handlers"
      min_lines: 100
    - path: "crates/assay-cli/src/commands/mcp.rs"
      provides: "McpCommand enum + MCP serve handler"
      min_lines: 20
    - path: "crates/assay-cli/src/commands/init.rs"
      provides: "handle_init function"
      min_lines: 10
    - path: "crates/assay-cli/src/main.rs"
      provides: "Cli struct, Command enum, dispatch logic, main()"
      min_lines: 50
  key_links:
    - from: "crates/assay-cli/src/main.rs"
      to: "crates/assay-cli/src/commands/mod.rs"
      via: "mod commands; use commands::*;"
    - from: "crates/assay-cli/src/commands/mod.rs"
      to: "crates/assay-cli/src/commands/gate.rs"
      via: "pub mod gate; pub use gate::*;"
---

<objective>
Extract the 2563-line CLI monolith (`main.rs`) into a `commands/` module hierarchy with one module per subcommand group.

Purpose: Unblock all v0.3.0 CLI feature work (worktree commands, CLI polish) by making the codebase modular and navigable.
Output: `commands/` directory with 6 handler modules + mod.rs, and a slim `main.rs` (~80 lines).
</objective>

<execution_context>
<!-- Executor agent has built-in instructions for plan execution and summary creation -->
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/pending/26-structural-prerequisites/26-CONTEXT.md
@.planning/phases/pending/26-structural-prerequisites/26-RESEARCH.md

@crates/assay-cli/src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create commands/ module structure and extract shared helpers</name>
  <files>
    crates/assay-cli/src/commands/mod.rs
    crates/assay-cli/src/commands/init.rs
    crates/assay-cli/src/commands/mcp.rs
  </files>
  <action>
1. Create `crates/assay-cli/src/commands/` directory.
2. Create `commands/mod.rs` with:
   - `pub mod` declarations for all 6 submodules (checkpoint, context, gate, init, mcp, spec)
   - `pub use` re-exports so `main.rs` can `use commands::*`
   - Move shared CLI helpers here: ANSI color constants (`ANSI_COLOR_OVERHEAD`), `ASSAY_DIR_NAME`, `assay_dir()`, any color/formatting functions, NO_COLOR detection
   - Mark helpers as `pub(crate)` — not `pub`
3. Create `commands/init.rs` — move `handle_init` (the Init variant handler) here. Keep it simple: just the init logic. Use `pub(crate)` visibility.
4. Create `commands/mcp.rs` — move `McpCommand` enum and `handle_mcp_serve` here. The async MCP serve function must preserve its tokio runtime boundary. Use `pub(crate)` visibility for the enum and handler.

IMPORTANT:
- Do NOT change any function signatures or behavior — pure extraction
- Use `pub(crate)` for all items that don't need to be public outside the crate
- Preserve all doc comments and `#[command]` / `#[arg]` attributes on enums
- Do NOT move the `Cli` struct or top-level `Command` enum — those stay in main.rs
  </action>
  <verify>
    `cargo check -p assay-cli` compiles without errors or warnings
  </verify>
  <done>
    `commands/mod.rs` exists with all module declarations and shared helpers. `init.rs` and `mcp.rs` compile and contain their respective handlers.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract remaining command modules and slim main.rs</name>
  <files>
    crates/assay-cli/src/commands/gate.rs
    crates/assay-cli/src/commands/context.rs
    crates/assay-cli/src/commands/spec.rs
    crates/assay-cli/src/commands/checkpoint.rs
    crates/assay-cli/src/main.rs
  </files>
  <action>
1. Create `commands/gate.rs` — move `GateCommand` enum, `handle_gate_run`, `handle_gate_run_all`, `handle_gate_report`, `handle_gate_history`, `stream_criterion`, `StreamConfig`, and all gate-related helpers. This is the largest module (~500 lines). Preserve `#[cfg(unix)]` / `#[cfg(not(unix))]` guards on signal handling.
2. Create `commands/context.rs` — move `ContextCommand` enum, `GuardCommand` enum, `handle_context_diagnose`, `handle_context_estimate`, `handle_guard_start`, `handle_guard_stop`, `handle_guard_status`. Guard commands create a tokio runtime — preserve that boundary.
3. Create `commands/spec.rs` — move `SpecCommand` enum, `handle_spec_list`, `handle_spec_show`, and any spec formatting helpers.
4. Create `commands/checkpoint.rs` — move `CheckpointCommand` enum, `handle_checkpoint_save`, `handle_checkpoint_restore`, `handle_checkpoint_list`.
5. Slim `main.rs` down to:
   - `mod commands;` declaration
   - `use` imports from commands and external crates
   - `Cli` struct with `#[derive(Parser)]`
   - Top-level `Command` enum with `#[derive(Subcommand)]` — each variant delegates to a subcommand enum from the modules (e.g., `Gate { #[command(subcommand)] command: commands::gate::GateCommand }`)
   - `fn main()` with the dispatch match

IMPORTANT:
- Do NOT change any function signatures, behavior, or test assertions
- Keep all `#[cfg(unix)]` / `#[cfg(not(unix))]` guard pairs intact
- Async functions (guard start, MCP serve) must preserve their runtime creation patterns
- All existing tests must pass unchanged — do NOT modify tests to accommodate the extraction
- Use qualified paths (`commands::gate::GateCommand`) or re-exports — pick one approach and be consistent
  </action>
  <verify>
    `cargo check -p assay-cli && cargo test -p assay-cli` — zero errors, zero test failures
  </verify>
  <done>
    `main.rs` is ~80 lines. All 6 command modules exist with correct handlers. `cargo test -p assay-cli` passes. `just ready` passes.
  </done>
</task>

</tasks>

<verification>
```bash
# All must pass:
cargo check -p assay-cli
cargo test -p assay-cli
just ready

# Structural checks:
test -f crates/assay-cli/src/commands/mod.rs
test -f crates/assay-cli/src/commands/gate.rs
test -f crates/assay-cli/src/commands/context.rs
test -f crates/assay-cli/src/commands/spec.rs
test -f crates/assay-cli/src/commands/checkpoint.rs
test -f crates/assay-cli/src/commands/mcp.rs
test -f crates/assay-cli/src/commands/init.rs

# main.rs should be small:
wc -l crates/assay-cli/src/main.rs  # expect ~80 lines
```
</verification>

<success_criteria>
- CLI monolith is split into 6 command modules + mod.rs
- main.rs contains only Cli struct, Command enum, and dispatch (~80 lines)
- All existing tests pass without modification
- `just ready` passes
</success_criteria>

<output>
After completion, create `.planning/phases/26-structural-prerequisites/26-01-SUMMARY.md`
</output>
