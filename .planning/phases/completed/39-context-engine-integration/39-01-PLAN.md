---
phase: 39-context-engine-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/assay-cupel/ (deleted)
autonomous: true
source_issue: ""

must_haves:
  truths:
    - "cargo check succeeds with cupel as a workspace dependency"
    - "crates/assay-cupel/ directory no longer exists"
    - "No workspace member or dependency references assay-cupel"
  artifacts:
    - "Cargo.toml with cupel path dependency and assay-cupel removed"
  key_links:
    - "cupel path dependency resolves to ../cupel/crates/cupel"
    - "workspace members glob still works after assay-cupel deletion"
---

<objective>
Replace the stale in-repo `assay-cupel` prototype with the external `cupel` v1.0.0 crate as a workspace dependency.

Purpose: The external cupel crate at `../cupel/crates/cupel` is the stable v1.0.0 context engine. The in-repo `crates/assay-cupel` is a stale copy of cupel's internals that must be fully removed to avoid confusion and compilation conflicts.

Output: Clean workspace with cupel as an external path dependency and no trace of assay-cupel.
</objective>

<execution_context>
<!-- Executor agent has built-in instructions for plan execution and summary creation -->
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/pending/39-context-engine-integration/39-CONTEXT.md
@.planning/phases/pending/39-context-engine-integration/39-RESEARCH.md

@Cargo.toml
@crates/assay-cupel/Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove stale assay-cupel and add external cupel dependency</name>
  <files>Cargo.toml, crates/assay-cupel/ (entire directory)</files>
  <action>
  1. Delete the entire `crates/assay-cupel/` directory (stale prototype).

  2. In the root `Cargo.toml`, remove the line:
     `assay-cupel = { path = "crates/assay-cupel" }`
     from `[workspace.dependencies]`.

  3. Add the external cupel dependency to `[workspace.dependencies]`:
     `cupel = { path = "../cupel/crates/cupel" }`

  4. Verify no other crate in the workspace has `assay-cupel` as a dependency (grep all `Cargo.toml` files). If any do, remove those references.

  5. Run `cargo check` to verify the workspace compiles cleanly with the new dependency available.

  NOTE: Do NOT add `cupel` as a dependency to any specific crate's `Cargo.toml` yet -- that happens in plan 02 when the integration code is written.
  </action>
  <verify>
  - `ls crates/assay-cupel` fails (directory deleted)
  - `grep -r "assay-cupel" Cargo.toml` returns nothing
  - `grep "cupel" Cargo.toml` shows the new path dependency
  - `rtk cargo check` succeeds
  </verify>
  <done>
  - `crates/assay-cupel/` directory is fully deleted
  - Root `Cargo.toml` has `cupel = { path = "../cupel/crates/cupel" }` in workspace dependencies
  - Root `Cargo.toml` has no `assay-cupel` reference
  - `cargo check` passes
  </done>
</task>

</tasks>

<verification>
- `just build` succeeds (workspace compiles)
- No references to `assay-cupel` exist anywhere in workspace Cargo files
- `cupel` is listed in `[workspace.dependencies]`
</verification>

<success_criteria>
- Stale assay-cupel prototype completely removed from workspace
- External cupel v1.0.0 available as workspace dependency
- All existing crates continue to compile without errors
</success_criteria>

<output>
After completion, create `.planning/phases/39-context-engine-integration/39-01-SUMMARY.md`
</output>
