---
phase: 01-workspace-prerequisites
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - deny.toml
  - crates/assay-mcp/Cargo.toml
  - crates/assay-mcp/src/lib.rs
autonomous: true

must_haves:
  truths:
    - "schemars 1.x is the workspace-level dependency (no 0.8 remnants)"
    - "All existing JsonSchema derives in assay-types compile without source changes"
    - "rmcp 0.17 is a workspace dependency with server and transport-io features"
    - "assay-mcp crate exists as a library and passes cargo check"
    - "`just ready` passes with zero warnings and zero errors"
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace dependency declarations for schemars 1.x, rmcp 0.17, and assay-mcp path reference"
      min_lines: 15
    - path: "crates/assay-mcp/Cargo.toml"
      provides: "assay-mcp crate manifest with workspace deps"
      min_lines: 10
    - path: "crates/assay-mcp/src/lib.rs"
      provides: "Minimal lib crate entry point (doc comment only)"
      min_lines: 1
  key_links:
    - from: "Cargo.toml"
      to: "crates/assay-types/Cargo.toml"
      via: "schemars workspace dependency — version change must not break assay-types derives"
    - from: "Cargo.toml"
      to: "crates/assay-mcp/Cargo.toml"
      via: "workspace member glob crates/* must pick up assay-mcp; rmcp and assay-core declared as workspace deps"
    - from: "deny.toml"
      to: "Cargo.toml"
      via: "cargo-deny license allow-list must cover all new transitive dependencies from rmcp"
---

<objective>
Upgrade schemars from 0.8 to 1.x across the workspace and scaffold the assay-mcp library crate with rmcp as its primary dependency.

Purpose: Eliminate the mandatory schemars version blocker that prevents adding rmcp (which requires schemars 1.x), and create the assay-mcp crate so downstream phases can build MCP server functionality on top of it.

Output: Updated root Cargo.toml with schemars 1.x and rmcp 0.17, a new assay-mcp library crate scaffold, updated deny.toml if needed, and a passing `just ready`.
</objective>

<execution_context>
<!-- Executor agent has built-in instructions for plan execution and summary creation -->
</execution_context>

<context>
@CLAUDE.md
@Cargo.toml
@deny.toml
@crates/assay-types/Cargo.toml
@crates/assay-types/src/lib.rs
@crates/assay-core/Cargo.toml
@crates/assay-cli/Cargo.toml
@crates/assay-tui/Cargo.toml
@.planning/phases/pending/01-workspace-prerequisites/01-CONTEXT.md
@.planning/phases/pending/01-workspace-prerequisites/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade schemars and add rmcp workspace dependency</name>
  <files>Cargo.toml, deny.toml</files>
  <action>
  In the root Cargo.toml `[workspace.dependencies]` section:
  1. Change `schemars = "0.8"` to `schemars = "1"`.
  2. Add `rmcp = { version = "0.17", features = ["server", "transport-io"] }`.
  3. Add `assay-mcp = { path = "crates/assay-mcp" }` to the workspace dependencies (following the existing pattern of assay-types and assay-core).

  After making the Cargo.toml changes, run `cargo check --workspace` to verify compilation. The existing `use schemars::JsonSchema` and `#[derive(JsonSchema)]` in assay-types should compile without any source changes.

  Then run `just deny`. If cargo-deny fails on license issues from rmcp's transitive dependencies (likely `ident_case` with non-SPDX `MIT/Apache-2.0` expression), fix deny.toml by adding the necessary license or exception. The research identified this as a known pitfall. Use whatever minimal deny.toml change resolves the issue cleanly.

  Do NOT modify any source files in existing crates (assay-types, assay-core, assay-cli, assay-tui). The schemars upgrade must be a pure version bump with zero code changes.
  </action>
  <verify>
  Run these commands and confirm they all succeed:
  - `cargo check --workspace` — zero errors
  - `just deny` — zero failures
  - Inspect `crates/assay-types/src/lib.rs` and confirm it is UNCHANGED from its current content (no source edits needed)
  </verify>
  <done>schemars 1.x and rmcp 0.17 are in workspace dependencies. All existing crates compile. cargo-deny passes.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold assay-mcp library crate</name>
  <files>crates/assay-mcp/Cargo.toml, crates/assay-mcp/src/lib.rs</files>
  <action>
  Create the assay-mcp crate directory and files:

  1. Create `crates/assay-mcp/Cargo.toml`:
     - Use `.workspace = true` for version, edition, license, repository (matching existing crate conventions).
     - Set `description = "MCP server library for Assay"`.
     - Dependencies (all `.workspace = true`): assay-core, rmcp, schemars, serde, serde_json, tokio.
     - Do NOT add any other dependencies beyond what's listed.

  2. Create `crates/assay-mcp/src/lib.rs`:
     - Minimal content: a module-level doc comment explaining the crate's purpose.
     - No module stubs, no `todo!()` macros, no imports, no code.
     - This is an intentionally empty library crate scaffold.

  The workspace glob `members = ["crates/*"]` will auto-include assay-mcp. Verify with `cargo check -p assay-mcp`.

  Then run `just ready` as the final gate. This must pass completely (fmt-check, lint, test, deny).
  </action>
  <verify>
  Run these commands and confirm they all succeed:
  - `cargo check -p assay-mcp` — zero errors
  - `cargo metadata --no-deps --format-version 1 | grep assay-mcp` — confirms workspace includes the crate
  - `just ready` — all checks pass (fmt-check, lint, test, deny)
  </verify>
  <done>assay-mcp library crate exists with correct dependencies. `cargo check -p assay-mcp` passes. `just ready` passes with zero errors and zero warnings.</done>
</task>

</tasks>

<verification>
Final phase verification — all of these must be true:

1. `grep 'schemars' Cargo.toml` shows `schemars = "1"` (not `"0.8"`)
2. `grep 'rmcp' Cargo.toml` shows rmcp 0.17 with server and transport-io features
3. `diff` of `crates/assay-types/src/lib.rs` against its current content shows zero changes
4. `cargo check -p assay-mcp` succeeds
5. `just ready` passes (fmt-check + lint + test + deny)
</verification>

<success_criteria>
1. `just ready` passes with schemars 1.x and the new assay-mcp crate in the workspace
2. All existing derives (Serialize, Deserialize, JsonSchema) compile without modification beyond the version bump
3. `cargo check -p assay-mcp` succeeds (empty lib crate with rmcp dependency)
</success_criteria>

<output>
After completion, create `.planning/phases/01-workspace-prerequisites/01-01-SUMMARY.md`
</output>
