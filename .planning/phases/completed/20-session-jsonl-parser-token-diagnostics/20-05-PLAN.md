---
phase: 20
plan: 5
wave: 4
depends_on: [20-03, 20-04]
files_modified:
  - crates/assay-cli/src/main.rs
  - crates/assay-mcp/src/server.rs
autonomous: true
must_haves:
  truths:
    - "`just ready` passes (fmt-check + lint + test + deny)"
    - "All 8 SDIAG requirements are satisfied end-to-end"
    - "CLI help text accurately describes all context commands"
    - "No clippy warnings in any workspace crate"
  artifacts: []
  key_links: []
---

<objective>
Final integration verification. Run `just ready` to confirm the entire workspace compiles, passes lint, tests, fmt-check, and cargo-deny. Fix any issues found. This is the quality gate before the phase is marked complete.
</objective>

<context>
@Cargo.toml — workspace root
@crates/assay-cli/src/main.rs — CLI with new context commands (Plan 03)
@crates/assay-mcp/src/server.rs — MCP with new context tools (Plan 04)
@crates/assay-core/src/context/mod.rs — core context API (Plan 02)
@crates/assay-types/src/context.rs — context types (Plan 01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run just ready and fix issues</name>
  <files>any files with issues</files>
  <action>
  Run `just ready` which executes: fmt-check, lint (clippy -D warnings), test (all workspace tests), and deny (cargo-deny).

  Fix any issues found:
  - **fmt**: Run `just fmt` to auto-fix formatting
  - **clippy**: Fix lint warnings (unused imports, unnecessary clones, missing docs, etc.)
  - **test failures**: Debug and fix failing tests
  - **deny**: Resolve any license or advisory issues with the new `dirs` and `regex-lite` dependencies

  Do NOT weaken tests or suppress warnings. Fix the actual issues.

  After fixing, run `just ready` again to confirm clean pass.
  </action>
  <verify>
  ```bash
  cd /Users/wollax/Git/personal/assay && just ready 2>&1
  ```
  </verify>
  <done>`just ready` passes with zero warnings and zero failures</done>
</task>

<task type="auto">
  <name>Task 2: Smoke test CLI commands</name>
  <files></files>
  <action>
  Run the actual CLI commands to verify they work end-to-end:

  ```bash
  # Verify help text
  cargo run -p assay-cli -- context --help
  cargo run -p assay-cli -- context diagnose --help
  cargo run -p assay-cli -- context list --help

  # Test against real sessions (if available)
  cargo run -p assay-cli -- context list
  cargo run -p assay-cli -- context list --tokens
  cargo run -p assay-cli -- context list --json
  cargo run -p assay-cli -- context diagnose
  cargo run -p assay-cli -- context diagnose --json
  cargo run -p assay-cli -- context diagnose --plain
  ```

  If no Claude Code sessions exist for the current project, verify the error message is clear and actionable.

  Check that JSON output is valid JSON (parseable).
  Check that plain output has no ANSI escape codes.
  Check that the dashboard sections render correctly.
  </action>
  <verify>
  ```bash
  cd /Users/wollax/Git/personal/assay && cargo run -p assay-cli -- context list --json 2>&1 | head -5
  ```
  </verify>
  <done>CLI commands produce expected output or clear error messages; JSON output is valid; plain output has no ANSI codes</done>
</task>

</tasks>

<verification>
```bash
cd /Users/wollax/Git/personal/assay && just ready
```
</verification>

<success_criteria>
1. `just ready` passes cleanly (fmt-check + lint + test + deny)
2. `assay context diagnose` produces output or a clear error
3. `assay context list` produces output or a clear error
4. `--json` produces valid JSON
5. `--plain` produces no ANSI escape codes
6. No clippy warnings across the workspace
</success_criteria>
