# Phase 11: Type System Foundation - Context

**Gathered:** 2026-03-04
**Status:** Ready for planning

<domain>
## Phase Boundary

Relocate result types from assay-core to assay-types and enforce serde hygiene across all domain types. Integrate all uncommitted type work (feature_spec.rs, gates_spec.rs, and related changes) into a single coherent delivery. This is the highest-churn refactor that must land before any feature work.

</domain>

<decisions>
## Implementation Decisions

### Re-export strategy
- **Clean break** — no re-exports from assay-core. All consumers update imports to `assay_types` directly
- Move obvious misplaced candidates too, not just GateRunSummary and CriterionResult — if other types clearly belong in assay-types, relocate them now to avoid a second migration
- Claude's discretion: whether assay-mcp imports from assay-types directly or through assay-core (decide based on current dependency graph)
- Claude's discretion: module structure in assay-types (flat vs submodules) — decide based on how many types end up there

### Serde hygiene scope
- Claude's discretion on which existing types get `skip_serializing_if` — assess which types have Optional fields and apply where it matters
- Claude's discretion on whether to add planned fields (like enforcement) now vs just establishing the pattern
- Roundtrip serde test is sufficient for backward compatibility — no need for a v0.1.0 fixture test
- Claude's discretion on whether serde changes and schema updates happen in one pass or separate steps

### Uncommitted type work
- **Integrate all uncommitted changes** into Phase 11 — feature_spec.rs, gates_spec.rs, and all modified files (error.rs, gate/mod.rs, init.rs, spec/mod.rs, server.rs, lib.rs)
- **Finalize in Phase 11** — no baseline commit first. Phase 11 reviews, cleans up, and commits everything together as one coherent delivery
- Claude's discretion on whether new schema files (feature-spec, gates-spec, gate-criterion) are explicit deliverables or auto-generated byproducts

### Schema snapshot strategy
- **Update snapshots in place** — use `cargo insta review` to accept changes. No parallel old/new snapshots
- Claude's discretion on whether to reorganize snapshot tests systematically or just add for new types
- Claude's discretion on schemas/ directory regeneration timing
- Claude's discretion on snapshot naming convention (type name vs module path)

### Claude's Discretion
- assay-mcp dependency routing (direct assay-types or through assay-core)
- Module structure in assay-types (flat vs domain submodules)
- Which existing types get serde hygiene sweep
- Whether to front-load fields needed by Phase 13 (enforcement)
- Schema update pass strategy (inline vs batch)
- Snapshot test organization and naming
- schemas/ directory regeneration timing

</decisions>

<specifics>
## Specific Ideas

No specific requirements — open to standard approaches. User wants a clean break with no backward-compat re-exports, and all uncommitted work folded into a single coherent delivery.

</specifics>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope

</deferred>

---

*Phase: 11-type-system-foundation*
*Context gathered: 2026-03-04*
