---
phase: 61-type-correctness-serde-consistency
plan: 02
type: execute
wave: 2
depends_on: ["61-01"]
files_modified:
  - crates/assay-types/src/criterion.rs
  - crates/assay-core/src/gate/mod.rs
  - crates/assay-core/src/pipeline_checkpoint.rs
  - crates/assay-core/src/spec/mod.rs
  - crates/assay-core/src/spec/validate.rs
  - crates/assay-core/src/spec/coverage.rs
autonomous: true
requirements: [TYPE-01, TYPE-03]

must_haves:
  truths:
    - "`Criterion.when` is `When` (not `Option<When>`) — compiler enforces at all sites"
    - "Omitting `when` in TOML/JSON deserializes to `When::SessionEnd` via `#[serde(default)]`"
    - "Serializing a criterion with `When::SessionEnd` omits the `when` key"
    - "`When::AfterToolCalls { n: 0 }` fails deserialization with clear error message"
  artifacts:
    - path: "crates/assay-types/src/criterion.rs"
      provides: "When::is_session_end method, nonzero_u32 deserializer, Criterion.when as When"
      contains: "pub when: When"
    - path: "crates/assay-types/src/criterion.rs"
      provides: "nonzero_u32 custom deserializer"
      contains: "fn nonzero_u32"
  key_links:
    - from: "crates/assay-core/src/gate/mod.rs"
      to: "crates/assay-types/src/criterion.rs"
      via: "criterion.when direct match (no Option unwrap)"
      pattern: "criterion\\.when"
    - from: "crates/assay-core/src/pipeline_checkpoint.rs"
      to: "crates/assay-types/src/criterion.rs"
      via: "criterion.when direct match in has_checkpoint_criteria"
      pattern: "c\\.when"
---

<objective>
Change `Criterion.when` from `Option<When>` to `When` with compiler-guided sweep across ~110 sites, add `When::is_session_end` helper, and add `nonzero_u32` validation on `AfterToolCalls.n`.

Purpose: Eliminate the representational ambiguity where `None` and `Some(SessionEnd)` mean the same thing, and prevent nonsensical `AfterToolCalls { n: 0 }`.
Output: Updated `Criterion` struct, all consumers fixed, validation in place, schema snapshots updated.
</objective>

<execution_context>
@/Users/wollax/.claude/kata-orchestrator/workflows/execute-plan.md
@/Users/wollax/.claude/kata-orchestrator/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/active/61-type-correctness-serde-consistency/61-CONTEXT.md
@.planning/phases/active/61-type-correctness-serde-consistency/61-RESEARCH.md
@.planning/phases/active/61-type-correctness-serde-consistency/61-01-SUMMARY.md
@crates/assay-types/src/criterion.rs
@crates/assay-core/src/gate/mod.rs
@crates/assay-core/src/pipeline_checkpoint.rs
@crates/assay-core/src/spec/mod.rs
@crates/assay-core/src/spec/validate.rs
@crates/assay-core/src/spec/coverage.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Change Criterion.when to non-optional When with validation</name>
  <files>crates/assay-types/src/criterion.rs</files>
  <behavior>
    - Test: `When::SessionEnd` is the default (existing test, update assertion from `None`)
    - Test: Criterion with no `when` field in TOML deserializes to `when: When::SessionEnd`
    - Test: Criterion with `when: When::SessionEnd` serializes without `when` key in output
    - Test: Pre-M024 roundtrip fixture asserts `c.when == When::SessionEnd` (not `None`)
    - Test: `AfterToolCalls { n: 0 }` deserialization returns error containing "must be >= 1"
    - Test: `AfterToolCalls { n: 1 }` deserializes successfully
  </behavior>
  <action>
1. Add `When::is_session_end(&self) -> bool` method: `matches!(self, Self::SessionEnd)`
2. Add private `fn nonzero_u32` deserializer that rejects `n == 0` with error message "AfterToolCalls.n must be >= 1 (got 0)"
3. Add `#[serde(deserialize_with = "nonzero_u32")]` on `AfterToolCalls { n: u32 }`
4. Change `Criterion.when` field from `Option<When>` to `When`
5. Update serde attributes on `when` field: `#[serde(default, skip_serializing_if = "When::is_session_end")]`
6. Update ALL test struct literals in `criterion.rs` tests module:
   - `when: None` becomes `when: When::SessionEnd` or `when: When::default()`
   - `when: Some(When::AfterToolCalls { n })` becomes `when: When::AfterToolCalls { n }`
   - `when: Some(When::OnEvent { ... })` becomes `when: When::OnEvent { ... }`
7. Update `criterion_when_roundtrip_pre_m024_fixture` test: change assertion from `c.when == None` to `c.when == When::SessionEnd`
8. Add test `when_after_tool_calls_zero_rejected`: deserialize JSON `{"type":"after_tool_calls","n":0}` into `When` and assert error
9. Add test `when_after_tool_calls_one_accepted`: deserialize JSON `{"type":"after_tool_calls","n":1}` into `When` and assert success
10. Run schema snapshot update: `INSTA_UPDATE=always cargo nextest run -p assay-types --test schema_snapshots` — the `criterion-schema` snapshot will change (when field is no longer optional)
  </action>
  <verify>
    <automated>rtk cargo nextest run -p assay-types 2>&1 | tail -20</automated>
  </verify>
  <done>`Criterion.when` is `When` (not `Option`). `is_session_end` method exists. `nonzero_u32` rejects zero. All assay-types tests pass including snapshots.</done>
</task>

<task type="auto">
  <name>Task 2: Update all assay-core consumers for non-optional When</name>
  <files>crates/assay-core/src/gate/mod.rs, crates/assay-core/src/pipeline_checkpoint.rs, crates/assay-core/src/spec/mod.rs, crates/assay-core/src/spec/validate.rs, crates/assay-core/src/spec/coverage.rs</files>
  <action>
This is a compiler-guided mechanical sweep. After Task 1, `cargo check` will emit type errors at every site that uses `Option<When>`. Fix each one:

In `crates/assay-core/src/gate/mod.rs`:
1. `criterion_matches_phase`: change match from `None | Some(When::SessionEnd) => false` to `When::SessionEnd => false`, `Some(When::AfterToolCalls { n }) => ...` to `When::AfterToolCalls { n } => ...`, etc. Remove all `Some()` wrappers.
2. Update ALL test struct literals (~15 sites): `when: None` → `when: When::SessionEnd`, `when: Some(When::AfterToolCalls { n: X })` → `when: When::AfterToolCalls { n: X }`

In `crates/assay-core/src/pipeline_checkpoint.rs`:
1. `has_checkpoint_criteria`: update pattern matching on `c.when` — remove `Some()` wrappers
2. `make_spec_with_checkpoint` helper: `when: Some(when)` → `when` (direct field shorthand)
3. `make_spec_session_end_only` helper: `when: None` → `when: When::SessionEnd`
4. Update ALL test struct literals (~18 sites): same Option removal pattern

In `crates/assay-core/src/spec/mod.rs`:
1. Update ~18 struct literal sites: `when: None` → `when: When::SessionEnd`

In `crates/assay-core/src/spec/validate.rs`:
1. Update ~12 struct literal sites: `when: None` → `when: When::SessionEnd`

In `crates/assay-core/src/spec/coverage.rs`:
1. Update 1 struct literal: `when: None` → `when: When::SessionEnd`

Strategy: Run `cargo check -p assay-core 2>&1` after each file to track remaining errors. Continue until zero errors.
  </action>
  <verify>
    <automated>rtk cargo nextest run -p assay-types -p assay-core 2>&1 | tail -20</automated>
  </verify>
  <done>Zero compilation errors. All `when: None` and `when: Some(...)` patterns replaced. Full assay-types + assay-core test suite green.</done>
</task>

</tasks>

<verification>
1. `rtk cargo check --workspace` — clean compilation
2. `rtk cargo nextest run -p assay-types -p assay-core` — all tests pass
3. `grep -rn "Option<When>" crates/ --include="*.rs"` — zero matches (type fully non-optional)
4. `grep -rn "when: None" crates/ --include="*.rs"` — zero matches in production/test code
5. `grep -rn "when: Some" crates/ --include="*.rs"` — zero matches
</verification>

<success_criteria>
- `Criterion.when` is `When` everywhere — no `Option<When>` remains
- `When::is_session_end` method exists and used by `skip_serializing_if`
- `AfterToolCalls { n: 0 }` rejected at deserialize time with descriptive error
- All ~110 struct literal sites updated (compiler verified)
- Schema snapshots updated for `criterion-schema`
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/active/61-type-correctness-serde-consistency/61-02-SUMMARY.md`
</output>
