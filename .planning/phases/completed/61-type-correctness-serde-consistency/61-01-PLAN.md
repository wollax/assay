---
phase: 61-type-correctness-serde-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/assay-types/src/review.rs
  - crates/assay-types/tests/schema_snapshots.rs
  - crates/assay-core/src/gate/mod.rs
  - crates/assay-core/src/pipeline_checkpoint.rs
  - crates/assay-core/src/review/mod.rs
  - crates/assay-cli/src/commands/spec.rs
autonomous: true
requirements: [TYPE-02, TYPE-05, TYPE-07]

must_haves:
  truths:
    - "`review::CheckpointPhase` is the canonical type name — no `SessionPhase` exists"
    - "`CheckpointPhase::OnEvent` deserializes from both `on_event` and `at_event` JSON tags"
    - "`evaluate_checkpoint` has a doc comment explaining SessionEnd is a no-op"
    - "`debug_assert!` fires if SessionEnd is passed to `evaluate_checkpoint` in debug builds"
  artifacts:
    - path: "crates/assay-types/src/review.rs"
      provides: "CheckpointPhase enum with OnEvent variant and at_event alias"
      contains: "pub enum CheckpointPhase"
    - path: "crates/assay-core/src/gate/mod.rs"
      provides: "evaluate_checkpoint doc comment and debug_assert"
      contains: "debug_assert!"
  key_links:
    - from: "crates/assay-core/src/pipeline_checkpoint.rs"
      to: "crates/assay-types/src/review.rs"
      via: "direct import of CheckpointPhase (no alias)"
      pattern: "use assay_types::review::CheckpointPhase"
    - from: "crates/assay-core/src/gate/mod.rs"
      to: "crates/assay-types/src/review.rs"
      via: "direct import of CheckpointPhase"
      pattern: "use assay_types::review::CheckpointPhase"
---

<objective>
Rename `review::SessionPhase` to `review::CheckpointPhase`, merge `AtEvent` into `OnEvent` with serde alias, and add SessionEnd no-op documentation + debug_assert to `evaluate_checkpoint`.

Purpose: Eliminate the type name collision between `review::SessionPhase` and `work_session::SessionPhase`, and document the SessionEnd no-op contract.
Output: Renamed enum, merged variant, updated imports across all consumers, updated schema snapshots.
</objective>

<execution_context>
@/Users/wollax/.claude/kata-orchestrator/workflows/execute-plan.md
@/Users/wollax/.claude/kata-orchestrator/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/active/61-type-correctness-serde-consistency/61-CONTEXT.md
@.planning/phases/active/61-type-correctness-serde-consistency/61-RESEARCH.md
@crates/assay-types/src/review.rs
@crates/assay-core/src/gate/mod.rs
@crates/assay-core/src/pipeline_checkpoint.rs
@crates/assay-cli/src/commands/spec.rs
@crates/assay-core/src/review/mod.rs
@crates/assay-types/tests/schema_snapshots.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename SessionPhase to CheckpointPhase and merge AtEvent into OnEvent</name>
  <files>crates/assay-types/src/review.rs, crates/assay-core/src/pipeline_checkpoint.rs, crates/assay-core/src/gate/mod.rs, crates/assay-core/src/review/mod.rs, crates/assay-cli/src/commands/spec.rs</files>
  <action>
In `crates/assay-types/src/review.rs`:
1. Rename `pub enum SessionPhase` to `pub enum CheckpointPhase`
2. Merge `AtEvent { event_type: String }` variant into `OnEvent { event_type: String }` — delete the `AtEvent` variant entirely
3. Add `#[serde(alias = "at_event")]` on the `OnEvent` variant for backward compat with persisted GateDiagnostic JSON
4. Update the schema registry entry: change name from `"checkpoint-session-phase"` to `"checkpoint-phase"`, update type path to `CheckpointPhase`
5. Update `GateDiagnostic.session_phase` field type annotation (if it references the old name)
6. Add a new test `checkpoint_phase_on_event_alias_roundtrip` that verifies `{ "type": "at_event", "event_type": "foo" }` JSON deserializes to `CheckpointPhase::OnEvent { event_type: "foo".into() }`

In `crates/assay-core/src/pipeline_checkpoint.rs`:
1. Change `use assay_types::review::SessionPhase as CheckpointPhase` to `use assay_types::review::CheckpointPhase` (direct import, no alias)
2. Update any match arms referencing `AtEvent` to `OnEvent`

In `crates/assay-core/src/gate/mod.rs`:
1. Update import from `SessionPhase as CheckpointPhase` (or `SessionPhase`) to direct `use assay_types::review::CheckpointPhase`
2. Update any match arms referencing `AtEvent` to `OnEvent`

In `crates/assay-core/src/review/mod.rs`:
1. Update reference from `assay_types::review::SessionPhase::SessionEnd` to `CheckpointPhase::SessionEnd`

In `crates/assay-cli/src/commands/spec.rs`:
1. Update type path from `SessionPhase` to `CheckpointPhase`
2. Update match arm from `AtEvent` to `OnEvent`
  </action>
  <verify>
    <automated>rtk cargo check --workspace 2>&1 | head -50</automated>
  </verify>
  <done>No `SessionPhase` references remain in codebase (only `CheckpointPhase`). `AtEvent` variant removed, `OnEvent` with alias in place. Workspace compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add SessionEnd no-op documentation and debug_assert, update schema snapshots</name>
  <files>crates/assay-core/src/gate/mod.rs, crates/assay-types/tests/schema_snapshots.rs</files>
  <action>
In `crates/assay-core/src/gate/mod.rs`:
1. Add doc comment above `evaluate_checkpoint` function explaining that SessionEnd phase is a no-op — criteria never match SessionEnd in this function because `criterion_matches_phase` returns false for SessionEnd. Callers should use `evaluate_all_with_events` for session-end evaluation.
2. Add `debug_assert!(!matches!(phase, CheckpointPhase::SessionEnd), "evaluate_checkpoint called with SessionEnd phase — this is a no-op; use evaluate_all_with_events for session-end evaluation")` at the top of `evaluate_checkpoint` body.
3. Find the existing test `evaluate_checkpoint_with_session_end_phase_skips_all_criteria` (around line 3284). This test passes `CheckpointPhase::SessionEnd` to `evaluate_checkpoint` which will now panic in debug mode. Delete this test — the no-op behavior is now enforced by the debug_assert and documented. If you want, replace it with a test that calls `criterion_matches_phase` with `SessionEnd` and asserts it returns `false`.

In `crates/assay-types/tests/schema_snapshots.rs`:
1. Find the `checkpoint_session_phase_schema_snapshot` test function — rename it to `checkpoint_phase_schema_snapshot`
2. Update the type reference from `assay_types::review::SessionPhase` to `assay_types::review::CheckpointPhase`
3. Update the snapshot name string from `"checkpoint-session-phase-schema"` to `"checkpoint-phase-schema"`
4. Run `INSTA_UPDATE=always cargo nextest run -p assay-types --test schema_snapshots` to regenerate all affected snapshots
5. Run `INSTA_UPDATE=always cargo nextest run -p assay-core` to update any core snapshots
6. Verify all snapshot files are updated and no stale `checkpoint-session-phase-schema` snap files remain (delete if so)
  </action>
  <verify>
    <automated>rtk cargo nextest run -p assay-types -p assay-core 2>&1 | tail -20</automated>
  </verify>
  <done>All tests pass. Schema snapshots reflect `CheckpointPhase` name. `debug_assert!` present in `evaluate_checkpoint`. Old SessionEnd test removed or restructured.</done>
</task>

</tasks>

<verification>
1. `rtk cargo check --workspace` — clean compilation
2. `rtk cargo nextest run -p assay-types -p assay-core` — all tests pass
3. `grep -r "SessionPhase" crates/ --include="*.rs"` — no references remain (only in comments if any)
4. `grep "debug_assert" crates/assay-core/src/gate/mod.rs` — debug_assert present
</verification>

<success_criteria>
- `review::SessionPhase` fully renamed to `review::CheckpointPhase` across all crates
- `AtEvent` variant merged into `OnEvent` with `#[serde(alias = "at_event")]`
- Schema registry entry renamed to `"checkpoint-phase"`
- `evaluate_checkpoint` has doc comment and `debug_assert!` for SessionEnd
- All schema snapshots updated
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/active/61-type-correctness-serde-consistency/61-01-SUMMARY.md`
</output>
