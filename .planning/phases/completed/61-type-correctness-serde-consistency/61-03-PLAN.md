---
phase: 61-type-correctness-serde-consistency
plan: 03
type: execute
wave: 3
depends_on: ["61-02"]
files_modified:
  - crates/assay-types/src/criterion.rs
  - crates/assay-core/src/gate/mod.rs
  - crates/assay-core/src/pipeline_checkpoint.rs
  - .assay/specs/self-check.toml
  - examples/close-the-loop/gates.toml
autonomous: true
requirements: [TYPE-04, TYPE-06]

must_haves:
  truths:
    - "`CriterionKind` uses internally tagged serde with `tag = \"type\"` and `rename_all = \"snake_case\"`"
    - "Old PascalCase format (`{ type = \"AgentReport\" }`) deserializes via alias"
    - "In-repo TOML spec files use new `kind = { type = \"agent_report\" }` format"
    - "`evaluate_checkpoint` passes `cli_timeout` and `config_timeout` through to `evaluate_all_with_events`"
    - "Timeout precedence documented: CLI > config > spec-level > default"
  artifacts:
    - path: "crates/assay-types/src/criterion.rs"
      provides: "CriterionKind with internally tagged serde and aliases"
      contains: "tag = \"type\""
    - path: "crates/assay-core/src/gate/mod.rs"
      provides: "evaluate_checkpoint with timeout params"
      contains: "cli_timeout"
    - path: ".assay/specs/self-check.toml"
      provides: "Migrated spec file with new kind format"
      contains: "kind.type"
    - path: "examples/close-the-loop/gates.toml"
      provides: "Migrated example spec with new kind format"
      contains: "kind.type"
  key_links:
    - from: "crates/assay-core/src/pipeline_checkpoint.rs"
      to: "crates/assay-core/src/gate/mod.rs"
      via: "evaluate_checkpoint call with timeout params"
      pattern: "evaluate_checkpoint.*cli_timeout"
    - from: ".assay/specs/self-check.toml"
      to: "crates/assay-types/src/criterion.rs"
      via: "TOML deserialization of CriterionKind"
      pattern: "kind.*type.*=.*agent_report"
---

<objective>
Align `CriterionKind` serde tagging to internally tagged format (consistent with `When` enum), migrate in-repo TOML spec files, and thread CLI/config timeout overrides through `evaluate_checkpoint`.

Purpose: Make `CriterionKind` serialization consistent with the rest of the type system, and fix silently dropped timeout overrides.
Output: Updated `CriterionKind` with aliases, migrated spec files, `evaluate_checkpoint` with timeout threading, updated schema snapshots.
</objective>

<execution_context>
@/Users/wollax/.claude/kata-orchestrator/workflows/execute-plan.md
@/Users/wollax/.claude/kata-orchestrator/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/active/61-type-correctness-serde-consistency/61-CONTEXT.md
@.planning/phases/active/61-type-correctness-serde-consistency/61-RESEARCH.md
@.planning/phases/active/61-type-correctness-serde-consistency/61-01-SUMMARY.md
@.planning/phases/active/61-type-correctness-serde-consistency/61-02-SUMMARY.md
@crates/assay-types/src/criterion.rs
@crates/assay-core/src/gate/mod.rs
@crates/assay-core/src/pipeline_checkpoint.rs
@.assay/specs/self-check.toml
@examples/close-the-loop/gates.toml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Align CriterionKind serde tagging and migrate spec files</name>
  <files>crates/assay-types/src/criterion.rs, .assay/specs/self-check.toml, examples/close-the-loop/gates.toml</files>
  <behavior>
    - Test: `CriterionKind::AgentReport` serializes to JSON `{"type":"agent_report"}`
    - Test: `{"type":"AgentReport"}` JSON deserializes to `CriterionKind::AgentReport` via alias
    - Test: `{"type":"agent_report"}` JSON deserializes to `CriterionKind::AgentReport`
    - Test: `CriterionKind::NoToolErrors` round-trips through JSON as `{"type":"no_tool_errors"}`
    - Test: `CriterionKind::EventCount` with fields round-trips correctly
    - Test: `{"type":"EventCount","event_type":"foo"}` deserializes via alias
    - Test: In-repo TOML files parse successfully after migration
  </behavior>
  <action>
CRITICAL PITFALL: Internally tagged enums cannot deserialize TOML bare strings. The current `kind = "AgentReport"` format WILL break. In-repo TOML files MUST be migrated in this same task.

In `crates/assay-types/src/criterion.rs`:
1. Add `#[serde(tag = "type", rename_all = "snake_case")]` to `CriterionKind` enum
2. Add `#[serde(alias = "AgentReport")]` on `AgentReport` variant
3. Add `#[serde(alias = "NoToolErrors")]` on `NoToolErrors` variant
4. Add `#[serde(alias = "EventCount")]` on `EventCount` variant
5. Keep the existing `Display` impl using PascalCase — that is for human-readable output, separate from serde
6. Add tests:
   - `criterion_kind_internally_tagged_roundtrip`: serialize each variant to JSON, verify `type` tag present with snake_case value
   - `criterion_kind_alias_roundtrip`: deserialize old PascalCase `{ "type": "AgentReport" }` format and verify it works
   - `criterion_kind_event_count_roundtrip`: test EventCount with fields in both old and new format
7. Run `INSTA_UPDATE=always cargo nextest run -p assay-types --test schema_snapshots` to update `criterion-kind-schema` snapshot (now internally tagged)

Migrate in-repo TOML files:
1. `.assay/specs/self-check.toml`: change `kind = "AgentReport"` to inline table `kind = { type = "agent_report" }` (or TOML dotted key `kind.type = "agent_report"` — use inline table for readability)
2. `examples/close-the-loop/gates.toml`: change `kind = "NoToolErrors"` to `kind = { type = "no_tool_errors" }`, and any `EventCount` entries similarly
3. After migration, verify the TOML files still parse: write a test or use existing spec parsing to load them

Note: The `examples/close-the-loop/gates.toml` file may be deleted on disk (shown in git status as deleted). If so, skip it — only migrate files that exist.
  </action>
  <verify>
    <automated>rtk cargo nextest run -p assay-types 2>&1 | tail -20</automated>
  </verify>
  <done>`CriterionKind` uses `#[serde(tag = "type", rename_all = "snake_case")]`. PascalCase aliases work. In-repo TOML files migrated and parseable. Schema snapshot updated.</done>
</task>

<task type="auto">
  <name>Task 2: Thread CLI/config timeout overrides through evaluate_checkpoint</name>
  <files>crates/assay-core/src/gate/mod.rs, crates/assay-core/src/pipeline_checkpoint.rs</files>
  <action>
In `crates/assay-core/src/gate/mod.rs`:
1. Update `evaluate_checkpoint` signature: add `cli_timeout: Option<u64>` and `config_timeout: Option<u64>` parameters after the existing `phase` parameter
2. Add doc comment documenting timeout precedence: CLI > config > spec-level > default
3. Change the inner call from `evaluate_all_with_events(&checkpoint_spec, working_dir, None, None, events)` to `evaluate_all_with_events(&checkpoint_spec, working_dir, cli_timeout, config_timeout, events)`
4. Update ALL call sites of `evaluate_checkpoint` within `gate/mod.rs` tests — add `None, None` for the new timeout params (tests don't exercise timeout threading)

In `crates/assay-core/src/pipeline_checkpoint.rs`:
1. Update `drive_checkpoints` signature: add `cli_timeout: Option<u64>` and `config_timeout: Option<u64>` parameters
2. Thread these through to the `evaluate_checkpoint` call inside `drive_checkpoints`
3. Update ALL call sites of `drive_checkpoints` — if there are callers in pipeline code, add `None, None` or thread from the caller's context
4. Update ALL call sites of `evaluate_checkpoint` if called directly from `pipeline_checkpoint.rs` tests — add `None, None`
5. Search for any other call sites across the workspace: `grep -rn "evaluate_checkpoint\|drive_checkpoints" crates/ --include="*.rs"` — update them all

Run full workspace check and tests to verify no breakage.
  </action>
  <verify>
    <automated>rtk cargo nextest run -p assay-types -p assay-core 2>&1 | tail -20</automated>
  </verify>
  <done>`evaluate_checkpoint` and `drive_checkpoints` accept `cli_timeout` and `config_timeout`. Timeout precedence documented. All call sites updated. Full test suite green.</done>
</task>

</tasks>

<verification>
1. `rtk cargo check --workspace` — clean compilation
2. `rtk cargo nextest run --workspace` — all tests pass
3. `just ready` — full check suite passes (fmt, lint, test, deny)
4. `grep -rn "tag = \"type\"" crates/assay-types/src/criterion.rs` — present on CriterionKind
5. `grep -rn "cli_timeout" crates/assay-core/src/gate/mod.rs` — timeout params present
</verification>

<success_criteria>
- `CriterionKind` serde tagging matches `When` enum style (internally tagged, snake_case)
- PascalCase aliases allow deserializing old format JSON
- In-repo TOML spec files migrated to `kind = { type = "..." }` format
- `evaluate_checkpoint` accepts and threads CLI/config timeout overrides
- Timeout precedence documented in doc comment
- `just ready` passes (full suite: fmt, lint, test, deny)
</success_criteria>

<output>
After completion, create `.planning/phases/active/61-type-correctness-serde-consistency/61-03-SUMMARY.md`
</output>
