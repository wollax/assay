# `StreamConfig::new()` Missing Doc Comment

## Description

`StreamConfig::new()` is the only undocumented associated function in the `StreamCounters`/`StreamConfig` group. All sibling items have doc comments; this one should too.

## File Reference

`crates/assay-cli/src/commands/gate.rs`

## Category

docs
