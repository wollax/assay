---
milestone: v0.2.0
audited: 2026-03-08T21:04:00-06:00
status: passed
scores:
  requirements: 52/52
  phases: 15/15
  integration: 10/10
  flows: 5/5
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: "22-pruning-engine"
    items:
      - "CLI --restore flag only lists backups; restore_backup() exists but is not wired to CLI"
  - phase: "20-session-diagnostics"
    items:
      - "sessions_from_history() marked #[allow(dead_code)] — pub fn never called from CLI or MCP"
  - phase: "general"
    items:
      - "Empty review and workflow placeholder modules in assay-core (pre-v0.2.0)"
---

# v0.2.0 Milestone Audit Report

## Overview

| Category | Score | Status |
|----------|-------|--------|
| Requirements | 52/52 (100%) | All satisfied |
| Phases | 15/15 (100%) | All verified |
| Integration | 10/10 (100%) | All connections wired |
| E2E Flows | 5/5 (100%) | All complete |

**Build:** `just ready` clean. All tests pass.

---

## Requirements Coverage

All 52 requirements mapped to phases 11-25 are satisfied:

| Category | Requirements | Status |
|----------|-------------|--------|
| Type System (TYPE-01 to TYPE-04) | 4 | All complete |
| Enforcement (ENFC-01 to ENFC-04) | 4 | All complete |
| History (HIST-01 to HIST-04) | 4 | All complete |
| Agent Gates (AGNT-01 to AGNT-05) | 5 | All complete |
| MCP Hardening (MCP-01 to MCP-04) | 4 | All complete |
| CLI Hardening (CLI-01 to CLI-04) | 4 | All complete |
| Testing (TEST-01 to TEST-03) | 3 | All complete |
| Tooling (TOOL-01 to TOOL-03) | 3 | All complete |
| Session Diagnostics (SDIAG-01 to SDIAG-08) | 8 | All complete |
| Team Protection (TPROT-01 to TPROT-13) | 13 | All complete |

---

## Phase Verification Status

| Phase | Name | Score | Status |
|-------|------|-------|--------|
| 11 | Type System Foundation | 11/11 | PASS |
| 12 | FileExists Gate Wiring | 5/5 | PASS |
| 13 | Enforcement Levels | ENFC-01/02 + 4 SC | PASS |
| 14 | Run History Core | 13/13 | PASS |
| 15 | Run History CLI | 14/14 | PASS |
| 16 | Agent Gate Recording | 25/25 | PASS |
| 17 | MCP Hardening & Agent History | 8/8 | PASS |
| 18 | CLI Hardening & Enforcement | 5/5 | PASS |
| 19 | Testing & Tooling | 13/13 | PASS |
| 20 | Session JSONL Parser & Diagnostics | 34/34 | PASS |
| 21 | Team State Checkpointing | 24/24 | PASS |
| 22 | Pruning Engine | 10/10 | PASS |
| 23 | Guard Daemon & Recovery | 32/32 | PASS |
| 24 | MCP History Persistence Fix | 6/6 | PASS |
| 25 | Tech Debt Cleanup | 10/10 | PASS |

---

## Cross-Phase Integration

### Type Wiring (5/5 Connected)

| Flow | From | Through | To | Status |
|------|------|---------|----|----|
| GateRunSummary | assay-types | evaluate_all (core) | CLI + MCP | Connected |
| Enforcement | assay-types | resolve_enforcement (core) | CLI exit code + MCP response | Connected |
| GateRunRecord | assay-types | history::save (core) | CLI + MCP gate_finalize + MCP gate_run | Connected |
| AgentReport | assay-types | gate_report/finalize (MCP) | session + history | Connected |
| GuardConfig | assay-types | config load (core) | daemon + CLI guard | Connected |

### History Persistence (5/5 Call Sites)

| Call Site | Purpose | Status |
|-----------|---------|--------|
| CLI gate run | Saves after evaluate_all | Connected |
| MCP gate_run (command-only) | Phase 24 fix — immediate save | Connected |
| MCP gate_finalize | Saves via finalize_session() | Connected |
| MCP timeout handler | Auto-saves timed-out sessions | Connected |
| CLI gate run --all | Per-spec history save | Connected |

### Context Pipeline (4/4 Stages)

| Stage | Module | Produces | Consumed By |
|-------|--------|----------|-------------|
| Parser | context/parser.rs | Vec\<ParsedEntry\> | Extractor, Pruning, Diagnostics |
| Extractor | checkpoint/extractor.rs | TeamCheckpoint | Checkpoint save, Guard daemon |
| Pruning | context/pruning/ | Modified session file | Guard daemon (soft/hard threshold) |
| Guard | guard/daemon.rs | Threshold responses | Circuit breaker, escalation |

### MCP Tools (8/8 Registered)

All 8 MCP tools are registered on AssayServer: spec_list, spec_get, gate_run, gate_report, gate_finalize, gate_history, context_diagnose, estimate_tokens.

### CLI Commands (17/17 Wired)

All CLI subcommands delegate to core functions correctly.

### Plugin Hooks (5/5 Valid)

All hook scripts referenced in hooks.json exist and use proper guards.

### E2E User Flows (5/5 Complete)

1. **Gate Run with History** — CLI → load spec → evaluate → save history → display with exit code
2. **Agent Gate Recording** — MCP gate_run → gate_report → gate_finalize → history save
3. **History Query** — CLI history / MCP gate_history → list → load → display
4. **Context Diagnostics** — CLI context diagnose → discover session → parse JSONL → report
5. **Guard Daemon** — CLI guard start → load config → spawn daemon → watcher + polling → prune → circuit breaker

---

## Previous Audit Gaps — All Resolved

| Gap (from 2026-03-07 audit) | Resolution |
|------------------------------|------------|
| MCP gate_run does not persist history for command-only specs | Fixed in Phase 24 |
| Phases 16, 19, 20 missing VERIFICATION.md | Backfilled in Phase 25 |
| 70+ open issues accumulated | Triaged in Phase 25 (17 closed, 106 categorized) |

---

## Tech Debt (Non-Blocking)

### 1. CLI --restore does not restore backups (Phase 22)

The `--restore` flag on `assay context prune` only lists available backups. The `restore_backup()` function exists in `assay_core::context::pruning::backup` but has no CLI caller.

### 2. Dead code: sessions_from_history() (Phase 20)

`crates/assay-core/src/context/discovery.rs` — `pub fn sessions_from_history()` is marked `#[allow(dead_code)]` and never called from CLI or MCP. Intended for future multi-session discovery.

### 3. Empty placeholder modules (Pre-v0.2.0)

`review` and `workflow` modules in assay-core contain doc comments only. These are pre-existing placeholders, not regressions.

---

## Verdict

**Status: PASSED.** All 52 requirements satisfied. All 15 phases verified. All cross-phase connections wired correctly. All E2E flows complete. Previous audit gaps fully resolved by phases 24-25. Remaining tech debt is minor (3 items, none blocking).
